# CLAUDE.md — 제3회 풍력발전량 예측 AI 경진대회 (BARAM 2026)

이 문서는 이 저장소에서 작업하는 모든 AI 어시스턴트(Claude Code, Codex 등)가 반드시 따라야 하는 총괄 규칙이다.

## 1. 프로젝트 개요

- **목표**: 강원도 태백 가덕산 풍력발전단지의 KPX 그룹 3개(`kpx_group_1/2/3`)에 대해 2025년 전체 기간의 **시간별 발전량(kWh)** 예측
- **평가지표**: `Score = 0.5 × (1 - NMAE) + 0.5 × FICR`
  - 실제 발전량이 설비용량의 10% 이상인 시간대만 평가
  - FICR: 오차율 ≤6% → 4원/kWh, ≤8% → 3원/kWh, 초과 → 0원 (계단 함수)
- **설비용량(1시간 환산)**: group_1 = 21,600 kWh, group_2 = 21,600 kWh, group_3 = 21,000 kWh
- **제출 마감**: 2026-08-14(금) 10:00, 일일 최대 5회 제출
- **2차 평가 요건**: train/inference 코드 분리, Private Score 재현 가능, 외부 API 추론 금지, pretrained 모델은 2026-07-05 이전 공개 가중치만 허용

## 2. 프로젝트 철학 (반드시 이해하고 시작)

이 프로젝트의 목적은 단순히 머신러닝 모델을 만드는 것이 아니다.

목표는 **2025년의 풍력발전량을 실제 운영 환경과 가장 가깝게 예측하는 것**이다.

따라서 이 프로젝트에서는 모델 자체보다 **데이터를 이해하는 과정**을 더 중요하게 생각한다.

좋은 모델은 좋은 데이터 이해에서 시작된다.

### 2.1 왜 EDA를 먼저 하는가?

풍력발전량은 일반적인 회귀 문제가 아니다. 발전량은 단순히 풍속만으로 결정되지 않으며, 다음이 함께 영향을 준다.

- 기상 조건
- 터빈 특성
- 출력 제한(Curtailment)
- 정비
- 계통 제약
- 계절성
- 시간대
- 운영 정책

데이터를 이해하지 못하면 어떤 Feature를 만들어야 하는지, 어떤 Validation을 해야 하는지, 어떤 모델이 적합한지 판단할 수 없다. **모델을 먼저 만드는 것은 금지한다.**

### 2.2 왜 시계열 분리를 먼저 설계하는가?

이번 대회는 과거 데이터를 이용하여 미래인 2025년을 예측하는 문제이다. Train과 Validation을 잘못 나누면 미래 정보를 과거가 학습하는 Data Leakage가 발생한다. Data Leakage가 발생하면 Validation 점수는 높아지지만 실제 리더보드 성능은 크게 떨어질 가능성이 높다. Validation은 반드시 실제 운영 환경을 최대한 모방해야 한다.

### 2.3 왜 연도별 Drift를 확인하는가?

Machine Learning은 Train과 Test의 데이터 분포가 비슷하다는 가정을 가진다. 하지만 풍력발전 데이터는 매년 기상 조건이 달라질 수 있다 (태풍, 강수량, 평균 풍속, 계절성, 운영 정책 등). 만약 2022년과 2024년의 데이터 분포가 크게 다르다면, 2022년 데이터를 많이 사용하는 것이 오히려 성능을 떨어뜨릴 수도 있다. 따라서 EDA 단계에서 연도별 Drift를 반드시 확인하고, 그 결과를 기반으로 Validation 전략과 학습 데이터 범위를 결정한다.

### 2.4 왜 출력 제한(Curtailment)을 찾는가?

풍속이 충분해도 발전량이 낮은 경우가 존재한다. 이는 출력 제한, 계통 제약, 정비, 운영 정책 등 때문일 수 있다. 이러한 데이터는 이상치가 아니라 실제 발전소 운영 데이터이므로 삭제하지 않고 패턴을 이해해야 한다.

### 2.5 왜 Feature Engineering보다 데이터 이해가 먼저인가?

좋은 Feature는 데이터를 이해한 결과물이다. EDA 없이 Feature를 만드는 것은 근거 없이 변수를 만드는 것과 같다. Feature는 반드시 도메인 지식, 논문, EDA 결과를 근거로 생성한다.

### 2.6 모델은 마지막에 선택한다

이 프로젝트에서는 다음 순서를 반드시 지킨다 (세부 로드맵은 3장 참조).

1. 데이터 이해
2. 데이터 검증
3. EDA
4. Drift 분석
5. Validation 설계
6. Feature Engineering
7. Baseline 구축
8. 모델 선택
9. Hyperparameter Tuning
10. Ensemble

절대로 모델부터 바꾸면서 성능을 올리려고 하지 않는다. 모델보다 데이터를 이해하는 것이 성능 향상에 훨씬 큰 영향을 준다.

### 2.7 모든 결정은 근거 기반으로 한다

프로젝트 내에서 이루어지는 모든 결정은 다음 네 가지 중 하나 이상의 근거를 가져야 한다.

- 데이터 분석 결과(EDA)
- 풍력발전 도메인 지식
- 논문 및 공식 문서
- 실험 결과(Validation)

"좋을 것 같다.", "많이 쓰니까.", "유명한 방법이다."와 같은 이유만으로는 어떠한 Feature, 모델, 전처리도 채택하지 않는다. 모든 선택은 **왜 그렇게 결정했는지를 설명할 수 있어야 한다.**

## 3. 폴더 구조

```
wind_foreset_codex/
├── CLAUDE.md              # 이 문서 (총괄 규칙)
├── HANDOFF.md             # 세션 인수인계 문서 (매 세션 종료 시 갱신)
├── .claude/skills/        # 프로젝트 스킬
├── .gitignore
├── venv/                  # 가상환경 (git 제외)
├── data/
│   ├── train/             # gfs/ldaps/scada/labels 원본
│   ├── test/              # gfs/ldaps 테스트
│   ├── processed/         # 전처리 캐시 (parquet)
│   ├── data_description.md
│   ├── info.xlsx
│   └── sample_submission.csv
├── src/
│   ├── metric.py          # 대회 공식 평가 산식 (수정 금지)
│   ├── submission.py      # build_submission()/save_submission()/validate_submission() — 제출 전 필수 검증
│   └── nn.py              # MLP(LightGBM 분위수회귀와 블렌드되는 신경망) 구조·FICR 손실·학습 루프
├── notebooks/
│   ├── 01_preprocessing.ipynb    # 원본 CSV 로딩 → pivot → 조인 → parquet 캐시
│   ├── 02_eda.ipynb              # 이용률·파워커브·결측·격자상관 등 데이터 탐색
│   ├── 03_features.ipynb         # 179개 피처(허브고도 외삽·파워커브·결빙 등) 생성
│   ├── 04_model_selection.ipynb  # 8개 후보(Ridge/LightGBM/XGBoost/CatBoost 등) 공정 비교 → LightGBM 채택
│   ├── 05_tuning.ipynb           # 그룹별 τ·actual가중·Optuna 튜닝(산식-인지 학습)
│   ├── train.ipynb               # 2차 평가 제출용 — 그룹별 완전분리 LightGBM+MLP 블렌드 학습
│   └── inference.ipynb           # 2차 평가 제출용 — train.ipynb 모델 로딩·예측·제출 파일 생성
├── reports/               # 단계별 분석 보고서 (01_preprocessing/02_eda/03_features/04_model_selection/05_tuning/train)
├── models/                # 학습된 모델 아티팩트 (git 제외, final/ 하위에 LightGBM·MLP·config.json)
├── submissions/           # 제출 CSV (날짜_버전_설명.csv)
└── archive/               # 이전 구조 보존 — notebooks/(01~06 탐색+구v5 train/inference), reports/, src/nn_v5.py, models/
```

## 4. 진행 로드맵 (반드시 이 순서)

1. 문제와 평가 지표 확인 → `src/metric.py`, `src/submission.py` 셋업
2. 학습·검증 데이터 분리 — **시계열이므로 과거로 학습, 미래로 검증** (전처리 전에 분리 설계). 확정된 방식은 5장 "검증 전략" 참조
3. EDA — 결측/이상치/분포/타깃 관계/시간 순서/그룹 구조
4. 기본 전처리 — `01_preprocessing.ipynb`, train+test 동일 로직 → `data/processed/` parquet 캐시
5. 베이스라인 — 단순 모델(평균, 선형회귀, LightGBM 기본값)로 비교 기준 확립
6. 피처 엔지니어링 및 선택 — 도메인 근거 기반 (`wind-domain-features` 스킬 참조)
7. 모델 선택 — 동일한 검증 방식으로 비교
8. 하이퍼파라미터 튜닝 — 유망 모델만, 동일 CV 구조 유지
9. 최종 검증 및 앙상블 — seed 안정성, 오류 분석
10. 전체 학습 데이터로 재학습 후 예측

## 5. 절대 규칙 (위반 금지)

### 데이터 누수 (가장 중요)
- **예측기준시점 원칙**: 각 예측값은 그 시점 이전에 실제로 활용 가능했던 정보만 사용. 예보 데이터는 `data_available_kst_dtm` 기준으로 판단
- 평가 기간의 실제 발전량·SCADA·재분석자료 사용 금지
- 파생 변수/통계값/보간값에도 미래 정보 포함 금지
- 모든 피처 생성 시 `leakage-guard` 스킬로 자가 점검

### metric.py
- `src/metric.py`는 대회 공식 산식 그대로. **절대 수정하지 않는다**

### 검증 전략 (2026-07-29 확정)
- 학습/검증 분리는 **A안(2022~2023 학습 → 2024 검증, test와 동일한 "1년 전체" 구조)과 B안(확장 윈도우 3-fold: ~23.06/23.07-12 검증, ~23.12/24.01-06 검증, ~24.06/24.07-12 검증)을 항상 함께 계산해서 나란히 비교한다.** 둘 중 하나만 골라 쓰지 않는다
- 이유: A안만 쓰면 2024년이 이례적인 해였을 경우(연도별 drift, CLAUDE.md 2.3절) 검증이 2025년 실제 성능을 잘못 대표할 수 있고, B안만 쓰면 fold별 6개월 구간이 계절에 편중될 수 있다. 두 방식이 서로 다른 편향을 잡아주므로 함께 봐야 한다
- 적용: 모델 비교·피처 선택·튜닝·앙상블 실험표에는 항상 A안 점수와 B안 3-fold 점수(평균·표준편차)를 같이 기록한다. 한쪽에서만 개선되고 다른 쪽은 그대로거나 악화되면 "효과 불확실"로 보고 채택하지 않는다
- 세부 근거와 배경은 `timeseries-validation` 스킬 1절 참조

### Git
- `git add / commit / push`는 민석님이 직접 실행한다. AI는 커밋 타이밍과 메시지 문구를 **제안만** 한다
- 저장소 명칭·파일명은 ASCII만 사용

### 실행 방식
- 셀 실행은 민석님이 직접 한다. AI는 코드를 작성/수정하고, 실행 결과(shape, head() 등)를 받은 뒤 다음 단계를 안내한다
- 무거운 연산을 자율 실행하지 않는다

## 6. 작업 방식

- **노트북 우선**: 주 작업 공간은 `.ipynb`. 필수 `.py`는 `src/metric.py`, `src/submission.py`뿐
- **가상환경**: 모든 패키지는 `venv` 안에서만 설치. 새 패키지 설치 시 `requirements.txt` 갱신 제안
- **근거 기반**: 모든 전처리·피처·모델 결정은 논문, 풍력 도메인 지식, 데이터 근거를 명시한다. "그냥 해보자"는 금지. 근거 예: 발전량 ∝ 풍속³, 파워커브, 윈드시어 멱법칙 등
- **중간 결과 확인**: 각 단계마다 shape 변화, 샘플 출력, 분포를 확인하고 진행
- **설명 언어**: 모든 설명은 평이한 한국어. 기술 용어는 괄호 안에 정의나 비유로 인라인 설명 (세부 규칙은 아래 6.2)
- **보고서**: 각 단계 완료 시 `reports/`에 Why / How / Result / So-what 구조로 문서화. How는 처리 단계 하나당 하위 항목 하나

### 6.1 이중 관점 검토 — 데이터 사이언티스트 + 풍력 도메인 전문가

이 프로젝트는 **데이터 사이언티스트 관점**과 **풍력발전 도메인 전문가 관점**을 동시에 만족해야 진행한다. AI는 모든 주요 의사결정(피처 생성, 전처리, 모델 선택, 튜닝, 오류 분석 등)에서 두 관점을 함께 점검하고, 결과를 제시할 때도 두 관점을 구분해서 설명한다.

- **데이터 사이언티스트 관점 체크**: 통계적으로 타당한가? (분포, 검증 방식, 누수 여부, 과적합, 유의성) 산식(NMAE/FICR)에 실제로 도움이 되는가?
- **풍력 도메인 전문가 관점 체크**: 물리적·공학적으로 말이 되는가? (파워커브, 윈드시어, 컷인/컷아웃 풍속, 출력 제한, 터빈 정비, 계통 제약 등과 부합하는가)
- 두 관점이 충돌하는 경우(예: 통계적으로는 유의하지만 물리적 근거가 약한 피처) 그 충돌 자체를 명시하고, 판단 근거를 함께 제시한다. 어느 한쪽 관점만으로 결정을 내리지 않는다.
- 새로운 피처·전처리·모델을 제안할 때는 "데이터 사이언티스트로서는 ~", "도메인 전문가로서는 ~" 형태로 각 관점의 근거를 나눠 적는다.

### 6.2 설명 스타일 — 쉽고 친절하게

민석님은 모든 과정을 직접 이해하면서 진행한다. AI는 전문가끼리 대화하듯 쓰지 말고, **처음 배우는 사람에게 과외하듯** 설명한다.

- **결론 먼저, 이유는 그다음**: "이번엔 결측치를 채울 거예요. 왜냐하면 ~" 순서로. 무엇을 → 왜 → 어떻게
- **전문 용어는 그냥 쓰지 않는다**: 처음 등장하는 용어는 반드시 한 줄 정의 + 일상 비유를 붙인다
  - 예: "리샘플링(10분마다 찍힌 데이터를 1시간 단위로 합치는 것 — 10분 단위 가계부를 월별 가계부로 합산하는 느낌)"
  - 예: "early stopping(검증 점수가 더 이상 좋아지지 않으면 학습을 멈추는 것 — 성적이 안 오르면 야자 그만하는 것)"
- **코드 셀마다 "이 셀이 하는 일"을 사람 말로 먼저**: 코드보다 마크다운 설명이 먼저 온다. 코드 안에도 줄 단위 주석
- **수식·산식은 말로 풀어서 한 번 더**: 예: "NMAE = 오차를 설비용량으로 나눈 것. 즉 '전체 용량 대비 몇 % 틀렸나'라는 뜻"
- **선택지가 있으면 비교표 + 추천 + 이유**: "A안/B안이 있고, 저는 A안을 추천해요. 이유는 ~" 형태. 일방적으로 결정하지 않는다
- **한 번에 하나씩**: 긴 설명은 단계로 쪼개고, 단계마다 "여기까지 이해되셨으면 다음으로 갈게요" 흐름 유지
- **어려운 개념은 스스로 확인 질문 제공**: 설명 끝에 "이해 체크: ~를 한 문장으로 말할 수 있으면 OK" 한 줄
- 단, 쉬운 설명이 **정확성을 희생하면 안 된다**: 비유가 개념과 어긋나는 부분이 있으면 "정확히는 ~"로 보정한다

## 7. 세션 연속성 (학교 ↔ 집)

- 매 세션 종료 시 `HANDOFF.md`를 갱신한다 (`session-handoff` 스킬 참조)
- 새 세션 시작 시 AI는 가장 먼저 `HANDOFF.md`를 읽고 현재 상태를 요약한 뒤 시작한다
- 커밋 제안 시 HANDOFF.md 갱신 여부를 함께 확인한다
- **HANDOFF.md는 짧은 요약이 아니라 "이 문서만 읽고도 실수 없이 이어갈 수 있는" 수준으로 자세히 쓴다.** AI가 이전에 실수한 적이 여러 번 있었기 때문에, 매번 처음부터 같은 시행착오를 반복하지 않도록 다음을 반드시 포함한다:
  - **이번 세션에서 발견한 버그·주의사항**: 코드 로직 함정(예: 특정 groupby 방식이 왜 잘못됐는지), 도구 사용 중 겪은 문제와 우회 방법, 사용자에게 받은 피드백(무엇을 지적받았고 왜인지)을 구체적으로 기록. "이런 게 있었다"가 아니라 "왜 틀렸는지 + 올바른 방법"까지 적어야 다음 세션에서 같은 실수를 피할 수 있다
  - **재사용 가능한 이름·규칙**: 노트북에서 정의한 함수/딕셔너리/컬럼 네이밍 규칙 등, 다음 노트북 작성 시 그대로 참고할 수 있는 것들을 나열
  - **결정 사항은 근거와 함께**: 결론만 적지 말고 "왜 그렇게 결정했는지"(수치·실험 결과)를 함께 적어, 다음 세션에서 그 결정을 재검증 없이 신뢰할 수 있게 한다
  - **미해결 질문은 트레이드오프까지**: 단순히 "~을 아직 안 정했다"가 아니라, 어떤 선택지들이 있고 각각의 장단점이 뭔지까지 적는다
  - 요약과 원본 보고서(`reports/*.md`)가 어긋나면 원본을 우선한다는 점도 HANDOFF.md 상단에 명시한다

## 8. 스킬 목록

**공통 스킬 (항상 적용)**

| 스킬 | 언제 사용 |
|---|---|
| `leakage-guard` | 데이터를 만지는 모든 순간 (피처, 분리, 보간, 스케일링) |
| `notebook-workflow` | 모든 노트북 작성 시 (셀 구성, 캐시, 보고서 규칙) |
| `session-handoff` | 세션 시작/종료, 커밋 제안 시 |

**단계별 스킬 (로드맵 순)**

| 로드맵 단계 | 스킬 |
|---|---|
| 1~2. 지표 확인·검증 분리 | `timeseries-validation` |
| 3. EDA | `eda-checklist` (+ 해석 시 `wind-domain-features`) |
| 4. 기본 전처리 | `preprocessing` |
| 5. 베이스라인 | `model-selection` |
| 6. 피처 생성 / 선택 | `wind-domain-features` / `feature-selection` |
| 7. 모델 선택 | `model-selection` |
| 8. 튜닝 | `model-tuning` |
| 9~10. 앙상블·최종 검증·재학습 | `ensemble-final` |

## 9. 제출 체크리스트

1. `src/submission.py`의 `validate_submission()` 통과 (forecast_id/시각 불변, 8760행, 결측·음수 없음, UTF-8)
2. 예측값 클리핑: `0 ≤ pred ≤ 그룹 설비용량`
3. 파일명: `submissions/YYYYMMDD_vN_설명.csv`
4. 제출 후 리더보드 점수를 HANDOFF.md에 기록
