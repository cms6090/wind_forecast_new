---
name: session-handoff
description: 세션 연속성(학교↔집 이동) 관리 스킬. 새 세션이 시작될 때, 작업을 마무리할 때, "오늘 여기까지", "이어서 하자", "정리하자" 같은 말이 나올 때, git 커밋을 제안할 때 반드시 사용한다. HANDOFF.md 갱신과 커밋 제안 절차를 담당한다.
---

# Session Handoff — 세션 인수인계 절차

민석님은 학교와 집을 오가며 작업하므로, 어느 기기에서 열어도 즉시 이어갈 수 있어야 한다. 연결 고리는 **git 저장소 + HANDOFF.md** 두 가지다.

## 1. 세션 시작 절차 (새 세션 첫 행동)

1. `HANDOFF.md`를 읽는다
2. `git log --oneline -5`와 `git status`를 확인한다 (읽기 명령은 AI가 실행 가능)
3. 현재 상태를 3줄 이내로 요약해 민석님에게 보고: "지난 세션에서 ~까지 했고, 다음 할 일은 ~입니다"
4. HANDOFF.md의 "다음 할 일" 첫 항목부터 제안한다

## 2. 세션 종료 절차 ("정리하자" / 작업 단위 완료 시)

1. HANDOFF.md를 아래 템플릿으로 갱신한다
2. 커밋을 **제안**한다 (메시지 문구 포함). `git add/commit/push`는 민석님이 직접 실행한다
3. push까지 되었는지 확인 요청 — push 안 하면 다른 기기에서 이어갈 수 없다

## 3. HANDOFF.md 템플릿

**분량 원칙**: 짧은 요약이 아니라 "이 문서만 읽고도 실수 없이 이어갈 수 있는" 수준으로 쓴다. AI가 과거에 같은 실수를 반복한 적이 있으므로, 특히 "버그·주의사항" 섹션은 절대 생략하지 않는다 — "무엇이 틀렸다"뿐 아니라 "왜 틀렸는지 + 올바른 방법"까지 적는다. 요약과 원본 보고서(`reports/*.md`)가 어긋나면 원본을 우선한다는 점도 상단에 명시한다.

```markdown
# HANDOFF — 마지막 갱신: YYYY-MM-DD HH:MM (장소: 학교/집)

## 새 세션 시작 시 가장 먼저 할 것
1. 이 파일 전체를 읽는다 (특히 "버그·주의사항" 섹션)
2. `git log --oneline -5`, `git status` 확인
3. 관련 `reports/*.md` 원문을 훑어 결론을 재확인한다
4. 3줄 이내로 상태 보고 후 "다음 할 일" 1번부터 제안

## 현재 위치
- 로드맵 단계: (예: 4. 기본 전처리)
- 작업 중 파일: notebooks/01_preprocessing.ipynb (셀 ~12까지 실행 완료)

## 지난 세션(들)에서 한 것 — 누적 요약
- (완료 항목을 노트북/보고서 단위로, 지난 세션들 내용도 이어서 축적)

## 다음 할 일 (우선순위순)
1. (한 줄로 뭉뚱그리지 말 것 — "무엇을 어떻게"까지 구체적으로. 특히 "추가"인지 "교체/수정"인지, 대상 범위(파일/셀/섹션)가 어디까지인지 명시. 짧게 쓰면 다음에 다르게 해석해서 엉뚱하게 처리한 실제 사례가 있었음 — 예: "boxplot 말고 실제 분포를 그려보고 싶었어"를 "boxplot을 히스토그램으로 교체"로 잘못 해석했으나 의도는 "boxplot은 유지하고 히스토그램을 추가"였음)
2. ...

## 결정 사항 / 근거 (누적)
- (결론만 적지 말고 왜 그렇게 결정했는지 — 수치·실험 결과 — 함께 적는다. 예: 결측 보간은 같은 발표분 내 선형보간만 — leakage-guard 판정 완료)

## 노트북 재사용 가능한 이름·규칙 (해당되는 경우)
- (다음 노트북에서 그대로 참고할 함수명/컬럼 네이밍 규칙/딕셔너리 등)

## 이번까지 발견된 버그·주의사항 (같은 실수 반복 방지용 — 생략 금지)
- (코드 로직 함정: 무엇이 왜 틀렸는지 + 올바른 방법)
- (도구 사용 중 겪은 문제와 우회 방법)
- (사용자에게 받은 피드백: 무엇을 지적받았고 왜인지)

## 실험 기록
| 날짜 | 실험 | 로컬 Score | 리더보드 | 결론 |
|---|---|---|---|---|

## 미해결 질문
- (다음 세션에서 토론할 것 — 선택지와 각각의 트레이드오프까지)

## 환경 메모
- venv 패키지 변경: (있으면 requirements.txt 갱신 여부)
```

## 4. 커밋 제안 규칙

- 타이밍: 노트북 하나 완성, 캐시 생성, 보고서 작성, HANDOFF 갱신 시점
- 메시지 형식 제안: `[단계번호] 요약` (예: `[01] 전처리 노트북: GFS/LDAPS 병합 및 parquet 캐시`)
- 커밋 제안 시 반드시 체크: ① HANDOFF.md 갱신됨? ② `data/processed/`·`venv/`가 .gitignore에 있음? ③ 노트북 출력 정리 여부
- 대용량 파일(parquet, csv 원본)은 커밋 금지 — .gitignore 유지. 캐시는 각 기기에서 노트북 재실행으로 재생성한다

## 5. 기기 간 동기화 주의

- 데이터 원본(`data/`)과 캐시(`data/processed/`)는 git에 없으므로 양쪽 기기에 각각 존재해야 한다. 새 기기 셋업 시: clone → venv 생성 → `pip install -r requirements.txt` → 데이터 복사 → `01_preprocessing.ipynb` 실행으로 캐시 재생성
- 이 셋업 절차가 README.md에 있는지 주기적으로 확인한다
