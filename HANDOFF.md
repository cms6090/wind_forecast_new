# HANDOFF — 마지막 갱신: 2026-08-01 밤 (장소: 집, 세션 3회차 종료 — 내일 이어감)

## 새 세션 시작 시 가장 먼저 할 것
1. 이 파일 전체를 읽는다 (특히 "이번까지 발견된 버그·주의사항")
2. **바로 아래 "🚀 내일 할 일"부터 본다.** 무엇을·어떤 셀에·왜 하는지 다 적혀 있다
3. `git log --oneline -5`, `git status`로 커밋 상태 확인. **학교↔집 이동 시 push 여부 반드시 확인**
4. `reports/04_model_selection.md`, `reports/02_eda.md`, `reports/03_features.md`를 훑는다 (**이 문서의 요약보다 원본 리포트가 항상 더 정확하다 — 어긋나면 리포트를 우선한다**)
5. `04_model_selection.ipynb`는 **185셀, 1~19절**. **9~11절의 저장된 출력은 누수 상태 숫자라 인용 금지** — 13절 이후만 유효
6. 새 기기라면 `venv` 새로 만들고 `pip install lightgbm xgboost catboost scikit-learn` + `pip install torch --index-url https://download.pytorch.org/whl/cpu` 필요 (`requirements.txt` 아직 없음)

---

# 🚀 내일 할 일 (2026-08-02~, 마감 2026-08-14 10:00)

## 0. 현재 위치 요약

| | 값 |
|---|---|
| **최신 제출** | `submissions/20260801_v2_wsgbdt_mlpblend_q60.csv` |
| **리더보드** | **0.6288** (1-NMAE 0.8595 / FICR 0.3981) |
| 로컬 B안 평균 | 0.6321 |
| **로컬 → 리더보드 오프셋** | **-0.003 (2회 연속 일정)** |
| 오라클 상한(완벽한 바람) | 0.8618 |

### ⭐ 로컬-리더보드 관계가 확립됐다 (2회 실측)
| 제출 | 로컬 B평균 | 리더보드 | 차이 |
|---|---|---|---|
| v1 (16절, GBDT 풍속) | 0.6298 | 0.6271 | -0.0027 |
| v2 (18절, +MLP 블렌드) | 0.6321 | 0.6288 | -0.0033 |

**⇒ 리더보드 ≈ 로컬 B안 평균 − 0.003.** 이제 제출 없이 로컬로 판단해도 된다.
**⇒ 다중비교 편향을 감안해 "B안 평균 +0.003 미만의 개선"은 개선으로 세지 말 것.** (B안 fold 간 표준편차가 0.02라 검정력이 낮다)

## 1. ⭐⭐ 최우선 — 20절: 터빈 가동률로 라벨 정제

### 왜 (근거는 이미 실측했다)
원본 SCADA에 **터빈별 발전량·풍속·풍향**이 전부 있다(`data/train/scada_vestas_train.csv` 12대, `scada_unison_train.csv` 5대). "자기 나셀 풍속 ≥ 5 m/s인데 출력이 정격의 1% 미만"을 정지로 판정해 세어본 결과:

| 그룹 | 평균 가동 | 전대수 가동 비율 | 1대 이상 정지 |
|---|---|---|---|
| kpx_group_1 | 5.76 / 6 | 83.4% | 11.3% |
| kpx_group_2 | 5.78 / 6 | 84.3% | 8.8% |
| kpx_group_3 | 4.78 / 5 | 48.8% | 6.8% |

**같은 SCADA 풍속 구간 안의 발전량 산포를 가동대수로 보정하면:**

| 그룹 | 구간 내 σ(용량대비) | 가동대수 보정 후 | 감소 |
|---|---|---|---|
| kpx_group_1 | 0.0745 | **0.0583** | **-22%** |
| kpx_group_2 | 0.0612 | **0.0483** | **-21%** |
| kpx_group_3 | 0.0783 | **0.0511** | **-35%** |

**14절에서 "예측 불가 성분"이라 부르며 포기했던 것의 20~35%가 사실은 "터빈 정지"였다.** 예측 불가가 아니라 우리가 안 본 정보였다.

### 왜 FICR에 특히 유리한가 (의사결정 이론)
- 지금 모델은 "정지 있는 시간"과 "정상 가동 시간"을 **섞어서** 배워, 예측이 모든 시간대에 조금씩 아래로 번져 있다
- 라벨을 가동률로 정규화해 학습하면 **"전 터빈 정상 가동"의 깨끗한 관계**를 배운다
- 정상 가동 시간(83~84%)은 정확히 맞히고, 정지 시간(8~11%)은 크게 틀린다 — **그런데 어차피 지금도 틀린다.** group_1에서 1대 정지 = 용량의 16.7% 손실인데 FICR 밴드는 ±6%라 지금 방식으로도 절대 못 들어간다
- **FICR은 계단 함수라 "85%를 정확히"가 "100%를 조금씩 틀리게"보다 낫다**
- 의사결정 이론적으로도 정당: 가동률 분포가 "85%는 100%, 나머지는 그 아래"이므로 **중앙값(L1 최적)도 최빈값(FICR 최적)도 둘 다 "정상 가동 예측"** 을 가리킨다
- **부수 가설**: τ=0.60이 도움됐던 이유 중 하나가 이것일 수 있다(정지로 아래로 번진 라벨을 보정). 라벨을 정제하면 **최적 τ가 0.5 쪽으로 돌아오면서 더 깨끗한 개선**이 나올 수 있다 → 20절에서 τ를 다시 훑을 것

### 정확히 무엇을 하나 (20절 신규 추가, 기존 셀은 건드리지 않음)
1. **20-1**: 가동률 계산 (아래 재현 코드 그대로). `data/train/scada_*.csv`를 읽어 시간별 `avail_{group}`(0~1) 시리즈를 만든다. **train에만 존재하며 피처가 아니라 "라벨 정제용"이다**
2. **20-2**: 세 가지 라벨 처리를 비교 (전부 fold-safe — 가동률은 train 라벨과 같은 시점 정보라 검증 구간 라벨을 안 쓰면 됨)
   - `A_asis`: 현행 (기준선, `W_gbdt_l2` 재현)
   - `B_norm`: 라벨을 `y / max(avail, 0.2)`로 정규화해 학습 → 예측은 그대로 사용(= 정상 가동 예측)
   - `C_dropdown`: 정지 시간대(`avail < 1`)를 **학습에서 제외**하고 나머지로만 학습
   - `D_weight`: 정지 시간대에 표본가중을 낮춤(`weight *= avail**2` 등)
3. **20-3**: 최고안에서 **τ를 0.50/0.55/0.60/0.65 재탐색** (위 부수 가설 검증)
4. **20-4**: 최종 구성으로 제출 파일 v3 생성 (19절 코드 재사용, 라벨 처리만 교체)

### 재현 코드 (그대로 붙여 쓸 것 — 이미 검증됨)
```python
v = pd.read_csv(REPO_ROOT/"data/train/scada_vestas_train.csv", parse_dates=["kst_dtm"])
u = pd.read_csv(REPO_ROOT/"data/train/scada_unison_train.csv", parse_dates=["kst_dtm"])
GRP = {"kpx_group_1": [("vestas", i) for i in range(1, 7)],
       "kpx_group_2": [("vestas", i) for i in range(7, 13)],
       "kpx_group_3": [("unison", i) for i in range(1, 6)]}
SRC = {"vestas": v, "unison": u}
avail = {}
for g, turbs in GRP.items():
    rated10 = CAPACITY_KWH[g] / len(turbs) / 6.0        # 터빈 1대의 10분 최대 발전량(kWh)
    acc = None
    for pre, i in turbs:
        d = SRC[pre]
        p, w = d[f"{pre}_wtg{i:02d}_power_kw10m"], d[f"{pre}_wtg{i:02d}_ws"]
        down = ((w >= 5.0) & (p <= rated10 * 0.01)).astype(float)   # 바람은 있는데 출력 없음 = 정지
        ok = 1 - down
        ok[w.isna() | p.isna()] = np.nan
        acc = ok if acc is None else acc + ok
    s = pd.Series(acc.to_numpy(), index=SRC[turbs[0][0]]["kst_dtm"])
    avail[g] = s.resample("h", closed="right", label="right").mean() / len(turbs)  # 0~1
```
**주의**: SCADA 1시간 집계는 `resample("h", closed="right", label="right")` 규칙을 그대로 지킬 것(01_preprocessing과 동일).

**⚠️ 리스크**: 이 정지 판정 기준(`ws>=5 & power<=1% 정격`)은 **curtailment(출력 제한)와 정비를 구분하지 못한다.** 둘 다 "바람은 있는데 출력 없음"이기 때문. curtailment는 이미 3구간을 라벨에서 뺐지만 미탐지분이 남아 있을 수 있다. 20-1에서 **정지 시간대의 월별·시간대별 분포**를 찍어보고, 특정 기간에 몰려 있으면 curtailment 의심 구간으로 따로 기록할 것.

## 2. 그다음 — 21절: 풍속 모델 입력에 도메인 피처 3종 추가 (비용 낮음)

전부 **풍속 모델(`build_wind_input_frame`)의 입력**에 넣는 것이다. 발전량 모델이 아니다.

1. **대기 안정도 지표** ⭐ — 안정도는 **윈드시어를 좌우**한다. 즉 "10m 예보에서 117m 풍속을 얼마나 잘 추정하나"를 물리적으로 결정한다. 안정한 야간엔 시어가 크고 불안정한 주간엔 작다. `blh`(경계층 높이)는 이미 피처에 있으나 안정도 지표는 없다. **벌크 리처드슨 수 근사**: LDAPS의 2m 기온과 상층 기온(있는 높이 확인 필요), 풍속 차이로 계산. **풍속 모델 입력으로 이보다 적합한 변수가 드물다**
2. **난류강도(TI)를 제대로** — TI = σ_u / U. 현재 `gust_proxy`는 **분모(U)로 안 나눈 raw 변동폭**이라 풍속과 뒤섞여 gain이 낮았다(0.037~0.231%). 13절의 `roll7_std`를 `roll7_mean`으로 나누면 바로 TI가 된다. TI는 파워커브 산포의 주원인(높은 TI는 ramp 구간 출력을 평활화하고 정격 근처를 낮춤)
3. **파워커브 입력에 밀도보정 적용** — IEC 61400-12-1 표준은 **밀도보정한 풍속을 파워커브에 넣으라**고 한다. 지금 `ws_est_corrected`를 만들어놓고 정작 파워커브에는 원본 `ws`를 넣고 있다. `build_frame_with_wind`/`build_frame_given_pc`에서 `apply_power_curve_oracle(ws)` → `apply_power_curve_oracle(ws * (rho/1.225)**(1/3))`로 바꾸면 끝. **비용 거의 0**

## 3. ‼️ 늦어도 다음 주 초 — train.ipynb / inference.ipynb 분리

**2차 평가 필수 요건**이다(train/inference 코드 분리, Private Score 재현 가능, 외부 API 추론 금지). **19절에 전체 경로가 완성돼 있어 지금이 옮기기 가장 쉬운 시점이다.**
- `train.ipynb`: 전체 train으로 ① 풍속 모델(GBDT l2 결정적 + MLP) ② 파워커브 ③ 발전량 모델(seed 5개) 학습 → `models/final/`에 저장 + `config.json`
- `inference.ipynb`: 저장된 모델 로딩 → test 예보에 적용 → 블렌드 → `submissions/` CSV
- 재사용할 함수: `build_wind_input_frame`, `fit_final_wind`, `fit_final_mlp_wind`, `fit_power_curve_oracle`/`apply_power_curve_oracle`, `build_frame_given_pc`, `_lgbm_train`/`_lgbm_train_rand`
- **CWD 함정 주의**: `src/submission.py`의 `SAMPLE_SUBMISSION_PATH`/`SUBMISSIONS_DIR`은 상대경로다. 19절 첫 셀의 `REPO_ROOT` 패턴을 반드시 가져갈 것
- `requirements.txt`도 이때 함께 작성

## 4. 남은 큰 카드 (시간이 되면)

- **격자 CNN 풍속 모델** — 유일하게 남은 구조적 카드. 850개 평평한 피처가 공간 구조를 못 살린다. 비용 크고 불확실. **1~3번을 끝낸 뒤에만 착수할 것**
- **강풍 구간 표본 가중** — 2025 예보 풍속이 train 3년보다 강한데(5.107 vs 4.74~4.98) 약풍 분포로 학습 중. 공변량 이동 대응
- **분포 예측 + 기대 FICR 최대화** — 14절에서 "위치 최적화는 다 짜냈다"가 확인돼 기대는 +0.005 안팎
- **풍향×풍속 상호작용** (후류 간접 반영, 미해결 질문 5번)

---

## 📋 이중 관점 평가 (2026-08-01, DS + 풍력 도메인)

### 데이터 사이언티스트 관점 — 잘한 것
1. **검증 설계가 실증됐다** — B안 평균이 리더보드를 0.003 이내로 2회 연속 예측. 대부분의 대회가 로컬-LB 괴리로 고생하는데 그 다리를 놓았다
2. **누수를 두 번 잡고 세 번째를 막는 구조를 만들었다** — 열거 대신 규칙(`assert_no_leak`)
3. **대조군 설계로 잘못된 결론을 두 번 막았다** — 단조제약(실제로는 huber alpha), avg12(실제로는 볼록성 함정)
4. **오라클 실험이 최고의 판단** — 12번 학습으로 방향 전체를 바꿨다. 안 했으면 발전량 모델 튜닝에 2주를 썼을 것(15절이 +0.0037이었던 걸 보면)
5. **실패를 정직하게 기록** — 스크리닝 지표, 하이퍼파라미터 튜닝, 시드 앙상블 설계 실수

### 데이터 사이언티스트 관점 — 부족한 것 / 리스크
1. **⚠️ 다중 비교 문제를 관리하지 않았다.** 검증 세트에서 30개 이상 변형을 비교했고, 그중 최고를 고르는 행위 자체가 낙관 편향을 만든다. 로컬-LB 격차 -0.003이 그 크기를 시사
2. **fold가 3개뿐이라 검정력이 낮다.** B안 fold 간 표준편차가 **0.02**인데 채택한 개선들은 0.002~0.016. **상당수가 통계적으로 유의하지 않다** (시드 앙상블 +0.0017, 그룹별 τ +0.0008, 리드타임 +0.0027은 노이즈일 수 있음)
3. **공변량 이동을 모델링하지 않았다.** 2025 예보 풍속이 train 3년보다 강한데 약풍 분포로 학습 → 강풍 구간은 외삽. `high(≥15)` 오차율이 0.22로 최악인 것과 연결
4. **불확실성을 전혀 정량화하지 않았다.** 점 예측만이고 τ는 전역 상수. 시각별 "이 예보는 못 믿겠다"를 반영 못 함
5. **진단이 잔차 평균·σ 위주.** 시간 자기상관, 풍향별·시간대별 조건부 편향은 한 번도 안 봤다

### 풍력 도메인 전문가 관점 — 잘한 것
1. SCADA 나셀 풍속을 정답 삼은 것(업계 표준 접근). 물리 공식 외삽 실패 후 데이터 기반 전환이 옳았다
2. 파워커브를 명시적 피처로 만든 것
3. curtailment 3구간 라벨 제외

### 풍력 도메인 전문가 관점 — 부족한 것
1. **⭐⭐ 터빈 가동 상태 완전 무시** — 위 "내일 할 일 1번" 참조. 가장 큰 미사용 정보
2. **밀도보정을 풍속에만 하고 파워커브 입력엔 안 씀** — IEC 61400-12-1 위반. 비용 0 수정
3. **대기 안정도 미사용** — 시어를 좌우하는 물리적 결정 요인인데 없다
4. **TI를 σ/U로 정규화 안 함** — `gust_proxy`가 풍속과 뒤섞여 gain이 낮았다
5. **후류를 "그룹 단위"라는 이유로 포기** — group_1/2는 같은 VESTAS 12대를 6대씩 나눈 것. 주풍향(WSW/W)일 때 상·하류 관계가 정해진다. 풍향×풍속 상호작용 미구현
6. **고풍속 구간이 여전히 최악인데 물리적 대응이 없다** — group_3 `high(≥15)` 오차율 0.221. 2025는 강풍 해이고 8~9월 태풍기도 있다

### ⭐ 두 관점의 충돌에서 얻은 교훈
도메인적으로 타당해 보였던 피처(`regime_calm/ramp/rated`, `high_wind_caution`, `풍속³`)가 **전부 gain 0.000%** 였다. 트리는 연속 변수를 알아서 구간으로 자르기 때문.
**⇒ 도메인 지식은 "어떤 변수를 만들지"에 쓰고, "어떻게 이산화할지"는 모델에 맡겨라.** 실제로 성공한 도메인 피처(`ws_est`, `power_curve_est`, `ws_est_corrected`)는 전부 **새 정보를 넣은 것**이지 기존 변수를 잘라낸 게 아니었다.

---

---

## ✅ 해결 완료 — `power_curve_est` 누수 (2026-08-01 발견 → 같은 날 수정·재실행 완료)

**측정된 누수 크기: A안 -0.0050 / B평균 -0.0045 (작았다).** `lgb_l1`(누수) 0.5965/0.5933 → `fix_base`(수정) 0.5915/0.5888. gain 1위를 차지하던 피처인데도 영향이 작았던 이유는 **fold-safe 버전(`power_curve_est_cv_*`)이 이미 거의 같은 정보를 담고 있어서** 원본이 추가로 준 정답 정보가 적었기 때문. 덕분에 9~11절의 **상대 비교 결론(단조제약 무효, LightGBM 채택, 가중·τ 효과)은 전부 유효**했다.

아래는 재발 방지를 위한 기록이다.

### 무엇이 잘못됐나
`04_model_selection.ipynb` 6절의 `leaky_cols_for_group(g)`가 반환하는 제외 목록에 **`f"{g}_power_curve_est"`(fold-safe가 아닌 원본)가 빠져 있었다.**

```python
# 버그 있는 원본 (6절)
def leaky_cols_for_group(g):
    base = [f"{g}_ws_est", f"{g}_ws_est_sq", f"{g}_ws_est_cube", f"{g}_ws_est_corrected",
            f"{g}_regime_calm", f"{g}_regime_ramp", f"{g}_regime_rated", f"{g}_high_wind_caution"]
    # ← f"{g}_power_curve_est" 가 없다!
```

`all_cv_cols_for_group(g)`는 `{g}_power_curve_est_cv_`처럼 **`_cv_` 접미사가 붙은 것만** 잡기 때문에, 접미사 없는 원본 `{g}_power_curve_est`는 어느 목록에도 안 걸려서 **그대로 모델 입력에 들어갔다.** 6절의 안전 점검 `assert`도 같은 목록을 기준으로 하니 못 잡았다.

`{g}_power_curve_est`는 `03_features.ipynb` 12절에서 **2022~2024 전체 라벨(발전량)로 적합한 파워커브**를 통과시킨 값이다. 즉 검증 구간의 정답이 피처 안에 들어 있다.

### 얼마나 심각한가
feature importance(10-3b, A안 fold)에서 원본 `power_curve_est`의 gain 비중:

| 그룹 | gain 비중 | 순위 |
|---|---|---|
| kpx_group_1 | 3.94% | 3위 |
| kpx_group_2 | **14.71%** | **1위** |
| kpx_group_3 | **13.84%** | **1위** |

group_2/3에서는 모델이 가장 많이 쓰는 피처였다. **9절·9-5절·10절·11절의 모든 숫자가 이 상태에서 나왔다.**

### 그래도 살릴 수 있는 결론
모든 변형이 **똑같은** 누수를 공유했으므로 **상대 비교(어느 모델·설정이 나은가)는 대체로 유효**할 가능성이 높다. 다만 **절대 점수는 낙관적으로 부풀려져 있고**, 특히 파워커브 피처 자체에 대한 판단(예: "group_3에서 파워커브가 gain 22%")은 신뢰하면 안 된다. 수정 후 재실행으로 전부 다시 확인해야 한다.

### 수정 방법 (이미 적용 완료)
**6절 셀을 제자리에서 고쳤다**(별도 v2 함수를 만들지 않음). `leaky_cols_for_group()`에 `f"{g}_power_curve_est"`를 추가하고, 진짜 방어선으로 **뿌리 컬럼 기반의 일반적 누수 점검** `assert_no_leak(columns, g, cv_suffix)`를 도입해 `build_group_feature_frame()` 안에서 매번 호출한다. 컬럼 이름에 `LABEL_DERIVED_MARKERS = ("ws_est", "power_curve_est")` 중 하나가 들어 있으면 `__`로 잘라 뿌리를 구한 뒤, 그 뿌리가 현재 fold의 허용 컬럼 2개(`{g}_ws_est_cv_{suffix}`, `{g}_power_curve_est_cv_{suffix}`)가 아니면 무조건 실패시킨다. 이 방식이었으면 원래 버그도 처음부터 잡혔다.

**검증 완료**(컬럼 이름 수준 시뮬레이션): 그룹×fold 9개 조합 전부에서 누수 컬럼 0개, 피처 수는 group_1/2 = 822개, group_3 = 821개(수정 전보다 각 1개 감소 = 빠졌던 `power_curve_est`). 13절 lag/lead를 켜면 정확히 +26개.

---

## 현재 위치
- 로드맵 단계: **7. 모델 선택 완료 → 8. 하이퍼파라미터 튜닝 시작 전** (`model-tuning` 스킬)
- **`reports/04_model_selection.md` 작성 완료** (Why/How/Result/So-what). 04단계의 정본이다
- `04_model_selection.ipynb` 12절(요약)도 채워짐
- 다음 노트북은 `05_tuning.ipynb`. 단 아래 "다음 할 일 0번(오라클 실험)"을 05를 시작하기 전에 먼저 하는 것을 권장
- 작업 중 파일: `notebooks/04_model_selection.ipynb` (**1~13절**)
  - 1~6절: 셋업·데이터 로드·피처 분류·fold 정의·fold-safe 파생 재계산·프레임 빌더 (**6절 셀은 2026-08-01에 누수 수정 완료 — 재실행 필요**)
  - 9절 헤더 앞에 **누수 경고 마크다운 셀**을 삽입해, 아래 9~12절 출력이 수정 전 숫자임을 문서에 박아뒀다
  - 7절: 베이스라인 사다리 4종 (실행 완료, 단 위 누수 포함)
  - 8절: 낡은 요약 (7절까지만 반영)
  - 9절: 후보 모델 7종 비교 (실행 완료, 누수 포함)
  - **9-5절: 단조 제약 진단 (실행 완료) — 결론 확정, 아래 참조**
  - **10절: 산식-인지 학습 (실행 완료, 누수 포함) — 표본 가중·분위수 τ 실험**
  - **11절: 앙상블 (실행 완료, 누수 포함)**
  - 12절: **최종 요약 (채워짐)**
  - **13절: 누수 수정 + 예보 lag/lead 피처 + 검증된 레버 결합 — 실행 완료. 이 절 숫자만 유효하다**
- `03_features.ipynb`(86셀, 1~13절), `02_eda.ipynb`, `01_preprocessing.ipynb`은 완료 상태 유지

---

## ⭐ 13절 최종 결과 (누수 수정 후 — **이것만 인용할 것**)

| 변형 | A안(2024) | B fold1 | B fold2 | B fold3 | B평균 |
|---|---|---|---|---|---|
| `fix_base` (누수 없는 출발점) | 0.5915 | 0.5665 | 0.5898 | 0.6102 | 0.5888 |
| `fix_dyn` (+예보 lag/lead) | 0.5991 | 0.5649 | 0.5981 | 0.6116 | 0.5915 |
| `dyn_w` (+표본가중) | 0.6171 | 0.5869 | 0.6089 | 0.6337 | 0.6098 |
| `dyn_q60` (+τ만) | 0.6174 | 0.5789 | 0.6137 | 0.6326 | 0.6084 |
| **`dyn_w_q60`** (가중+τ, 최고 단일) | **0.6252** | 0.5926 | 0.6086 | 0.6361 | **0.6124** |
| `dyn_w_q65` | 0.6214 | 0.5927 | 0.6057 | 0.6382 | 0.6122 |
| `dyn_w_q70` | 0.6138 | 0.5931 | 0.5927 | 0.6331 | 0.6063 |
| `dyn_mlp` | 0.5997 | 0.5567 | 0.5992 | 0.6077 | 0.5879 |
| **`dyn_w_q60` 0.7 + `dyn_mlp` 0.3 (최종)** | **0.6288** | – | – | – | **0.6170** |
| **총 개선폭** | **+0.0373** | | | | **+0.0282** |

### 레버별 기여도
| 레버 | A안 | B평균 | 판정 |
|---|---|---|---|
| 누수 제거 | -0.0050 | -0.0045 | 필수 |
| 예보 lag/lead | +0.0076 | +0.0027 | **조건부 유지** (fold1만 -0.0016 → 규칙상 "효과 불확실"이나 σ를 줄인 유일한 변형) |
| 표본 가중(actual) | +0.0180 | +0.0183 | **채택** |
| 분위수 τ=0.60 | +0.0183 | +0.0169 | **채택** (0.65 동률, 0.70 하락 = 꼭짓점 확인) |
| 가중 × τ 결합 | +0.0261 | +0.0209 | **채택** (단순 합보다 작음 = 두 레버가 겹침) |
| MLP 블렌드 0.3 | +0.0036 | +0.0046 | **채택** (단 아래 주의) |

### ⚠️ 블렌드 이득의 주의점
`dyn_w_q60`은 편향 **+0.054(과대)**, `dyn_mlp`는 **-0.042(과소)** 라서 섞으면 편향이 상쇄된다. **블렌드 이득의 상당 부분이 "모델 다양성"이 아니라 "우연히 편향이 반대여서"일 수 있다.** MLP에도 같은 가중·τ를 적용한 뒤 이득이 남는지 반드시 재확인할 것.

### ⭐ 오차 해부 — 위치 최적화는 끝났고, 남은 건 폭(σ)
| 변형 | 잔차 σ | 편향 | 밴드폭 ÷ σ |
|---|---|---|---|
| `fix_base` | 0.1708 | -0.0503 | 0.351 |
| `fix_dyn` | **0.1679** | -0.0502 | 0.357 |
| `dyn_w_q60` | 0.1728 | **+0.0539** | 0.347 |
| `dyn_mlp` | 0.1768 | -0.0419 | 0.339 |

- **이번 +0.028은 전부 "밴드의 좋은 위치로 옮기기"에서 나왔고 σ는 하나도 못 줄였다**(오히려 소폭 증가)
- 편향이 -0.050 → **+0.054로 반대편까지 넘어갔다.** τ=0.70이 하락한 것이 "더 밀 여지 없음"의 증거
- **남은 상방은 전부 σ에 있다.** σ 절반이면 통과율 27%→52%, 총점 +0.1 규모

### 그룹별 편향 (`dyn_w_q60`) — 그룹별 τ 필요
| 그룹 | 편향 | ≤6% 비중 | FICR |
|---|---|---|---|
| kpx_group_1 | +0.036 | 0.3036 | 0.3795 |
| kpx_group_2 | **+0.076 (과함)** | 0.3644 | 0.4432 |
| kpx_group_3 | +0.050 | 0.2462 | 0.3044 |

group_2는 원래 편향이 가장 작았던(-0.021) 그룹이라 같은 τ에 과대 쪽으로 밀렸다.

### 풍속 구간별 잔차 (`dyn_w_q60`)
| 그룹 | ramp(3~12) | rated(12~15) | high(≥15) |
|---|---|---|---|
| kpx_group_1 | +0.042 | -0.025 | **+0.103** |
| kpx_group_2 | +0.078 | +0.071 | +0.063 |
| kpx_group_3 | +0.061 | **-0.051** | +0.060 (오차율 0.2196, 전체 최악) |

group_3 rated는 누수 수정 전 -0.150 → -0.051로 크게 완화됐으나 **여전히 유일한 음의 편향 구간**.

---

## 9~11절 실행 결과 (⚠️ 누수 상태 — 상대 비교용으로만, 절대 점수 인용 금지)

### 7절 베이스라인 사다리
| baseline | A안(2024) | B fold1 | B fold2 | B fold3 |
|---|---|---|---|---|
| 0_시간x월평균(기상 미사용) | 0.4334 | 0.4188 | 0.4486 | 0.4183 |
| 1_물리파워커브(ML 없음) | 0.5825 | 0.5550 | 0.5653 | 0.6006 |
| 2_선형회귀(피처 8개) | 0.5797 | 0.5429 | 0.5614 | 0.6001 |
| 3_LightGBM기본값 | 0.5965 | 0.5697 | 0.5947 | 0.6154 |

0<1<3이고 2<1인 것은 버그가 아니라 **3차 다항식이 파워커브의 S자 비선형(컷인/정격 꺾임)을 못 따라가서** 생기는 정상 결과.

### 9절 + 9-5절 + 10절 + 11절 통합 (Score)
| 모델 | A안(2024) | B fold1 | B fold2 | B fold3 | B 평균 |
|---|---|---|---|---|---|
| **lgb_w_actual** (actual 가중) | **0.6175** | 0.5861 | 0.6058 | **0.6347** | **0.6089** |
| **lgb_q60** (τ=0.60) | 0.6151 | 0.5813 | 0.6151 | 0.6289 | 0.6084 |
| lgb_w_eval (채점대상 가중) | 0.6079 | 0.5789 | 0.6062 | 0.6272 | 0.6041 |
| lgb_q55 | 0.6092 | 0.5720 | 0.6079 | 0.6265 | 0.6022 |
| lgb_l1 + mlp (0.5:0.5) | 0.6048 | – | – | – | 0.5975 |
| GBDT3+MLP 블렌드(GBDT 0.7) | 0.6032 | 0.5730 | 0.6028 | 0.6127 | 0.5962 |
| lgb_l1 (기준선) | 0.5965 | 0.5697 | 0.5947 | 0.6154 | 0.5933 |
| catboost | 0.5983 | 0.5688 | 0.5903 | 0.6149 | 0.5913 |
| mlp | 0.6019 | 0.5652 | 0.6005 | 0.6019 | 0.5892 |
| lgb_q50 | 0.5921 | 0.5677 | 0.5892 | 0.6151 | 0.5907 |
| lgb_huber_auto_mono | 0.5897 | 0.5642 | 0.5886 | 0.6071 | 0.5866 |
| lgb_huber_auto | 0.5954 | 0.5557 | 0.5951 | 0.6060 | 0.5856 |
| xgboost | 0.5864 | 0.5648 | 0.5778 | 0.6073 | 0.5833 |
| lgb_q45 | 0.5851 | 0.5614 | 0.5828 | 0.5961 | 0.5801 |
| lgb_q40 | 0.5671 | 0.5456 | 0.5603 | 0.5828 | 0.5629 |
| **lgb_huber_a0.9** | **0.4177** | 0.4275 | 0.4334 | 0.4094 | 0.4234 |
| lightgbm_mono (9절) | 0.4177 | 0.4274 | 0.4334 | 0.4096 | 0.4235 |
| catboost_mono (9절) | 0.3666 | 0.3488 | 0.3820 | 0.3344 | 0.3551 |

---

## 확정된 결론 (근거 포함)

### 1. 단조 제약(monotonic constraint) — **채택하지 않음, 판정 종료**
- 9절에서 `lightgbm_mono` 0.4177/`catboost_mono` 0.3666으로 폭락했으나, **비교 조건이 불공정했다.** LightGBM이 `l1`(MAE)+단조제약 조합을 지원하지 않아 손실을 `huber`로 바꿨는데 **`alpha` 기본값 0.9를 그대로 뒀다.**
- 후버의 `alpha`는 **타깃과 같은 단위**(kWh)의 임계값이고, 선형 구간에서 그래디언트가 alpha로 잘린다. 타깃이 0~21,600 kWh인데 alpha=0.9면 그래디언트가 사실상 1/1000로 눌려 **2000 트리를 다 써도 학습이 안 된다.**
- **9-5절 진단으로 확정**: 제약을 뺀 `lgb_huber_a0.9`가 A안 **0.4177** / B평균 **0.4234** — 9절 `lightgbm_mono`(0.4177 / 0.4235)와 **소수점 넷째 자리까지 일치**. 범인은 손실함수 확정, 단조 제약은 무죄.
- **공정한 비교 결과**: `lgb_huber_auto_mono`(0.5897/0.5866) vs `lgb_huber_auto`(0.5954/0.5856) → A안에서 지고 B안에서 이기는 엇갈림 = **"효과 불확실"**. XGBoost의 공정 비교(0.5864→0.5870, 0.5833→0.5840)도 사실상 변화 없음.
- **도메인 해석**: 이미 `power_curve_est`(단조증가를 강제해 만든 피처)를 입력으로 넣었기 때문에 단조성 정보가 피처 안에 이미 있다. 제약을 또 거는 건 중복이다.
- ⚠️ 다음에 후버/분위수 손실을 쓸 때는 **alpha를 반드시 타깃 스케일에 맞춰 명시**할 것.

### 2. 표본 가중(sample weight) — **채택, 가장 큰 단일 개선**
- 산식 근거: ① `actual < 설비용량×10%`인 행은 **채점에서 아예 제외**된다 ② FICR은 `sum(actual×단가)/sum(actual×4)`로 **actual 가중**이다. 그런데 기존 학습은 모든 행을 똑같이 취급했다.
- `lgb_w_actual`(가중 = actual/capacity, 하한 0.1): **A안 +0.021, B평균 +0.016, 4개 fold 전부 개선.** 1-NMAE는 거의 그대로고 FICR이 0.327→0.367로 크게 올랐다 (원래 FICR을 겨냥한 레버이므로 의도대로).
- `lgb_w_eval`(채점대상 1.0/나머지 0.1)도 개선되지만 `w_actual`보다 약함.
- **하한 0.1을 두는 이유**: 예측 시점엔 어떤 행이 채점 대상인지 모른다(실제 발전량을 봐야 앎). 예보가 틀려서 "저풍속인 줄 알았는데 실제 발전량이 컸던" 행은 채점 대상이므로, 저풍속 구간을 완전히 버리면 위험하다.

### 3. 분위수 τ — **채택, 단 최적점 아직 못 찾음**
- τ 0.40→0.60까지 **단조 증가**(0.5629 → 0.6084). 꼭짓점이 안 나왔으므로 **0.65, 0.70을 반드시 더 봐야 한다.**
- 원리: FICR은 계단 함수라 예측 분포를 통째로 조금 옮기면 문턱을 넘나드는 표본이 크게 바뀐다. 모델이 채점 구간에서 **편향 -0.05(과소예측)** 이었으므로 위로 올리는 τ>0.5가 유리한 것이 근거와 일치.
- τ 방향 규칙: **τ가 크면 과소예측 벌점이 커져 예측이 위로 올라간다.**
- **그룹별 τ가 필요하다는 근거 확보**: τ=0.60 시점의 편향이 group_1 -0.031 / group_2 **+0.014(이미 과대로 넘어감)** / group_3 -0.030. group_2는 더 올리면 손해, group_1/3은 더 올려야 이득.

### 4. GBDT 라이브러리 — **LightGBM 채택**
- LightGBM(B평균 0.5933) ≈ CatBoost(0.5913) > XGBoost(0.5833). LightGBM/CatBoost 차이는 fold 표준편차(0.023)보다 훨씬 작아 무의미. **성능이 동률이면 훨씬 빠른 LightGBM**(CatBoost는 매우 느림).

### 5. MLP — 주모델 아님, **앙상블 자원으로는 최고**
- A안 0.6019로 단일 모델 1위였으나 B fold1에서 0.5652로 떨어짐(학습 구간이 가장 짧은 fold). 데이터 부족에 GBDT보다 취약.
- **잔차 상관이 GBDT 대비 0.84~0.89로 가장 낮다**(GBDT끼리는 0.93~0.95) = 다양성 최고.
- 현재 MLP는 **full-batch로 36~104 에폭 만에 멈추는 사실상 미학습 상태**인데도 GBDT급. 미니배치·학습률 스케줄을 넣으면 더 오를 여지가 크다.

### 6. 앙상블 — 이득 있으나 작음
- `lgb_l1`+`mlp` 0.5:0.5 → A안 0.6048 / B평균 0.5975 (기준선 대비 +0.008/+0.004). GBDT끼리 섞는 건 거의 무의미(상관 0.93+).
- 가중치는 **곡선이 완만한 구간의 둥근 값**을 고를 것. 검증 점수를 보고 0.63 같은 값을 고르면 검증 과적합.

### 7. 미해결 질문 4번 답 — **13절 구간 더미 피처들은 죽었다**
- `regime_calm/ramp/rated`, `high_wind_caution`, `풍속^3`이 전부 **gain 0.000%, 순위 584~743위**(전체 860개 중). 트리 모델은 연속 변수를 알아서 구간으로 자르므로 손으로 만든 구간 더미가 정보를 추가하지 못한다.
- 반대로 **`ws_est_corrected`(밀도보정 풍속)가 group_1에서 gain 33.98%로 1위**. `03_features.ipynb` 4절에서 "효과 미미(상관 +0.001)"로 판정했던 피처가 실제 모델에서는 주력이었다. **단변량 상관으로 피처 가치를 판정하면 안 된다**는 교훈.
- 결론: 구간 더미들은 제거해도 무방(해롭진 않으나 근거 없음). 단 **누수 수정 후 다시 확인** 필요.

### 8. 미해결 질문 6번 답 — 고풍속 리스크는 실재, 단 **그룹마다 정반대**
| 그룹 | ramp(3~12) 편향 | rated(12~15) 편향 | high(≥15) 편향 | high 표본수 |
|---|---|---|---|---|
| kpx_group_1 | -0.066 | -0.053 | **+0.066** | 49 |
| kpx_group_2 | -0.035 | +0.052 | **+0.052** | 399 |
| kpx_group_3 | -0.053 | **-0.150** | **-0.129** | 51 |
- group_3(UNISON)만 고풍속에서 심하게 **과소예측**(평균오차율 0.199/0.258, ≤6% 통과율 0.14/0.09). group_1/2는 과대예측.
- 12-4절에 기록한 파워커브 고풍속 리스크가 **실재하지만 방향이 그룹마다 다르다** → 일괄 클리핑/평활화는 부적절, **그룹별 보정**이 필요.

### 9. ⭐ 가장 중요한 전략적 통찰 — FICR은 "위치"가 아니라 "폭"의 문제
- 잔차 표준편차 σ ≈ **0.17 × 설비용량**. FICR 4원 밴드는 ±0.06 × 설비용량 = **±0.35σ**.
- 정규분포라면 통과율 ≈ 27%. **실측 actual가중 통과율 25~34%와 거의 일치.**
- 즉 **FICR이 0.33에 머무는 건 예측 위치가 틀려서가 아니라 오차의 폭 자체가 밴드보다 3배쯤 넓기 때문.** τ·사후보정으로 얻는 건 제한적(실제로 τ 0.5→0.6이 FICR 0.32→0.36, 유의미하지만 여기까지).
- **σ를 절반으로 줄이면 밴드가 ±0.70σ가 되어 통과율 27%→52%, 총점 +0.1 규모.** 지금까지 찾은 모든 레버를 합친 것보다 크다.
- **따라서 앞으로의 최우선 방향은 "산식 튜닝"이 아니라 "예측 오차 자체를 줄이는 것"** = 피처와 모델 구조. CLAUDE.md 2.5절("좋은 Feature는 데이터를 이해한 결과물")과 일치.

---

## 📌 18절 결과 (2026-08-01) — 풍속 GBDT는 포화, MLP 블렌드만 유효

**최종: `W_gbdt_l2` 0.7 + `M_mlpws` 0.3 → A안 0.6412 / B평균 0.6321 / B최솟값 0.6107** (단독 대비 +0.0023 / 최악 fold +0.0046)

### ❌ 실패 1 — "발전량 환산 오차" 스크리닝 지표 (내가 만든 것)
추정풍속·실측풍속을 **같은 파워커브에 통과시켜 발전량 단위 오차를 재는** 지표를 만들어, 발전량 모델 학습 없이 풍속 후보를 고르려 했다. **검증에서 실패.**

| 풍속 | 새 지표 | 실제 점수 |
|---|---|---|
| avg12 | **0.6999 (1위)** | 0.6286 (2위) |
| gbdt_l2 | 0.6985 (2위) | **0.6298 (1위)** |

하필 이 지표를 만든 이유였던 미묘한 케이스에서 순서가 뒤집혔다. 18-3에서도 지표 1위 `reg`(0.7002)가 실제로는 0.6262로 기본값(0.6298)보다 낮았다.
**실패 이유**: 실제 파이프라인에는 풍속 뒤에 **학습된 발전량 모델**(200피처+τ+표본가중)이 붙어 풍속의 체계적 편향을 부분적으로 **교정**한다. "환산 오차가 조금 큰 풍속"이라도 하류에서 교정하기 쉬운 형태면 최종 점수가 더 높을 수 있는데, 지표는 그 교정 가능성을 모른다.
**⇒ 큰 차이(ridge vs GBDT)는 잡지만 미세한 차이는 못 잡는다. 최종 판단은 반드시 실제 발전량 점수로.**

### ❌ 실패 2 — 풍속 하이퍼파라미터 튜닝 (효과 없음, 유용한 negative result)
| 변형 | A안 | B평균 |
|---|---|---|
| **`W_gbdt_l2`(LightGBM 기본값)** | 0.6391 | **0.6298** |
| `C_reg`(colsample .7/subsample .8/min_child 40/L2) | 0.6352 | 0.6262 |
| `C_wide_reg`(leaves 255 + 규제) | 0.6369 | 0.6229 |

5가지 방향(slow/wide/reg/wide_reg) 전부 기본값을 못 이겼다. early stopping이 트리 33~282개에서 걸린 것도 "데이터의 신호를 이미 다 뽑았다"는 신호.
**⇒ 풍속 GBDT 튜닝에 더 시간 쓰지 말 것.** 05_tuning에서도 풍속 모델은 건드릴 필요 없다.

### ✅ 성공 — MLP 풍속 모델 (다양성 자원)
- 13절 MLP와 달리 **제대로 학습**: 미니배치 512, 코사인 감쇠, BatchNorm+Dropout 0.15, 512-256-128. 단 12~22 에폭에서 early stopping(과적합이 빨리 온다)
- **풍속 잔차 상관 gbdt vs mlp = 0.851** (낮음 = 다양성 있음)
- 단독은 GBDT보다 약함(`M_mlpws` B평균 0.6211) but 블렌드에서 이득
- **블렌드 비중 0.7(GBDT):0.3(MLP)** — 곡선이 0.5~0.8에서 완만하고 0.7이 B평균·B최솟값 둘 다 최고
- **17절 교훈 준수**: 풍속을 평균 내지 않고 **각 풍속으로 발전량까지 예측한 뒤 그 예측을 평균**

---

## 📌 17절 결과 + 첫 제출 (2026-08-01)

### 최종 확정 구성
**풍속 모델**: LightGBM `objective="l2"`, `n_estimators=3000`, **무작위성 없음(결정적)**, 입력 853개(예보만)
**발전량 모델**: LightGBM `objective="quantile"`, **τ=0.60**, 표본가중 `actual/capacity`(하한 0.1), 피처 상위 200개(2단계 축소), **시드 5개 앙상블**(`RAND_PARAMS`)
**검증 점수**: A안 0.6391 / B안 평균 0.6298

### 17-1·17-2 — ⭐ 풍속 앙상블은 오히려 손해 (중요한 교훈)
| 풍속 | 1단계 상관 | 1단계 RMSE | **2단계 B평균** |
|---|---|---|---|
| ridge | 0.8798 | 1.720 | 0.6134 |
| **l2 (채택)** | 0.9109 | 1.496 | **0.6298** |
| l1 | 0.9124 | 1.487 | 0.6253 |
| avg12 | 0.9142 | 1.470 | 0.6286 |
| seedens | 0.9135 | 1.477 | 0.6278 |
| **avgall** | **0.9146(1위)** | **1.467(1위)** | 0.6276(거의 꼴찌) |

**1단계 1등이 2단계 꼴찌.** 이유: **발전량은 풍속에 볼록(~v³)하게 반응**하므로, 여러 풍속 추정을 평균 내면 분산이 줄어 **극값이 눌리고 강풍 구간 발전량을 체계적으로 과소평가**한다(Jensen 부등식: `평균(v³) ≥ (평균v)³`). RMSE는 좋아져도 FICR은 발전량 큰 시간대에 가중을 두므로 손해.
**→ 규칙: 볼록한 변환을 통과시킬 값(풍속)은 평균 내지 말 것. 앙상블은 파이프라인 마지막 단(발전량 예측)에서만 한다.**
이는 16절의 "l1이 풍속은 정확한데 l2가 발전량 점수는 높다"와 **같은 뿌리의 현상**이다.

### 17-3 — τ 재조정: 변화 없음
편향이 +0.053 → +0.028로 바뀌었으니 최적 τ도 옮겨갔을 거라 예상했으나 **τ=0.60 그대로**(0.50: 0.6203 / 0.55: 0.6260 / 0.60: 0.6298 / 0.65: 0.6278). 그룹별로도 group_3만 0.65가 +0.0014 높은데 노이즈 수준.

### 17-4 — 제출 경로 리허설 + 첫 제출 파일
- **`submissions/20260801_v1_wsgbdt_q60_seed5.csv` 생성 완료.** `validate_submission()` 통과, 8760행
- **발견한 구조적 문제**: `test_features_v1.parquet`에는 train에만 있는 컬럼 28개가 없다(라벨 3, `_cv_*` 18, `scada_*` 6, `year`). 따라서 **`build_group_feature_frame_v3(test, ...)`는 KeyError로 죽는다.** 파워커브도 `df[g]`(라벨)로 적합하는 구조라 test에 못 쓴다
  - **해결**: `build_frame_given_pc(df, g, ws_series, edges, vals, tag)` — 파워커브를 **'적합'하지 않고 '적용'만** 한다. 라벨 없는 test에도 동작. train/test 컬럼 일치 assert 포함
- **CWD 함정**: `src/submission.py`의 `SAMPLE_SUBMISSION_PATH`/`SUBMISSIONS_DIR`이 상대경로라 노트북(`notebooks/`)에서 돌리면 `notebooks/data/...`로 잘못 해석돼 `FileNotFoundError`. 17-4 첫 셀에서 `REPO_ROOT`를 잡아 모듈 전역을 덮어씀. **`SUBMISSIONS_DIR`도 반드시 같이 고쳐야 한다** — 안 고치면 에러 없이 `notebooks/submissions/`에 조용히 저장된다
- **예측 상식 점검 — 이용률이 train보다 높은 것은 정상**:

| 그룹 | 예측 평균 이용률 | train 실제 | 예측 ≥10% 비율 | 실제 |
|---|---|---|---|---|
| kpx_group_1 | 0.406 | 0.311 | 0.786 | 0.617 |
| kpx_group_2 | 0.431 | 0.333 | 0.774 | 0.615 |
| kpx_group_3 | 0.363 | 0.270 | 0.697 | 0.547 |

  **버그 아님. 2025년 예보 자체가 3년치 어느 해보다 바람이 강하다**: `ldaps_ws10_avg16` 2022 4.976 / 2023 4.743 / 2024 4.789 → **2025 5.107**, GFS 100m +0.417.
  숫자 검산: 예보 풍속 +5.6% → v³이므로 발전량 약 +18% (0.311→0.367) + τ=0.60의 상향 편향(용량의 +0.028) = **0.395** ≈ 실제 예측 0.406. 월별 계절성 모양도 train 실제와 일치(1·11·12월 높고 9·10월 최저). 2월 예측이 0.606으로 튄 것은 2025년 2월 예보 풍속이 6.646으로 train 평균 5.165보다 1.48 높기 때문
- **⚠️ 수정함(2026-08-01)**: `fit_final_wind()`에 `RAND_PARAMS`가 들어가 있었는데 이는 검증된 구성과 다르다(무작위 풍속 `W_seedens` 0.6278 < 결정적 `W_gbdt_l2` 0.6298). **`rand=False`를 기본값으로 바꿈.** 발전량 모델의 시드 앙상블은 그대로 유지. **17-4를 다시 돌려 제출 파일을 재생성할 것**

### ⭐⭐ 첫 리더보드 점수 (2026-08-01) — 검증 설계가 검증됐다

**리더보드: Score 0.6271 / 1-NMAE 0.8592 / FICR 0.3950**

| 지표 | A안(2024) | **B안 평균** | **리더보드** | B안과의 차이 |
|---|---|---|---|---|
| Score | 0.6391 | **0.6298** | **0.6271** | **-0.0027** |
| 1-NMAE | 0.8736 | 0.8676 | 0.8592 | -0.0084 |
| FICR | 0.4045 | 0.3921 | 0.3950 | **+0.0029** |

**⇒ B안 평균이 리더보드를 0.003 이내로 맞혔다. A안은 0.012 낙관적이었다.**

**결정: 앞으로 모델 채택의 주 지표는 `B안 3-fold 평균`으로 한다.** A안은 보조 지표(방향 일치 확인용)로만 쓴다. CLAUDE.md 5장의 "A안·B안 병행" 원칙은 유지하되, 둘이 엇갈릴 때는 **B안을 우선**한다. 근거: 실측 1건이지만 차이가 뚜렷하고(0.003 vs 0.012), B안이 "여러 시점에서 반복 검증"이라는 구조적 이유도 A안보다 우수하다.

**⇒ 로컬 점수를 믿고 빠르게 반복해도 된다.** 남은 기간 실험 사이클을 로컬로 돌리고, 확실한 개선만 제출하면 된다.

**흥미로운 세부**: 1-NMAE는 예상보다 낮고(-0.0084) **FICR은 예상보다 높았다(+0.0029)**. 2025년이 예보상 3년치 어느 해보다 바람이 강한데, 강풍 구간(rated 이상)은 파워커브가 평평해 FICR 통과율이 훨씬 높기 때문으로 보인다(검증에서 group_2 기준 ramp 0.33 vs rated 0.51 vs high 0.71). **2025년의 강풍 편향이 우리에게 유리하게 작용하고 있다.**

### 남은 기간 (마감 2026-08-14(금) 10:00, 일일 5회 제한)
FICR이 0.395로 1-NMAE(0.859)보다 1에서 훨씬 멀다. 오라클에서 FICR 0.76이 가능했으므로 **상방은 여전히 FICR에 있고, 그 열쇠는 풍속 정확도다.**

---

## 🏆 16절 풍속 모델 교체 성공 (2026-08-01) — 단일 최대 기여

**14절 오라클 진단이 가리킨 방향이 옳았다.** `ws_est`(예보 6개 컬럼 Ridge)를 **850개 피처 LightGBM**으로 바꾼 것만으로:

| 변형 | 풍속 출처 | A안 | B평균 | 잔차 σ | 편향 |
|---|---|---|---|---|---|
| `W_ridge` | Ridge(예보 6개) | 0.6267 | 0.6134 | 0.1727 | +0.0526 |
| **`W_gbdt_l2`** | **LightGBM(예보 850개)** | **0.6391** | **0.6298** | **0.1684** | **+0.0283** |
| `W_gbdt_l1` | LightGBM(l1) | 0.6352 | 0.6253 | 0.1700 | +0.0329 |
| `W_blend` | Ridge+GBDT 평균 | 0.6335 | 0.6236 | 0.1684 | +0.0421 |

**B평균 +0.0164, A안 +0.0124, 4개 fold 전부 개선.** 15절 전체(+0.0037)의 4배 넘음.

### 1단계(풍속 정확도) 결과 — 12개 fold×그룹 조합 전부 개선
| 그룹 | ws_est(Ridge) 상관 | ws_gbdt_l1 | ws_gbdt_l2 | RMSE 개선 |
|---|---|---|---|---|
| kpx_group_1 | 0.8734 | **0.9109** | 0.9096 | 1.658 → 1.410 |
| kpx_group_2 | 0.8788 | **0.9172** | 0.9159 | 1.828 → 1.530 |
| kpx_group_3 | 0.8871 | **0.9092** | 0.9073 | 1.675 → 1.521 |

상관 평균 0.880 → 0.912, RMSE -15%, MAE -18%.

### ⚠️ 반드시 기억할 3가지 교훈
1. **1단계 승자와 2단계 승자가 달랐다.** 풍속 정확도는 `l1`이 이겼는데(상관·RMSE·MAE 전부, 세 그룹 모두) **발전량 점수는 `l2`가 이겼다**(0.6298 vs 0.6253). 이유: `l2`는 큰 풍속 오차에 더 큰 벌점을 주는데, 파워커브가 가파른 ramp 구간(3~12m/s)에서는 큰 풍속 오차가 큰 발전량 오차로 증폭되기 때문. **최종 판단 기준은 항상 2단계(발전량 점수)다.**
2. **Ridge와 섞으면 손해.** `W_blend`(0.6236) < 순수 GBDT(0.6298). Ridge가 모든 면에서 열등하고 오차 방향도 비슷해서 끌려 내려간다. 13절 MLP 블렌드가 통했던 것과 대조 — 그때는 오차 방향이 달랐다
3. **σ는 -2.5%뿐인데 점수는 +0.016.** 진짜 개선은 **편향**에서 왔다(+0.0526 → +0.0283). 풍속이 정확해지니 예측이 저절로 FICR 밴드 중앙에 가까워졌다

### 15-6절 — 시드 앙상블 (15-4의 설계 실수 수정)
- **15-4가 무효였던 이유**: LightGBM 기본값은 `subsample=1.0`, `colsample_bytree=1.0`, `subsample_freq=0`이라 **무작위 요소가 아예 없다.** 완전 결정적이라 seed를 바꿔도 같은 트리가 나온다(seed 간 표준편차 0.0000)
- **수정**: `RAND_PARAMS = dict(subsample=0.8, subsample_freq=1, colsample_bytree=0.8)`. **`subsample_freq=1`을 빠뜨리면 `subsample`이 통째로 무시된다**
- 결과: 무작위성 도입 자체는 손해(0.6153→0.6131), 앙상블이 메우고 더 벌어서 **`R_randavg5` 0.6259/0.6170** (T_q60 대비 +0.0017, 4 fold 중 3개 개선). seed 간 표준편차 0.0015

---

## 🔥 14절 오라클 실험 결과 (2026-08-01) — 프로젝트 방향을 바꾼 결정적 결과

| 변형 | 풍속 | 방법 | A안 | B평균 | 잔차 σ | 밴드폭÷σ |
|---|---|---|---|---|---|---|
| `pc_forecast` | 예보 | 파워커브만 (ML 없음) | 0.5825 | 0.5737 | 0.1950 | 0.308 |
| `dyn_w_q60` (13절 최고) | 예보 | LightGBM | 0.6252 | 0.6124 | 0.1728 | 0.347 |
| **`pc_oracle`** | **실측** | **파워커브만 (ML 없음)** | **0.8268** | **0.8254** | **0.0801** | **0.749** |
| **`oracle_ws`** | **실측** | LightGBM | **0.8587** | **0.8618** | **0.0786** | **0.763** |

**결론 3가지 (전부 사전 등록한 해석 규칙의 최상단 구간 = "0.70 이상")**
1. **예보 오차가 전체 σ의 89%다.** √(0.1728² − 0.0786²) = 0.154
2. **머신러닝을 전부 걷어내고 파워커브 하나만 써도 바람만 정확하면 0.83.** 우리는 850피처+LightGBM으로 0.61 — **지금 하는 일의 대부분이 "틀린 바람으로 발전량 맞히기"** 이고 그 천장이 0.61 근처다
3. **바람만 알면 모델링은 이미 바닥에 닿는다.** `oracle_ws` 그룹별 σ(0.068/0.071/0.094) ≈ 예측 불가 바닥(0.075/0.061/0.082). 즉 **튜닝·앙상블로 짜낼 여지는 거의 없다**

**⇒ 앞으로의 유일한 큰 레버는 "풍속 추정 정확도"다.** 그런데 현재 `ws_est`는 `03_features.ipynb` 3절에서 **예보 풍속 6개 컬럼에 Ridge 선형회귀**를 돌린 것뿐이다. 850개 피처가 있는데 풍속 추정엔 6개만 쓰고 있다. → **16절 과제**(아래 "다음 할 일" 참조)

### 예측 불가 성분(irreducible noise)의 바닥 — 14-1절 측정
실측 풍속 0.5m/s 구간 **내부**의 발전량 표준편차(용량 대비, 채점 대상 행만):

| 그룹 | 예측불가 σ | 밴드폭÷σ | 실제 FICR(`dyn_w_q60`) |
|---|---|---|---|
| kpx_group_1 | 0.0748 | 0.802 | 0.3795 |
| kpx_group_2 | **0.0612** (가장 쉬움) | 0.981 | **0.4432** (최고) |
| kpx_group_3 | **0.0817** (가장 어려움) | 0.734 | **0.3044** (최저) |

**바닥의 순서와 실제 점수의 순서가 정확히 일치한다** → **group_3의 낮은 점수는 모델 탓이 아니라 그 그룹이 원래 어렵기 때문**(미해결 질문 12번 사실상 해소).

---

## 🆕 2026-08-01 추가 발견 (14절 준비 중 데이터 확인)

### 1. 예보 리드타임 — **처음 생각보다 가치가 낮다 (정정)**
- `data_available_kst_dtm_*`와 `kst_dtm` 차이 = **12.0 ~ 35.0시간, 고유값 24개**. GFS와 LDAPS 리드타임은 **완전히 동일**
- **⚠️ 정정**: 예보 발표 시각이 **항상 13시 한 번뿐**이라 **리드타임이 시각(hour)으로 100% 결정된다**(시각별 리드타임 고유값이 전부 1개). 이미 `hour_sin/cos`가 피처에 있으므로 **새 정보가 아니라 재표현일 뿐**이다
- 리드타임별 실제 예보 오차도 단조롭지 않다(12~17h 1.38 → 23~29h **1.23** → 29~35h 1.38 m/s) — 리드타임 효과와 바람의 일주기가 뒤섞여 있어서
- **그래도 넣는 이유**: 트리에게 원형좌표 2개(`hour_sin/cos`) 대신 **직선 축** 하나를 주면 "리드타임 30시간 이상" 같은 분할이 1회로 끝난다. 정보량은 같고 표현만 다르다. 비용 0이라 넣고 확인만 함(15-1절)
- **기대효과: 낮음** (처음에 "중간~상"이라고 적었던 것을 하향 정정)

### 2. **예측 불가 성분(irreducible noise)의 바닥을 측정했다**
실측 풍속(SCADA) 0.5m/s 구간 **안에서의** 발전량 표준편차(용량 대비, 채점 대상 행만):

| 그룹 | 예측불가 σ | 참고 |
|---|---|---|
| kpx_group_1 | 0.1045 | |
| kpx_group_2 | **0.0799** | 가장 쉬움 — 실제로 FICR도 가장 높음(0.4432) |
| kpx_group_3 | **0.1376** | 가장 어려움 — 실제로 FICR 최저(0.3044) |

- **현재 잔차 σ = 0.1728.** 바닥이 0.08~0.14라는 것은 **바람을 완벽히 알아도 σ가 0.10 근처까지밖에 안 내려간다**는 뜻
- 어림 분해: √(0.1728² − 0.10²) ≈ 0.14가 예보 오차 몫 → **예보 오차가 여전히 지배적**이지만 상한이 존재
- **group_3의 낮은 점수는 모델 탓이 아니라 그 그룹이 원래 어렵기 때문**일 가능성이 크다(미해결 질문 12번과 연결)
- 14-1절에서 정식으로 계산·기록

---

## 성능 개선 아이디어 목록 (2026-08-01 정리, 우선순위 포함)

### 갈래 A — σ(예측 오차의 폭)를 줄인다 : 상방 가장 큼

**A0. 오라클 실험(SCADA 실측 풍속으로 상한 측정) — 갈래 A에 투자할지를 결정하는 관문** ⭐⭐
- 아래 A1~A5는 전부 "풍속 추정을 개선하면 σ가 준다"는 가정 위에 있다. **그 가정 자체를 12번 학습으로 검증할 수 있다.** 상세는 "다음 할 일 0번" 참조

**A1. 예보 lag/lead 피처 — 구현·검증 완료 (2026-08-01)** ✅
- **결과: 약한 개선.** A안 +0.0076, B평균 +0.0027 (4 fold 중 3개 개선, fold1만 -0.0016). **σ를 줄인 유일한 변형**(0.1708→0.1679, -1.7%)
- **해석: 위상 오차 가설은 약하게만 지지됐다.** 시간 방향에서 짜낼 게 생각보다 적으므로 **A3(시퀀스 모델) 기대치는 낮춰 잡을 것.** 반대로 공간 방향(A2)은 아직 미검증이라 상대적 우선순위가 올라감
- 아래는 당시 판단 근거 기록:
- `train_features_v1.parquet` 888개 컬럼에 lag/lead/rolling이 **하나도 없음**(확인 완료). 각 시각의 예보만 보고 그 시각을 예측 중.
- 수치예보의 가장 흔한 오차는 **위상 오차(phase error)**: 전선이 3시간 늦게 통과하면 예보는 풍속 급증을 맞혔는데 시각이 어긋나 그 시간대가 전부 크게 틀린다. t시각 예보만 보면 이걸 알 방법이 없다.
- **누수 아님**: 예보는 발표 시점에 미래 시각까지 통째로 제공된다(`data_available_kst_dtm` 기준). t+3 예보는 t시점에 이미 손에 있다. 실측(SCADA/라벨)의 lag는 금지지만 **예보의 lead는 합법.**
- 13절에 구현됨 (`add_forecast_dynamics`).

**A2. 격자를 이미지로 보는 CNN** ⭐
- 현재 GFS/LDAPS 격자 수십 개를 **평평한 800개 피처**로 넣는 중. importance상 상위 15개가 gain의 44~65%를 먹고 나머지 수백 개는 죽어 있음 = 트리가 **공간 구조**를 전혀 못 쓰고 있다는 뜻.
- 격자를 `(변수·높이, 위도, 경도)` 이미지로 만들어 CNN에 넣으면 바람이 어느 방향에서 다가오는지를 직접 학습. 풍력 예측 논문의 표준 접근.

**A3. 시퀀스 모델 (1D-CNN / LSTM / Temporal Fusion Transformer)**
- A1을 손으로 만드는 대신 시간축을 통째로 모델에 넘김. TFT는 "미래 시점까지 알려진 공변량(=예보)" 처리 구조가 내장돼 이 문제에 특히 적합. 구현 비용이 크므로 **A1을 먼저 해보고 판단**.

**A4. 파워커브 잔차 학습**
- `y - power_curve_est`를 타깃으로 학습해 모델이 물리로 설명 안 되는 부분에만 집중하게 함. (주의: fold-safe 버전 사용 필수)

**A5. 예보 불확실성 피처**
- GFS-LDAPS 불일치(`gfs_ldaps_diff` 이미 있음), 인접 격자 분산, 시간 방향 변동성 → "이 시각은 예보가 못 미더움"을 모델이 인지. B1(분포 예측)과 결합하면 특히 강력.

### 갈래 B — 산식을 직접 최적화 : 확실하고 즉시 가능

**B1. 분포 예측 + 기대 FICR 최대화** ⭐ 이론적 정석
- 점수를 예측값 `f`의 함수로 정리하면:
  `점수기여 = -(0.5/3nC)·E|f-a|  +  (0.5/(12·Σa))·E[a·단가(|f-a|/C)]`
- 앞항은 **중앙값**을, 뒷항은 **폭 6%짜리 창을 확률질량이 가장 두꺼운 곳에 놓기**(≈최빈값)를 요구.
- τ=0.6이 통한 이유가 이것. **분포를 알면 최적점을 손으로 더듬지 않고 계산**할 수 있고, 시각마다 다른 최적점도 가능.
- 구현: LightGBM 분위수 9~19개로 사다리를 만들거나, 신경망 출력을 발전량 구간 100개 softmax로 두고 f를 격자 탐색.

**B2. 그룹별 τ** — 근거 확보됨(위 "확정된 결론 3번")

**B3. FICR 계단의 미분가능 근사 손실** — 시그모이드 2개로 근사. 단 **B1이 더 안전**(계단 근처 그래디언트 불안정 위험). 학습은 안정적 손실로, 의사결정만 산식에 맞추는 B1을 권장.

**B4. 검증된 레버 결합** — ✅ **완료(2026-08-01).** `dyn_w_q60` + MLP 0.3 블렌드로 `fix_base` 대비 **+0.037(A안)/+0.028(B평균)**. 예상대로 가중과 τ는 크게 겹쳤다(단순 합의 60%). **위치 최적화는 여기서 사실상 끝났다 — 편향이 -0.050에서 +0.054로 반대편까지 넘어갔고 τ=0.70은 오히려 하락했다**

### 갈래 C — 딥러닝 / 사전학습 모델

**유망**
- **C1. MLP를 제대로 학습**(미니배치·학습률 스케줄·BatchNorm·넓은 층). 지금 미학습 상태로도 GBDT급 + 잔차 상관 최저 = 가성비 최고
- **C2. 표 형식 딥러닝**(FT-Transformer, TabNet): 단독으로 GBDT를 이기긴 어려우나 앙상블 다양성 기여
- **C3. A2·A3의 CNN/시퀀스 모델**: 딥러닝이 GBDT 대비 **구조적으로 우월한 유일한 지점**. 딥러닝을 쓸 거면 여기에 써야 함

**실익 낮음 (검토했으나 권장 안 함)**
- **기상 파운데이션 모델(GraphCast/Pangu-Weather/Aurora)**: 이들은 *기상 예보를 생성*하는 모델인데 우리에겐 이미 GFS·LDAPS 예보가 주어져 있음. 돌리려면 전 지구 초기장(ERA5 해석장 등)이 필요한데 없음. 대회 규정(외부 API 추론 금지, Private Score 재현)상 운영 부담도 큼
- **시계열 파운데이션 모델(Chronos/TimesFM/Moirai)**: "과거 시계열 → 미래" 구조에 최적화됐으나 우리 문제는 **기상 예보가 정보의 대부분인 공변량 주도** 문제. 2025년 전체를 예측하므로 자기회귀 구조가 무용(1월 예측 시 쓸 과거 발전량이 없음). **구조적으로 안 맞음**

### 갈래 D — 아직 한 번도 안 건드린 것들 (2026-08-01 전수 점검으로 추가)

**D1. 예보 리드타임 피처** ⭐ 위 "추가 발견 1번" 참조. 비용 거의 0

**D2. 시드 앙상블** ⭐ — 같은 모델을 seed만 바꿔 3~5개 학습해 평균. **σ를 줄이는 가장 싸고 확실한 방법**(모델 분산이 √n으로 감소). 지금까지 seed=42 단일. `ensemble-final` 스킬 범위

**D3. 피처 축소** — 상위 15개가 gain의 44~65%, 수백 개가 gain 0. gain 0 피처 제거 + 상위 N개만. 학습 속도·잡음 양쪽 이득 (`feature-selection` 스킬)

**D4. 그룹별 3모델 vs 통합 1모델** — 지금은 그룹별 완전 분리. 통합하면 group_3(2022년 라벨 없음, 표본 부족)이 다른 그룹의 패턴을 빌려 쓸 수 있다. 그룹 ID를 범주형 피처로 넣고 비교

**D5. 타깃 스케일** — GBDT는 kWh, MLP는 이용률(0~1)로 학습 중. 이 차이가 MLP 선전의 이유일 수 있음. GBDT도 이용률로 바꿔 비교

**D6. curtailment 추가 탐지** — 현재 3구간만 라벨에서 제외. "풍속은 충분한데 발전량이 비정상적으로 낮은" 구간이 더 있는지 잔차 기반으로 탐색. 남아 있으면 학습 잡음이자 예측 불가 성분의 일부

**D7. 오차가 큰 시간대의 정체 파악** — 언제 크게 틀리는가(계절/시간대/램프/특정 날짜). 잔차 상위 1% 구간을 직접 들여다보면 미탐지 curtailment·센서 고장·특수 이벤트가 나올 수 있음

**D8. 잔차의 시간 자기상관** — 연속으로 같은 방향으로 틀리면 구조적 원인이 있다는 뜻. 자기상관이 크면 (제출 시점에 쓸 수 있는 범위 안에서) 보정 여지 검토

**D9. 그룹 간 상호정보 / 멀티태스크** — 3개 그룹은 **같은 단지**다. 한 그룹의 예보 오차는 다른 그룹과 강하게 상관될 것. 다만 예측 시점에 다른 그룹의 실제 발전량은 모르므로, 쓸 수 있는 건 "공유 표현 학습"(멀티태스크)뿐

**D10. 풍향×풍속 상호작용 / 후류** — 미해결 질문 3·5번. 주풍향 정렬도, 풍향별 그룹 간 후류 관계

**D11. 대기 안정도 지표** — PBL height(`blh`)는 이미 피처에 있음. 온도 프로파일 기반 안정도(리처드슨 수 근사) 추가 검토. 안정도는 윈드시어와 난류를 좌우해 허브고도 풍속 추정에 직접 영향

**D12. 사후 보정(calibration)** — out-of-fold 예측에 대해 isotonic/affine 보정을 적합. **반드시 학습 구간 내부에서 적합**해야 함(검증 구간으로 적합하면 검증 과적합)

**D13. 2단계 모델** — "채점 대상이 될 것인가(발전량 ≥ 10%)"를 먼저 분류하고 회귀를 따로. 산식 ①번 구조를 그대로 모델링

### 갈래 E — 대회 운영·재현성 (미룰수록 위험)

**E1. test 경로 검증** ⭐ — 13절 lag/lead와 fold-safe 파생은 **train에서만 확인**했다. `build_group_feature_frame_v2(test, ...)`가 실제로 동작하는지, test 시간축이 1시간 연속인지, `cv_suffix=None`(최종 재학습) 경로가 정상인지 **아직 한 번도 안 돌려봤다.** 제출 직전에 터지면 치명적

**E2. train.ipynb / inference.ipynb 분리** — 2차 평가 필수 요건. `ensemble-final` 스킬

**E3. `src/submission.py` 검증 통과 확인** — 8760행, forecast_id 불변, 결측·음수 없음, 클리핑

**E4. seed 고정·재현성 점검** — Private Score 재현 요건

**E5. `requirements.txt` 작성** — 아직 없음

**E6. 첫 제출로 로컬↔리더보드 상관 확인** — 로컬 0.62가 리더보드에서 몇 점인지 모른다. **일찍 한 번 내보는 것 자체가 정보다**(일일 5회 제한)

---

## 다음 할 일 (우선순위순) — 2026-08-01 오라클 결과 반영해 재정렬

**현재 상태**: `04_model_selection.ipynb` **125셀, 1~15절**. 14절(오라클)까지 실행 완료. **15절은 코드만 작성, 미실행.**

### 지금 노트북에 있는 것
- **15절(미실행)**: 확정 개선 4종을 `15-1 리드타임 → 15-2 피처축소 → 15-3 그룹별 τ → 15-4 시드앙상블` 순으로 배치. 순서 근거는 15-0절 표 참조(피처축소를 앞에 둬야 이후가 빨라지고, τ는 피처 확정 후에 정해야 하며, 시드앙상블은 먼저 하면 이후 실험 비용이 5배가 된다)
  - 총 약 180번 학습이나 축소 후에는 훨씬 빨라짐. **15-1 → 15-2 → 15-3 → 15-4 순서대로 나눠 실행할 것**
  - 15-2의 `BEST_TOPN`과 15-3의 `BEST_TAU`는 **자동 선택되지만 수동으로 덮어쓸 수 있게** 해뒀다. 점수가 비슷하면 **더 단순한 쪽(피처 적은 쪽, 단일 τ)** 을 고를 것

### ⭐⭐ 16절 (아직 코드 없음) — 진짜 승부처
**SCADA 실측 풍속을 타깃으로 제대로 된 풍속 추정 모델을 학습한다.**
- **왜**: 14절에서 예보 오차가 σ의 89%임이 확인됐고, 바람만 알면 파워커브만으로 0.83이 나온다. 그런데 현재 `ws_est`는 **예보 6개 컬럼 Ridge**뿐이다
- **정확히 무엇을**: 입력 = 850개 피처 전부(모든 격자·높이 + 13절 lag/lead 파생), 타깃 = `scada_ws_{group}`, 모델 = LightGBM → MLP → (여력되면) 격자 CNN
- **누수 아님**: SCADA는 train에 존재하고 학습된 모델은 test의 **예보**에 적용된다. 지금 Ridge가 하는 일과 구조가 완전히 같고 모델만 바뀐다
- **fold-safe 필수**: `03_features.ipynb` 3-5절 `CV_CUTOFFS` 패턴을 그대로 따라 fold별로 따로 학습. 이걸 안 지키면 04절 전체를 다시 해야 한다
- **평가는 2단계**: ① 추정 풍속이 SCADA에 얼마나 가까워졌나(상관·RMSE) ② 그 풍속으로 발전량 모델을 돌렸을 때 점수가 올랐나
- **현재 기준선**: `ws_est`의 SCADA 상관 = 0.854 / 0.856 / 0.870. **0.90 이상으로 올리는 것이 목표**
- **주의**: 새 풍속으로 `power_curve_est`도 다시 만들어야 한다(파워커브는 풍속을 통과시켜 만드는 피처)

### 그 이후
1. **E1. test 경로 검증** ⭐ — 13·15절의 파생(lag/lead, 리드타임, 피처축소)은 **train에서만 확인**했다. `build_group_feature_frame_v3(test, ...)`가 도는지, test 시간축이 1시간 연속인지, `cv_suffix=None`(최종 재학습) 경로가 정상인지 **한 번도 안 돌려봤다.** 제출 직전에 터지면 치명적
2. **MLP 제대로 학습** + 같은 조건(축소·τ·가중)으로 맞춘 뒤 블렌드 이득이 남는지 재확인
3. `05_tuning.ipynb` — Optuna (`model-tuning` 스킬). **단 14절이 "모델링은 이미 바닥에 닿았다"고 말했으므로 기대치는 낮게**
4. 첫 제출로 로컬↔리더보드 상관 확인 (일일 5회 제한, 마감 2026-08-14)
5. train.ipynb / inference.ipynb 분리, `requirements.txt`

### (완료) 오라클 실험
0. ~~**⭐ 오라클 실험 — 05_tuning을 시작하기 전에 이것부터.**~~ ✅ **완료 (14절, 위 결과 참조)** `scada_ws_{group}`(SCADA 실측 풍속)을 풍속 피처 자리에 넣고 같은 LightGBM을 돌려 **"바람을 완벽히 알았다면 몇 점인가"** 를 측정한다. **12번 학습이면 끝나는데 앞으로의 방향 전체를 결정한다.**
   - **정확히 무엇을 하는가**: `04_model_selection.ipynb`에 14절을 새로 만든다. `build_group_feature_frame_v2`를 그대로 쓰되, 그룹의 `{g}_ws_est_cv_{suffix}` 자리에 `scada_ws_{g}`를 대입한 변형(`oracle_ws`)을 하나 추가한다. 기존 13절 셀은 **건드리지 않는다**(추가이지 교체가 아니다). `dyn_w_q60`과 동일한 설정(가중 actual, τ=0.60, lag/lead 켬)으로 돌려 `dyn_w_q60`(0.6252/0.6124)과 나란히 비교한다
   - **주의**: SCADA는 test에 없으므로 이건 **진단 전용**이다. 절대 제출용 모델에 쓰면 안 된다. `scada_ws_*`는 `NON_FEATURE_COLS`에 있으므로 이 실험에서만 예외적으로 꺼내 쓴다는 주석을 반드시 남길 것
   - **결과 해석**:
     - 오라클 ≫ 0.62 → 남은 오차의 주범은 **예보 오차**. 공간 CNN(A2)·시퀀스 모델(A3)·풍속 추정 개선에 투자할 가치 있음
     - 오라클 ≈ 0.62 → 바람을 완벽히 알아도 발전량은 못 맞힌다는 뜻(터빈 가동상태·정비·계통제약 등 관측 불가 요인). σ 줄이기를 포기하고 **분포 예측 + 기대 FICR 최대화(B1)** 로 방향 전환
1. **그룹별 τ** — group_2 편향 +0.076이 과하다. group_2만 τ를 낮춰본다. 단 **검증 점수를 보고 3개 파라미터를 고르면 낙관 편향**이 생기므로, A안·B안에서 일관된 방향일 때만 채택할 것
2. **MLP 제대로 학습** — 현재 full-batch 36~104 에폭의 사실상 미학습 상태. 미니배치 + 학습률 스케줄 + BatchNorm 추가. **그리고 MLP에도 같은 표본가중·τ를 적용한 뒤 블렌드 이득(+0.005)이 남는지 재확인** (편향 상쇄로 얻은 이득일 수 있음)
3. **피처 축소** — 상위 15개가 gain의 44~65%, 수백 개가 gain 0. `regime_*`/`high_wind_caution`/`ws_est_cube`는 gain 0.000%로 무용 확인됨
4. `05_tuning.ipynb` — Optuna 하이퍼파라미터 튜닝 (`model-tuning` 스킬). 위 0~3번 결과를 반영한 구성으로 시작
5. 그 이후: 오라클 결과에 따라 A2(공간 CNN) 또는 B1(분포 기반 결정)

---

## 결정 사항 / 근거 (누적 — 03_features 이전 세션 분)

- **주력 풍속 피처가 "허브고도 외삽"에서 "SCADA 회귀 추정풍속"으로 바뀜.** 경위:
  1. **1차 시도(기각)**: 윈드시어 멱법칙으로 GFS 10m/100m에서 시어지수 α를 구해 LDAPS 10m을 117m로 외삽 → 발전량 상관이 오히려 나빠짐(group_1 0.73→0.60).
  2. **분석 중 발견**: "최근접 LDAPS 격자 1개" vs "16격자 평균" 중 뭐가 나은지 그룹마다 정반대(group_1은 평균 유리, group_2/3은 최근접 유리). Steiger's Z 검정(p<0.001)으로 우연 아님 확인.
  3. **2차 시도(채택, 민석님 제안)**: SCADA를 정답 삼아 6개 예보 풍속을 회귀로 결합. 2022~2023 학습 → 2024 out-of-sample 검증:

     | | 기존 최고 단일 예보 피처 | 회귀 추정풍속 |
     |---|---|---|
     | group_1 | 0.793 | **0.854** |
     | group_2 | 0.814 | **0.855** |
     | group_3 | 0.827 | **0.869** |
  4. Steiger's Z로 유의성까지 코드로 검증(p<0.001, |Z|>20).
  5. **fold-purity 문제 발견 및 해결**: 3-5절에서 학습구간 경계별 fold-safe 3종(`ws_est_cv_2023_07/2024_01/2024_07`) 추가.
  6. **다중공선성 → Ridge로 해결**: GFS 80m/100m 계수가 -2.5~1.8로 요동 → 3-2c에서 Ridge(`RIDGE_ALPHA=30`) 전환. 성능 유지·개선하면서 계수 절댓값 최댓값 뚜렷이 감소.
- **공기밀도 보정 효과, 단변량 상관으로는 미미했음**: group_1 +0.0013, group_2 +0.0011, group_3 -0.0006. ⚠️ **그러나 04절 feature importance에서는 `ws_est_corrected`가 group_1 gain 1위(33.98%)** — 단변량 상관과 모델 기여도는 다르다는 것을 실증. 컬럼 유지가 옳았다.
- **파워커브 변환 피처 추가**: SCADA(풍속,발전량) 곡선을 0.5m/s 구간+단조증가 강제로 만들고 `ws_est`를 통과시켜 `{group}_power_curve_est` 생성(12절). out-of-sample 2024 상관 0.854→0.859, 0.856→0.871, 0.870→0.878. fold-safe 버전(`power_curve_est_cv_*`)도 처음부터 같이 만듦. **⚠️ 그런데 04절에서 원본 버전이 피처로 새어 들어가는 사고가 발생함(위 최우선 이슈).**
- **12절 잔여 리스크 3가지(12-4절 기록)**:
  1. (DS) 고풍속(15m/s+) 표본이 적은데 `np.maximum.accumulate`가 우연히 튄 bin 평균을 이후 전 구간에 영구 전파 → 정격구간 과대추정 위험
  2. (DS) `ws_est`는 추정치(상관 0.83~0.87)인데 ramp 구간에서 풍속오차가 발전량오차로 증폭
  3. (도메인) 누적최댓값 곡선은 절대 안 떨어져서 컷아웃(25m/s 안전정지) 급락을 반영 못함. train/test 3년엔 25m/s 이상이 없었으나 2025년 태풍기엔 가능
  - **→ 04절 10-3a에서 확인 완료**: 리스크는 실재하나 **방향이 그룹마다 정반대**(위 "확정된 결론 8번"). 일괄 보정은 부적절.
- **파워커브 구간 더미 + 고풍속 주의(13절)**: 원래 계획한 컷아웃(25m/s) 기준은 `ws_est` 실제 분포상 train 3년 내내 한 번도 안 넘어서 상수 컬럼이 될 뻔함 → 실제 신호가 있는 **15m/s "고풍속 주의"** 로 기준·이름 변경. **⚠️ 04절 결과 이 피처들은 전부 gain 0.000으로 무용 판정.**
- **curtailment 확정 구간(2023-02-13~17, 2024-01-18~24, 2024-02-22~03-01)을 학습 라벨에서 제외**. 평가 산식이 발전량 10% 미만을 채점 안 하므로 leakage가 아니라 노이즈 제거. 입력은 그대로, 라벨만 NaN.
- 라벨 결측: `kpx_group_1/2`의 2022-10-24~27 나흘은 NaN 유지(보간 안 함). 6시간 이내 짧은 결측만 선형보간.
- test_base LDAPS 결측 3시점은 ffill.
- **결빙(icing) 위험 피처(group_1/2 전용)**: 영하 AND 3~7m/s. 검증은 반드시 **SCADA 실측 풍속**으로 통제(ws_est로 통제하면 반대 결과 나오는 함정 있었음).
- **GFS-LDAPS 앙상블 차이**: `{group}_gfs_ldaps_diff`(평균 -2.26~-2.68, GFS가 체계적으로 낮음), `_absdiff`.
- **돌풍성(gust) 피처**: `{group}_gust_proxy`, LDAPS 50m u/v 최대-최소 범위의 벡터 크기.
- **`ws_est` 파생 6종의 fold-safe 처리(04절 5절)**: `sq/cube/corrected/regime_*/high_wind_caution/icing_risk`가 전부 leaky한 원본 `ws_est`에서 나온 것을 발견 → 폴드별 `ws_est_cv_*`로부터 그 자리에서 재계산(`add_fold_safe_ws_features`). 민석님이 (a)재계산 방식을 선택(새 fit 없이 산술 재계산만 하면 되고, high_wind_caution 효과 확인도 가능하다는 이유).
- **`{group}_air_density`는 재계산 안 함**: LDAPS 기온·기압만으로 계산돼 라벨/SCADA 의존이 없어 애초에 안 leaky.

### 02_eda에서 이어받은 것 (원본은 `reports/02_eda.md`)
- 검증 전략: A안(2022~2023 학습→2024 검증) + B안(확장 윈도우 3-fold) 항상 병행
- `kpx_group_3`은 2022년 라벨 자체가 없음
- 격자 매핑: GFS 전 그룹 공통 g5, LDAPS는 group_1→g5, group_2→g6, group_3→g12
- 후류효과 관측됐으나 그룹 단위 모델링이라 추가 조치 없음
- 예보 오차 하한: LDAPS-SCADA RMSE 2~3m/s — 모델 튜닝으로 못 줄이는 구조적 한계
- 풍향 피처는 그룹별로 설계(3개 그룹 다 WSW/W가 주풍향, group_3만 W가 크게 앞섬)

---

## 노트북 재사용 가능한 이름·규칙
- `wind_speed(df, u, v)` = `sqrt(u**2+v**2)`, `wind_direction_deg(df, u, v)` = `(270-degrees(atan2(v,u)))%360`
- `GROUP_NEAREST_LDAPS = {"kpx_group_1": 5, "kpx_group_2": 6, "kpx_group_3": 12}`, `GFS_NEAREST_GRID = 5`
- `TURBINE_GROUP_MAP`: VESTAS 1~6→group_1, 7~12→group_2, UNISON 1~5→group_3
- SCADA 그룹 집계: 10분→1시간 **평균**(발전량과 달리 합계 아님), `resample("h", closed="right", label="right")`
- `steiger_z_dependent(r_a, r_b, r_ab, n)`: 종속 상관계수 차이 유의성 검정 (2-1에서 정의)
- `fit_ridge(X, y, alpha)`: 능형회귀(numpy만, 절편은 규제 안 함), `RIDGE_ALPHA=30`
- `fit_power_curve(scada_ws, power, bin_edges)` / `apply_power_curve(ws, bc, vals)`: 12-1에서 정의
- `CV_CUTOFFS = {"2023_07":..., "2024_01":..., "2024_07":...}`: fold-safe 재학습용 컷오프
- `03_features.ipynb` 컬럼 네이밍: `{group}_ws10_nearest`, `ldaps_ws10_avg16`, `gfs_g5_ws_{10m,80m,100m,850hPa}`, `scada_ws_{group}`(train 전용), **`{group}_ws_est`(전체데이터 학습 — 최종 재학습 전용)**, **`{group}_ws_est_cv_{2023_07,2024_01,2024_07}`(fold-safe — 검증용)**, `{group}_ws_est_corrected/_sq/_cube`, `{group}_wd_sin/cos`, `hour_sin/cos`, `month_sin/cos`, `{group}_gfs_ldaps_diff/absdiff`, `{group}_icing_risk`(group_1/2만), `{group}_gust_proxy`, **`{group}_power_curve_est`(+`_cv_*`)**, `{group}_regime_calm/ramp/rated`, `{group}_high_wind_caution`
- **`train_features_v1.parquet`에 모델 입력으로 쓰면 안 되는 컬럼**: `scada_ws_kpx_group_*`, `scada_kpx_group_*`, `year`, 그리고 **`{group}_ws_est`·`{group}_power_curve_est`(원본, fold-safe 아님)**
- `04_model_selection.ipynb` 재사용 함수:
  - 5절 `add_fold_safe_ws_features(df, group_col, ws_col)`
  - 6절 `build_group_feature_frame(df, g, cv_suffix)` **(2026-08-01 누수 수정 완료)**, `leaky_cols_for_group(g)`, **`assert_no_leak(columns, g, cv_suffix)`**, `LABEL_DERIVED_MARKERS`, `score_predictions(actual_df, pred_df)`
  - 9-1절 `build_monotone_spec`, 9-2절 `fit_predict_gbdt`, 9-3절 `SimpleMLP`/`fit_predict_mlp`
  - **9-5절 `FOLD_INFO`/`PRED_CACHE`/`run_variant(variant, fit_fn)`/`summarize(result_dfs)`/`make_sample_weight(y, g, mode)`/`fit_predict_lgbm(...)`** ← 앙상블 실험을 재학습 없이 하기 위한 예측 캐시 인프라. 05_tuning에서도 그대로 재사용 권장
  - 10-2절 `eval_frame(variant)`/`wmean`/`error_anatomy(ef, by)` — 채점 대상 행만 모아 오차를 산식 관점으로 해부
  - 11절 `score_blend(weights, label)` — 캐시된 예측을 가중평균해 재학습 없이 채점
  - **13절 `add_forecast_dynamics(df, cols)`**(예보 lag1~3/lead1~3/ramp_prev·next/roll3_mean/roll7_mean·std·max·min = 컬럼당 13개), **`build_group_feature_frame_v2(df, g, cv_suffix, dynamics=True)`**(6절 빌더 + 동적 피처), **`fit_predict_lgbm2`/`lgbm2_fit_fn`/`fit_predict_mlp2`** ← 앞으로 학습은 이쪽을 쓸 것
- `FOLD_SPECS`/`CUTOFFS`(4절)는 A안·B안 fold 경계 딕셔너리로 이후 노트북에서도 재사용 가능

---

## 이번까지 발견된 버그·주의사항 (같은 실수 반복 방지용 — 꼭 읽을 것)
1. **연속-0 구간(zero-run) 탐지 로직 함정**: `run_id = (~is_zero).cumsum()`로 그룹을 나누면 각 그룹은 "0이 아닌 값 1개 + 뒤따르는 0들"이라 `is_zero.loc[grp.index].all()`이 거의 항상 False. 올바른 방법: `zero_s = s[is_zero]; zero_run_id = run_id[is_zero]`로 0인 값만 걸러낸 뒤 groupby.
2. **노트북 파일이 커지면 `Read`/`NotebookEdit` 도구가 실패한다**: 별도 `.py` 스크립트로 `json.load`/`json.dump`를 써서 직접 조작. `python -c "..."`로 bash에 백틱 포함 텍스트를 넣지 말 것.
   - **⚠️ 추가(2026-08-01)**: json으로 셀을 만들 때 `source`는 **각 줄이 `\n`으로 끝나는 리스트**여야 한다. `text.split("\n")`만 하고 `\n`을 다시 안 붙이면 모든 줄이 한 줄로 붙어 **SyntaxError**가 난다. 반드시 `[l+"\n" for l in lines[:-1]] + [lines[-1]]` 형태로 만들 것.
3. **`NotebookEdit`으로 여러 셀 순서대로 삽입 시**: `edit_mode="insert"`는 anchor 바로 뒤에 넣으므로 여러 개면 거꾸로 반복 삽입. json 스크립트는 `cells[idx:idx] = [...]`로 원하는 위치에 바로 삽입 가능.
4. **`01_preprocessing.ipynb` 실수**: SCADA `_power_kw10m`을 "10분 평균출력"으로 가정해 평균 집계했더니 라벨과 상관 거의 0 — 실제로는 "10분간 발전량(kWh)"이라 **합계**가 맞았다. 컬럼명을 그대로 믿지 말고 라벨/실측과 대조 검증.
5. **시각화는 "대체"가 아니라 "추가"가 기본**: boxplot을 히스토그램으로 통째로 바꿨다가 "둘 다 하라고" 피드백 받음.
6. **AI가 물리 공식으로 만든 파생 피처는 검증 전엔 신뢰하지 말 것**: 시어지수 외삽이 원래 피처보다 나빴던 사례.
7. **추정치로 만든 피처를 "다른 추정치 구간"으로 검증하면 착시**: 결빙 피처를 `ws_est` 구간으로 통제하면 반대 결과. **실측(SCADA)으로 통제**할 것.
8. **구간·임계값은 "EDA에서 확인했다"가 아니라 실제 숫자를 같이 남길 것**: 결빙 3~7m/s 상한이 group_2엔 안 맞는 근사치였음.
9. **라벨/SCADA를 참고해 만드는 피처는 처음부터 fold-safe하게 설계**.
10. **"물리적으로 그럴듯한 임계값"도 실제 분포를 찍어보기 전엔 쓰지 말 것**: 컷아웃 25m/s가 train에서 0건이라 상수 컬럼이 될 뻔함.
11. **fold-safe 피처 하나를 만들었다고 파생 컬럼이 자동으로 fold-safe인 게 아니다**: `ws_est`의 파생 6종이 원본을 참조하고 있었음.
12. **AI 백그라운드 검증과 민석님 노트북 실행이 CPU 경합**: 무거운 자체 검증 전에 "지금 노트북 실행 중인지" 먼저 확인할 것.
13. **CatBoost 기본 설정이 LightGBM/XGBoost보다 훨씬 느림**: 성능은 LightGBM과 동률인데 훨씬 느리므로 LightGBM 채택.
14. **⭐ (2026-08-01, 가장 비싼 실수) 제외 목록을 "이름 열거"로 관리하면 반드시 하나를 빠뜨린다**: `leaky_cols_for_group`에 `power_curve_est` 원본을 빠뜨려 9~12절 전체를 누수 상태로 돌렸다. 11번 교훈("파생까지 챙겨라")을 배우고도 **본체를 빠뜨린** 것. **올바른 방법: 열거가 아니라 규칙으로 판정한다.** 컬럼 이름에 라벨 의존 표식(`ws_est`, `power_curve_est`)이 들어 있으면 `__`로 잘라 뿌리를 구하고, 그 뿌리가 현재 fold의 허용 컬럼이 아니면 무조건 실패(`assert_no_leak`). 새 라벨 의존 피처를 추가할 때는 이 표식 목록에 추가만 하면 된다.
15. **⭐ 손실함수의 스케일 파라미터는 타깃 단위다**: LightGBM `huber`의 `alpha` 기본값 0.9를 kWh 타깃(0~21,600)에 그대로 써서 학습이 사실상 안 되는데도 "단조 제약이 나쁘다"는 **완전히 틀린 결론**을 낼 뻔했다. 손실함수를 바꿀 때는 스케일 파라미터(`alpha`, `delta`, `huber_slope` 등)를 반드시 타깃 스케일에 맞춰 명시하고, **"제약을 뺀 대조군"을 같이 돌려서 무엇이 원인인지 분리**할 것.
16. **⭐ 단변량 상관으로 피처 가치를 판정하지 말 것**: `ws_est_corrected`(밀도보정)를 상관 +0.001이라고 "효과 미미"로 판정했는데, 실제 모델에서는 group_1 gain 1위(33.98%)였다. 반대로 `regime_*`/`high_wind_caution`은 도메인 근거가 그럴듯했으나 gain 0.000이었다. **최종 판정은 반드시 모델 안에서(importance/ablation)** 할 것.

---

## 실험 기록
| 날짜 | 실험 | 로컬 Score | 리더보드 | 결론 |
|---|---|---|---|---|
| 2026-07-31 | 04 베이스라인 사다리 4종 | 0.4334~0.5965 (A안) | – | LightGBM이 물리 파워커브·선형회귀를 모두 이김 (⚠️ 누수 포함) |
| 2026-08-01 | 04 후보 모델 7종 | 0.5833~0.5933 (B평균) | – | LightGBM≈CatBoost>XGBoost, 단조제약 무효, MLP는 앙상블용 (⚠️ 누수 포함) |
| 2026-08-01 | 04 산식-인지(가중·τ) | 0.6089 / 0.6084 (B평균) | – | actual 가중 +0.016, τ=0.6 +0.015, 둘 다 전 fold 개선 (⚠️ 누수 포함) |
| 2026-08-01 | **13절 누수 수정 기준선 `fix_base`** | **0.5915 / 0.5888** | – | 누수 크기는 -0.005로 작았음. 이제부터 진짜 기준선 |
| 2026-08-01 | 13절 `fix_dyn` (예보 lag/lead) | 0.5991 / 0.5915 | – | 4 fold 중 3개 개선, fold1만 -0.0016. σ 0.1708→0.1679 (유일하게 σ 감소) |
| 2026-08-01 | 13절 `dyn_w_q60` (가중+τ) | 0.6252 / 0.6124 | – | 최고 단일. 1-NMAE를 팔아 FICR을 사는 거래 |
| 2026-08-01 | **13절 최종 블렌드 (0.7 LGB + 0.3 MLP)** | **0.6288 / 0.6170** | – | **04단계 최종. `fix_base` 대비 +0.037/+0.028** |
| 2026-08-01 | 14절 오라클 (SCADA 실측 풍속) | 0.8587 / 0.8618 | – | 예보 오차가 σ의 89%. 방향을 풍속 개선으로 전환 |
| 2026-08-01 | 15절 리드타임·피처축소·그룹τ·시드 | 0.6259 / 0.6170 | – | 4종 합쳐 +0.0037. 작음 |
| 2026-08-01 | **16절 풍속 모델 교체(Ridge→LightGBM)** | **0.6391 / 0.6298** | **0.6271** | **단일 최대 기여 +0.0164. 첫 제출** |
| 2026-08-01 | 17절 풍속 앙상블 / τ 재조정 | 0.6286 / 0.6276 | – | 둘 다 개선 없음. 풍속 평균내기는 오히려 손해(Jensen) |
| 2026-08-01 | 18절 풍속 HP 튜닝 5종 | 0.6262 / 0.6229 | – | **전부 기본값에 패배.** 풍속 GBDT는 포화 |
| 2026-08-01 | **18절 MLP 풍속 블렌드(0.7:0.3)** | **0.6412 / 0.6321** | **0.6288** | **채택. v2 제출** |
| (예정) | 20절 터빈 가동률 라벨 정제 | | | 예측불가 성분의 20~35%가 터빈 정지로 확인됨 |
| (예정) | 21절 안정도·TI·파워커브 밀도보정 | | | 풍속 모델 입력 보강 |

---

## 미해결 질문
1. group_1/2의 2022-10-24~27 결측 나흘, NaN 유지로 확정했으나 실제 모델 성능 영향은 미확인
2. group_3(UNISON)만 결빙 효과가 없는 이유는 공식 스펙 없이는 확정 불가
3. 후류효과의 정량적 반영 — 그룹 단위 모델링이라 "안 함"으로 결론, 성능이 기대 미달이면 재검토
4. ~~`high_wind_caution`이 유효한가~~ → **종결: gain 0.000, 무용**(단 누수 수정 후 재확인 필요)
5. 풍향×풍속(주풍향 정렬도) 상호작용 피처 미구현 — 우선순위 낮음
6. ~~12절 파워커브 잔여 리스크 3가지~~ → **부분 종결: 실재하나 방향이 그룹마다 정반대**(group_3만 고풍속 과소예측). 일괄 보정 부적절, **그룹별 보정**을 05_tuning에서 검토
7. ~~누수의 실제 크기는?~~ → **종결: A안 -0.0050 / B평균 -0.0045로 작았음**
8. ~~표본가중과 τ는 얼마나 겹치는가?~~ → **종결: 크게 겹침.** 각각 +0.018/+0.017인데 결합은 +0.021(단순 합 +0.035의 60%)
9. ~~예보 lag/lead가 σ를 줄이는가?~~ → **부분 종결: 줄이긴 하나 폭이 작다**(0.1708→0.1679, -1.7%). 위상 오차 가설은 **약하게만** 지지됨 → 시퀀스 모델(LSTM/TFT) 기대치는 낮춰 잡을 것. 반면 **공간 방향(격자 CNN)은 아직 전혀 시도 안 함**
10. **(신규) 바람을 완벽히 알면 몇 점인가?** — 오라클 실험으로 측정 예정(다음 할 일 0번). 이 답이 A2/A3(CNN·시퀀스)로 갈지 B1(분포 예측)으로 갈지를 가른다
11. **(신규) MLP 블렌드 이득은 진짜 다양성인가, 편향 상쇄인가?** — LightGBM +0.054 과대 / MLP -0.042 과소. MLP에도 같은 가중·τ를 적용한 뒤 이득이 남는지 확인 필요
12. **(신규) group_3의 rated(12~15) 구간만 여전히 과소예측(-0.051)인 이유는?** — 누수 수정으로 -0.150에서 크게 완화됐으나 유일한 음의 편향 구간으로 남음. UNISON 터빈의 파워커브 특성 차이 추정(미해결 질문 2번과 같은 뿌리일 수 있음)

---

## 환경 메모
- venv 설치 패키지: **scipy**, **lightgbm 4.7.0 / xgboost 3.3.0 / catboost 1.2.10 / scikit-learn 1.9.0**, **torch 2.13.0+cpu**(CPU 전용 빌드 — 2차 평가 재현성 유리). **`requirements.txt` 아직 없음** — 모델 선택·튜닝 단계 패키지가 다 들어간 뒤 한꺼번에 정리 예정
- `venv\Scripts\python.exe`를 써야 한다. 시스템 `python`에는 pandas조차 없다
- `03_features.ipynb` 1~13절 전체를 민석님이 직접 실행 완료(에러 0건). `data/processed/train_features_v1.parquet`(26304,888)/`test_features_v1.parquet`(8760,860) 확정
- **`04_model_selection.ipynb` 1~13절 전체를 민석님이 직접 실행 완료**(2026-08-01, 에러 0건). 9~11절 출력은 누수 상태이나 13절이 그 위를 덮는 최종 결과다. `reports/04_model_selection.md`도 13절 기준으로 작성 완료
- **노트북 셀 수 98개.** 9절 헤더 앞(인덱스 39)에 누수 경고 마크다운 셀이 삽입돼 있다
- **13절 설계 결정: `03_features.ipynb`와 parquet을 건드리지 않는다.** lag/lead 피처는 `04_model_selection.ipynb` 13절의 프레임 빌더 안에서 그때그때 계산한다. 이유: (a) parquet 재생성 없이 즉시 실험 가능 (b) fold-safe 컬럼(`ws_est_cv_*`)에서 파생하므로 fold마다 다른 컬럼을 써야 하는데, parquet에 미리 만들면 fold 조합 수만큼 컬럼이 폭증한다 (c) `build_group_feature_frame`이 이미 `df`를 인자로 받으므로 test에도 같은 코드가 그대로 적용된다. **효과가 확인되면 그때 03으로 옮겨 정식화할지 결정한다**
- 노트북 파일 크기: `02_eda.ipynb` 122셀, `04_model_selection.ipynb` 80셀+ — 둘 다 "버그·주의사항 2번" 방식(json 스크립트)으로 편집할 것
