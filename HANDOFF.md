# HANDOFF — 마지막 갱신: 2026-07-30 (장소: 학교)

## 새 세션 시작 시 가장 먼저 할 것
1. 이 파일 전체를 읽는다 (특히 "이번까지 발견된 버그·주의사항" — 같은 실수를 반복하지 않기 위해 필수)
2. `git log --oneline -5`, `git status`로 커밋 상태 확인
3. `reports/02_eda.md`를 훑어 EDA 결론을 확인한다 (이 문서의 요약보다 원문이 항상 더 정확함 — 요약과 다르면 리포트를 우선한다)
4. 민석님에게 "지난 세션에서 EDA 1~6절 완료했고, 다음은 `03_features.ipynb` 시작입니다" 정도로 3줄 이내 보고 후 시작

## 현재 위치
- 로드맵 단계: 3. EDA **완료** → 다음은 4. 기본 전처리 확장(`preprocessing` 스킬) / 6. 피처 엔지니어링(`wind-domain-features` 스킬) — `03_features.ipynb`는 아직 파일 자체가 없음(새로 만들어야 함)
- 완료 파일: `notebooks/02_eda.ipynb`(1~6절, 122개 셀, 전부 실행 완료), `reports/02_eda.md`(전체 정리 완료)
- 아직 git 커밋 안 됨 (아래 "환경 메모" 참고)

## 지난 세션(들)에서 한 것 — 누적 요약
- `01_preprocessing.ipynb`: GFS/LDAPS pivot, SCADA 10분→1시간 집계, train_base/test_base parquet 캐시 생성 (완료, `reports/01_preprocessing.md`)
- `02_eda.ipynb` 1절(타깃): 분포 L자형, 평가대상비율 53.7~60.7%, 그룹간 상관 0.90~0.96, 일주기·연도별 drift 확인
- `02_eda.ipynb` 2절(기상예보): 격자-터빈 거리 계산, 결측 분해, 높이별 풍속 상관 순위
- `02_eda.ipynb` 3절(타깃×기상 심화): 파워커브(예보기반, 넓은 삼각형), 바람장미, 계절×풍속 상호작용(그룹마다 다름 — 나중에 4절에서 대부분 착시로 판명)
- `02_eda.ipynb` 4절(SCADA 심화): SCADA 풍속 새로 집계, 예보-실측 편향/RMSE, SCADA기준 파워커브(뚜렷한 S자), 일주기 저점 원인(curtailment 아님), 24h+ 연속0 구간의 실제 시각·SCADA풍속 확인
- `02_eda.ipynb` 5절(시간 무결성): 중복/간격/경계 전부 깨끗함 확인
- `02_eda.ipynb` 6절(DS·도메인 후속검증): train/test 분포 비교, 컷아웃 재확인, 결빙 가설 검증, 후류효과 탐색
- **EDA 전체 완료. 다음 세션은 `03_features.ipynb`를 새로 만드는 것부터 시작.**

## 다음 할 일 (우선순위순)
1. `03_features.ipynb` 신규 생성 — `notebook-workflow` 스킬(공통 구조)과 `wind-domain-features` 스킬(피처 근거) 참고
2. 최우선 피처 후보 (아래 "결정 사항"에 근거 상세):
   - 허브고도(117m) 외삽 풍속 (윈드시어 멱법칙, GFS 10/80/100m 또는 LDAPS 10m을 외삽)
   - curtailment 후보 구간 플래그 (완전 제외 아님 — 아래 미해결질문 참고)
   - group_1/2 전용 결빙(icing) 위험 피처 (기온<0℃ & 저~중풍속)
   - 그룹별 최근접 LDAPS 격자, 그룹별 주풍향(WSW/W) 기반 방향 피처
3. 피처 생성 전에 leakage-guard 스킬로 각 피처의 예측기준시점 위반 여부 먼저 점검 (특히 curtailment 플래그처럼 "과거 라벨을 보고 만드는 피처"는 test에 라벨이 없으므로 test에는 다른 방식으로 만들어야 함 — 아래 미해결질문 1번 참고)
4. 결측치 처리(2022-10-24~27 나흘 등)는 `preprocessing` 스킬 관점에서 03_features 초반에 먼저 정리

## 결정 사항 / 근거 (누적, EDA 최종본 — `reports/02_eda.md`가 원본, 여기는 요약)
- 검증 전략: A안(2022~2023 학습→2024 검증) + B안(확장 윈도우 3-fold) 항상 병행 — CLAUDE.md 확정 사항. 6절에서 train/test 기상 분포가 비슷함(test가 5~9% 더 바람 셈, 영하비율 26.6%→27.2%로 거의 동일)을 확인해 추가 보정 불필요함을 재확인
- `kpx_group_3`은 2022년 라벨 자체가 없음 → 학습 시 2023년부터 시작
- `kpx_group_1/2`의 2022-10-24~27 나흘 연속 결측은 보간하지 않고 학습 제외 또는 결측 플래그 처리 방향 (미확정, 03_features에서 결정)
- test_base의 LDAPS 결측 3시점(2025-04-08 17시/06-18 18시/07-18 06시)은 직전 예보값으로 채움 (미래 시점 아님 → leakage 아님)
- 격자 매핑(확정): GFS는 전 그룹 공통 g5(터빈 전체가 GFS 셀 하나 안에 들어감). LDAPS는 group_1→g5, group_2→g6, group_3→g12 (터빈 중심좌표-격자 거리로 계산, 2절에서 확정, 3절부터 실제 적용)
- **주력 피처: 허브고도(117m) 외삽 풍속** — SCADA풍속-발전량 상관(0.9112~0.9395)이 예보 기반 최고 상관(GFS850hPa/LDAPS10m, 0.7389~0.7689)보다 훨씬 높아, 10m/100m 예보의 낮은 고도가 병목이었음이 확정됨. 컷인 약 3m/s, 정격(설비용량 도달) 약 11~12m/s(SCADA 관찰값)
- **curtailment 확정 — 3그룹 동시 발생 구간(정확한 시각, group_1 기준)**:
  | 시작 | 끝 | 시간 | group_1 평균/최고풍속(m/s) |
  |---|---|---|---|
  | 2023-02-13 02:00 | 2023-02-16 14:00 | 85h | 4.95 / 13.03 |
  | 2024-01-18 17:00 | 2024-01-23 16:00 | 120h | 8.15 / 17.83 |
  | 2024-02-22 01:00 | 2024-02-29 01:00 | 169h | 5.43 / 11.33 |

  (group_2/group_3도 거의 같은 날짜에 겹쳐서 발생 — 정확한 그룹별 시각은 `reports/02_eda.md` 4절 Result 표 참고). 풍속 충분 + 컷아웃(최고풍속 20.4m/s 이하, 25m/s 미만) 배제 → 순수 계통 제약/정비로 확정. **완전 제외보다 플래그 처리를 우선 검토** (2025 test에도 유사 이벤트가 있을 수 있어 완전 제외는 위험할 수 있음 — 미해결질문 참고). 2023-08-16~17(풍속 1.6~1.7m/s)만 진짜 무풍이므로 정상 데이터로 유지
- **일주기(13~16시 저점)는 curtailment 아니라 실제 풍속 저하로 확정** — SCADA 풍속·발전량 시간대별 프로파일이 거의 완벽히 같은 모양으로 움직임(국지순환 등 물리적 원인 추정, 원인 자체는 더 규명 안 함)
- **계절×풍속 상호작용은 대부분 예보 대리변수의 한계였음으로 확정** — SCADA 기준 재검증 시 3-3의 그룹별 큰 차이(group_1 최대 2배 등)가 거의 사라짐. 별도 계절 보정 피처보다 허브고도 외삽 풍속 자체가 우선
- **결빙(icing) 효과 확정 — 그룹별로 다름**: group_1/2(VESTAS)는 저~중풍속(3~7m/s)·영하 조건에서 발전량이 영상 대비 10~50% 낮음(결빙 전형 패턴: 저풍속일수록 차이 크고 고풍속으로 갈수록 사라짐). **group_3(UNISON)은 거의 차이 없음** — 결빙 위험 피처는 group_1/2에만 적용, group_3엔 넣을 근거 약함(터빈 제작사 차이로 추정, 공식 스펙 문서 없어 확정 아님)
- **후류효과(wake effect) 관측됨**: group_1 6기 중 NW~WNW 방위에서 vestas_wtg01·wtg04만 풍속이 높고(8~9m/s) 나머지 4기(wtg02/03/05/06)는 낮음(4~6m/s) — 그룹 평균 SCADA-예보 상관이 100%가 아닌 이유 중 하나. 그룹 단위 모델링 구조상 터빈별 피처는 안 만듦 — "그룹 평균 풍속"에 내재된 잡음으로 이해하고 추가 조치 없음
- 예보 오차 하한: LDAPS-SCADA RMSE 2.09~3.15m/s, 편향 +0.73~+2.11m/s(예보가 항상 낮음) — 모델 튜닝으로 이 이상 줄일 수 없는 구조적 오차. 모델 선택/튜닝 단계 기대치 설정에 참고(즉, NMAE가 이 오차보다 훨씬 낮게 나오길 기대하지 않는다)
- 풍향 피처는 그룹 공통이 아니라 그룹별로 설계 (3개 그룹 모두 WSW/W가 주풍향·고발전 방향, 다만 group_3만 W가 WSW를 크게 앞섬 11556 vs 2978시간 — 그룹별 국지 지형차)
- LDAPS 50m max/min·5m 경계층 바람(`heightAboveGround_50_50MUmax/min`, `50MVmax/min`, `heightAboveGround_5_XBLWS/YBLWS`)은 평균값이 아니라서 2~4절에서 미사용 — `(max+min)/2` 근사평균, `max-min` 돌풍성 피처로 03_features에서 활용 검토 가능(숙제로 남겨둠, 아직 시도 안 함)

## 노트북(`02_eda.ipynb`) 재사용 가능한 이름·규칙 — 03_features 작성 시 참고
새 노트북에서 그대로 베낄 필요는 없지만, 같은 방식을 쓰면 결과가 일관됨:
- `wind_speed(df, u_col, v_col)`: `sqrt(u**2+v**2)` — 2-3에서 정의, 3~6절 전체에서 재사용
- `GROUP_NEAREST_LDAPS = {"kpx_group_1": 5, "kpx_group_2": 6, "kpx_group_3": 12}` — 그룹별 최근접 LDAPS 격자 번호
- `TURBINE_GROUP_MAP`: VESTAS 1~6→group_1, 7~12→group_2, UNISON 1~5→group_3 (01_preprocessing과 동일 매핑)
- `wind_direction_deg(u, v)` = `(270 - degrees(atan2(v, u))) % 360` (기상학적 풍향, 0=북), `to_16_sector(deg)`로 16방위 분류 — 3-2에서 정의
- SCADA 그룹별 시간 집계: 10분→1시간은 **평균**(발전량처럼 합계 아님), `resample("h", closed="right", label="right")`로 시간 경계 정렬 — 4-1에서 정의
- 컬럼 네이밍: `gfs_g{1~9}_...`, `ldaps_g{1~16}_...`, `{group}_ws_nearest`(3-1), `scada_ws_{group}`(4-1, 평균), `scada_ws_max_{group}`(6-2, 최고순간), `{group}_wd_sector`(3-2), `{group}_scada_ws_bin`(4-6), `{group}_freezing`(6-3)

## 이번까지 발견된 버그·주의사항 (같은 실수 반복 방지용 — 꼭 읽을 것)
1. **연속-0 구간(zero-run) 탐지 로직 함정**: `run_id = (~is_zero).cumsum()`로 그룹을 나누면, 각 그룹은 "0이 아닌 값 1개 + 뒤따르는 0들"로 구성된다(맨 앞 그룹 제외). 그래서 `is_zero.loc[grp.index].all()`로 "그룹 전체가 0인지" 검사하면 **거의 항상 False가 나와 아무것도 못 찾는다**(실제로 4-5에서 이 버그로 결과가 안 나왔었음). 올바른 방법: `zero_s = s[is_zero]; zero_run_id = run_id[is_zero]` 로 **0인 값만 먼저 걸러낸 뒤** 그걸 `run_id`로 groupby해야 각 그룹이 순수하게 "연속된 0 구간"이 된다. 이 패턴은 `03_features.ipynb`에서 유사한 "연속 구간 탐지"를 다시 짤 때도 그대로 적용된다.
2. **노트북 파일이 커지면 `Read` 도구가 실패한다**: `02_eda.ipynb`가 122개 셀(이미지 출력 포함)까지 커지자 `Read` 도구가 "25000 토큰 초과"로 전체는 물론 `offset`/`limit`을 줘도 읽기 자체가 안 됐다(이 도구는 오프셋과 무관하게 렌더링 크기로 제한을 거는 것으로 보임). `NotebookEdit`은 대상 파일을 `Read`로 먼저 읽어야만 동작하므로, 이 상태에서는 `NotebookEdit`도 못 쓴다. **해결책**: `python -c` 대신 **별도 `.py` 스크립트 파일을 작성**해 `json.load`/`json.dump`로 노트북 파일을 직접 읽고 수정한다(cell 리스트를 딕셔너리로 조작 — `id`/`cell_type`/`source`/`outputs`/`execution_count` 필드를 직접 채워야 함, `source`는 `text.splitlines(keepends=True)` 형태). **`python -c "..."`처럼 bash 커맨드라인에 곧바로 파이썬 코드를 넣지 말 것** — 마크다운 텍스트 안의 백틱(`` ` ``)이 bash에서 명령어 치환으로 오작동해 SyntaxError가 난다(이번 세션에 실제로 겪음). 항상 `Write`로 `.py` 파일을 만들고 `python 파일경로`로 실행할 것.
3. **`NotebookEdit`으로 여러 셀을 순서대로 삽입할 때**: `edit_mode="insert"`는 지정한 `cell_id` **바로 뒤**에 새 셀을 넣는다. 여러 셀을 원하는 순서로 넣으려면, **같은 anchor cell_id에 거꾸로(마지막 셀부터) 반복 삽입**하면 최종 순서가 맞게 나온다(먼저 넣은 게 뒤로 밀려나므로). 파일이 작을 때(Read 가능할 때)는 이 방법이 잘 통했음.
4. **`01_preprocessing.ipynb`에서 이미 겪은 실수(참고, `reports/01_preprocessing.md` 원문 참고)**: SCADA `_power_kw10m`을 "10분 평균출력(kW)"으로 가정해 평균으로 집계했더니 라벨과 상관이 거의 0 — 실제로는 "10분간 발전량(kWh)"이라 **합계**로 집계해야 했다. **컬럼명을 그대로 믿지 말고 항상 라벨/실측과 대조 검증**하는 습관을 `03_features.ipynb`에서도 유지할 것.
5. **사용자 피드백 — 시각화는 "대체"가 아니라 "추가"가 기본**: `yearly-drift` 셀을 boxplot에서 히스토그램으로 통째로 바꿨더니 "boxplot이랑 plot 둘 다 하라고, 하나만 하지 말고"라는 피드백을 받음. 기존 시각화를 새 시각화로 교체하라는 명시적 지시가 없으면, 다른 관점의 그림은 기존 것에 이어서(별도 셀로) 추가하는 걸 기본값으로 한다.

## 실험 기록
| 날짜 | 실험 | 로컬 Score | 리더보드 | 결론 |
|---|---|---|---|---|
| (아직 없음 — 베이스라인 이전 단계) | | | | |

## 미해결 질문
1. **curtailment 후보 구간을 03_features에서 완전 제외할지 플래그만 남길지 — 특히 test(2025)에는 라벨이 없어서 "발전량=0인데 풍속 충분"으로 curtailment를 판정하는 지금 방식 자체를 쓸 수 없다.** test용으로는 "언제 curtailment가 일어날지"를 예측할 방법이 없으므로, train에서 이 구간들을 제외/플래그하는 게 오히려 실제 운영에서 못 맞히는 상황을 학습에서 인위적으로 없애는 셈일 수도 있다 — 이 트레이드오프를 03_features 시작할 때 반드시 다시 논의할 것
2. group_1/2의 2022-10-24~27 결측 나흘 구간을 학습에서 완전히 뺄지, 짧게라도 보간할지 아직 미정
3. group_3(UNISON)만 결빙 효과가 없는 이유(터빈 제작사 차이 추정)는 공식 스펙 문서 없이는 확정 불가 — 근거는 약하지만 무시하지 말고 그룹별 피처 차별화의 이유로만 활용
4. LDAPS 50m max/min 기반 돌풍성 피처, 후류효과의 정량적 반영 여부(현재는 "안 함"으로 결론냈으나 모델 성능이 기대에 못 미치면 재검토 후보)

## 환경 메모
- venv 패키지 변경 없음
- `notebooks/02_eda.ipynb`, `reports/02_eda.md`, `HANDOFF.md` 변경분이 아직 git에 커밋 안 됨 — 커밋 명령은 직전 대화에서 제안됨(`git add notebooks/02_eda.ipynb reports/02_eda.md HANDOFF.md && git commit ...`), 민석님이 아직 실행 안 하셨다면 이 세션에서 먼저 확인할 것
- `02_eda.ipynb`는 이제 파일이 커서(122셀) IDE에서 열 때 느릴 수 있고, Claude가 도구로 편집할 때도 위 "버그·주의사항 2번" 방식(직접 json 스크립트)을 써야 한다 — `03_features.ipynb`는 처음부터 이미지 출력이 많아지지 않도록 무거운 그림은 필요한 것만 그리는 걸 권장
