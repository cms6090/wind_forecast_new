# HANDOFF — 마지막 갱신: 2026-07-29 (장소: 학교)

## 현재 위치
- 로드맵 단계: 3. EDA (진행 중)
- 작업 중 파일: `notebooks/02_eda.ipynb` — 1절(타깃)·2절(기상 예보 GFS/LDAPS) 실행 완료, 3~5절은 아직 셀 없음

## 지난 세션에서 한 것
- `01_preprocessing.ipynb`로 만든 parquet 캐시(`train_base`, `test_base`, `gfs_grid_meta`, `ldaps_grid_meta`)를 기반으로 `02_eda.ipynb` 1~2절 작성·실행 완료
- `reports/02_eda.md` 작성 (Why/How/Result/So-what)
- `height-vs-target-corr` 셀을 group_1만 계산하던 것에서 3개 그룹 모두 반복하도록 수정, 재실행 완료 — 값 확정
- 노트북 "요약 및 다음 단계" 셀 갱신

## 다음 할 일 (우선순위순)
1. `02_eda.ipynb` 3절 작성 — 타깃×기상 관계 심화: 파워커브(예보 풍속 vs 발전량 산점도), 풍향별 발전량, 계절×풍속 상호작용
2. `02_eda.ipynb` 4절 작성 — SCADA 심화: 실측 풍속 vs 예보 풍속 대조, **일주기 저점(13~16시) 원인이 curtailment인지 실제 풍속 저하인지 판별**, SCADA 합계 vs train_labels 대조
3. `02_eda.ipynb` 5절 작성 — 시간 무결성: 중복 타임스탬프, 시간 간격 균일성, train/test(2025-01-01) 경계
4. 3~5절 결과를 `reports/02_eda.md`에 이어서(또는 별도 절로) 정리
5. EDA 전체 마무리 후 `preprocessing` 스킬로 넘어가 결측 처리 방침(아래 결정 사항 참고)을 실제 전처리 코드에 반영

## 결정 사항 / 근거 (누적)
- 검증 전략: A안(2022~2023 학습→2024 검증) + B안(확장 윈도우 3-fold) 항상 병행 — CLAUDE.md 확정 사항, 이번 세션에서 연도별 median이 2022→2024로 완만히 하락하는 경향을 추가로 확인해 이 방침이 여전히 유효함을 재확인함
- `kpx_group_3`은 2022년 라벨 자체가 없음 → 학습 시 2023년부터 시작
- `kpx_group_1/2`의 2022-10-24~27 나흘 연속 결측은 보간하지 않고 학습 제외 또는 결측 플래그 처리 방향 (3_features에서 확정)
- test_base의 LDAPS 결측 3시점(2025-04-08 17시/06-18 18시/07-18 06시)은 직전 예보값으로 채움 (미래 시점 아님 → leakage 아님)
- 격자 매핑: GFS는 전 그룹 공통 g5. LDAPS는 group_1→g5/g6 인근, group_2→g6, group_3→g12 인근 (터빈 중심좌표-격자 거리 계산으로 확정)
- 주력 피처 1차 후보 순위(그룹 공통): GFS 850hPa > LDAPS 10m > GFS 100m > GFS 80m > GFS 10m — 단, 850hPa이 허브고도(117m)에 가까운 100m보다 상관이 높은 것은 아직 가설 단계, 4절 SCADA 대조 후 확정

## 실험 기록
| 날짜 | 실험 | 로컬 Score | 리더보드 | 결론 |
|---|---|---|---|---|
| (아직 없음 — 베이스라인 이전 단계) | | | | |

## 미해결 질문
- 일주기 발전량 저점(13~16시)이 curtailment(계통 출력제한) 때문인지, 실제 산곡풍 물리 현상으로 낮에 풍속이 약해지는 것인지 — 4절 SCADA 실측 풍속과 대조해서 판별 필요
- 발전량 상관이 GFS 100m(허브고도 근접)보다 GFS 850hPa(자유대기)에서 더 높게 나온 이유 — 지형 영향 때문인지 GFS 100m 산출 방식 문제인지 미확정
- group_1/2의 2022-10-24~27 결측 나흘 구간을 학습에서 완전히 뺄지, 짧게라도 보간할지 아직 미정

## 환경 메모
- venv 패키지 변경 없음
- `notebooks/02_eda.ipynb`, `reports/02_eda.md`는 아직 git에 커밋 안 됨 (커밋 제안 아래 참고)
