# HANDOFF — 마지막 갱신: 2026-07-30 (장소: 학교)

## 새 세션 시작 시 가장 먼저 할 것
1. 이 파일 전체를 읽는다 (특히 "이번까지 발견된 버그·주의사항" — 같은 실수를 반복하지 않기 위해 필수)
2. `git log --oneline -5`, `git status`로 커밋 상태 확인
3. `reports/02_eda.md`, `reports/03_features.md`를 훑어 결론을 확인한다 (이 문서의 요약보다 원문이 항상 더 정확함 — 요약과 다르면 리포트를 우선한다)
4. 민석님 답변 대기 중인 질문이 있으면(아래 "다음 할 일" 0번) 먼저 여쭤본다
5. 민석님에게 "지난 세션에서 EDA 완료 + `03_features.ipynb` 전체(1~11절, 결측/curtailment 처리·SCADA 회귀 풍속·밀도보정·시간/방향/앙상블차이/결빙/돌풍성 피처) 작성 완료했고, 다음은 민석님이 노트북을 실행해 확인하는 것입니다" 정도로 3줄 이내 보고 후 시작

## 현재 위치
- 로드맵 단계: 6. 피처 엔지니어링 **완료** (`wind-domain-features` 스킬) → 다음은 7. 모델 선택(`model-selection` 스킬, `04_model_selection.ipynb` 신규 생성)
- 작업 중 파일: `notebooks/03_features.ipynb` (**58개 셀**, 1~11절 전부) — **민석님이 직접 실행 완료, 에러 0건, 모든 수치가 AI 사전 검증값과 정확히 일치 확인됨**(연도별 상관, out-of-sample 검증, 회귀계수 표, 결빙 위험 검증 표 전부 일치)
- `reports/03_features.md` **작성 완료, 수치 검증 완료**
- `train_features_v1.parquet`(26304,855)/`test_features_v1.parquet`(8760,845) 최종 저장 확인됨
- EDA(`02_eda.ipynb` 1~6절)는 완료 상태 유지, `reports/02_eda.md`도 완료
- (참고) 여러 셀에서 `PerformanceWarning: DataFrame is highly fragmented` 경고가 뜨지만 에러 아님 — 컬럼을 하나씩 추가해서 생기는 성능 힌트일 뿐, 이 데이터 규모에서는 무시해도 됨. 나중에 여유 있으면 `pd.concat`으로 한번에 합치는 방식으로 정리 가능(우선순위 낮음)

## 지난 세션(들)에서 한 것 — 누적 요약
- `01_preprocessing.ipynb`: GFS/LDAPS pivot, SCADA 10분→1시간 집계, train_base/test_base parquet 캐시 생성 (완료, `reports/01_preprocessing.md`)
- `02_eda.ipynb` 1~6절: 타깃 분포·기상예보·타깃×기상 심화·SCADA 심화·시간 무결성·DS/도메인 후속검증 — EDA 완료 (완료, `reports/02_eda.md`)
- `03_features.ipynb` (이번 세션, 1~11절 전부 작성 완료, 아직 미실행):
  - 1절: 라벨 결측(짧은 것만 보간, 4일짜리 긴 구간은 그대로 NaN 유지) / test LDAPS 결측 3시점 ffill / **curtailment 확정 3구간을 학습 라벨에서 제외**
  - 2절(+2-1 근거검증): 그룹별 최근접·16격자평균 LDAPS 풍속, GFS 4개 높이(g5) 풍속
  - **3절(+3-2b 근거검증, 핵심): SCADA 회귀 기반 추정풍속(`{group}_ws_est`)** — 아래 "결정 사항"에 상세
  - 4절: 공기밀도 보정(IEC 61400-12-1 방식)
  - 5절: v², v³ (파워커브 동력학)
  - 6절: 풍향 sin/cos
  - 7절: 시간(hour/month) sin/cos
  - 9절: GFS-LDAPS 앙상블 차이 피처
  - 10절: group_1/2 전용 결빙(icing) 위험 피처 — **검증 시 SCADA로 통제해야 하는 함정을 겪고 고침(아래 버그·주의사항 7번)**
  - 11절: LDAPS 50m max/min 기반 돌풍성(gust) 피처
  - 8절: `train_features_v1.parquet`(26304,855)/`test_features_v1.parquet`(8760,845) 저장 (AI가 검증 목적으로 이미 한 번 생성해둠 — 민석님이 노트북 실행하면 동일 내용으로 덮어써짐)

## 다음 할 일 (우선순위순)
0. **민석님 답변 대기 중**: "`requirements.txt`를 지금 만들지, 나중에 한꺼번에 정리할지" AI가 질문했고 아직 답을 못 들음
1. `04_model_selection.ipynb` 신규 생성 — `model-selection` 스킬 참고, 후보 모델(Ridge/LightGBM/XGBoost/CatBoost 등) 공정 비교
2. **`04_model_selection.ipynb`를 시작하면 가장 먼저 확인할 것**: 3절 회귀추정풍속(`ws_est`)의 fold-purity 캐벗 — A안/B안 검증 점수에서 이 피처가 `03_features.ipynb` 3-2의 out-of-sample 수치(0.85~0.87 근처)와 비슷하게 유지되는지 재확인. 떨어지면 fold별 재학습으로 바꿔야 함
3. `train_features_v1.parquet`에서 모델 입력 피처 목록을 고를 때 `scada_ws_kpx_group_*`, `scada_kpx_group_*`, `year` 컬럼 반드시 제외(아래 참고)

## 결정 사항 / 근거 (누적)

### 이번 세션에서 새로 정한 것 (03_features.ipynb)

- **주력 풍속 피처가 "허브고도 외삽"에서 "SCADA 회귀 추정풍속"으로 바뀜.** 경위를 자세히 남긴다(민석님이 낸 아이디어이자 판단이므로, 틀렸을 경우를 대비해 특히 상세히 기록):
  1. **1차 시도(기각)**: 윈드시어 멱법칙으로 GFS 10m/100m에서 시어지수 α를 구해 LDAPS 10m을 117m로 외삽. 결과: 발전량 상관이 오히려 나빠짐(group_1 0.73→0.60). 원인: 저풍속에서 log비율(α)이 -0.48~0.75까지 요동해 신호보다 잡음이 커짐. 상수 α를 곱하는 건 상관계수 자체를 안 바꾼다는 것도 확인함(스케일만 바뀌므로 — Pearson 상관·트리 분할 모두 불변).
  2. **분석 중 발견**: 그룹의 "최근접 LDAPS 격자 1개" vs "16격자 평균" 중 뭐가 나은지도 그룹마다 정반대였음(group_1은 평균이 압도적으로 나음 +0.11~0.12, group_2/3은 최근접이 나음 -0.02~-0.05). 연도별(2022/23/24)로 다 확인했고 Steiger's Z 검정(p<0.001)으로도 우연이 아님을 확인함.
  3. **2차 시도(채택, 민석님 제안)**: "허브고도를 공식으로 계산하지 말고, SCADA를 정답 삼아 여러 예보 풍속(그룹 최근접 LDAPS, 16격자평균 LDAPS, GFS 10/80/100m/850hPa)을 회귀로 결합해서 추정하면 안 되냐" → **2022~2023년만으로 회귀를 학습시키고 한 번도 안 보여준 2024년으로 검증**(out-of-sample, curtailment 구간 제외 반영):

    | | 기존 최고 단일 예보 피처 | 회귀 추정풍속 |
    |---|---|---|
    | group_1 | 0.793 | **0.854** |
    | group_2 | 0.814 | **0.855** |
    | group_3 | 0.827 | **0.869** |

    학습기간과 검증기간 성능 차이가 거의 없어(오히려 검증이 더 높음) 과적합 징후 없음(피처 6개뿐인 단순 선형회귀, n=17,000~26,000). **채택.**
  4. **"확실히 더 좋다"는 주장을 코드로 검증까지 함(민석님이 근거를 요구해서 추가)**: `03_features.ipynb` 2-1에서 최근접/16격자평균 차이를 연도별(2022/23/24)로 재확인 + Steiger's Z 검정(종속 상관계수 두 개 비교하는 통계 검정)으로 유의성 확인(3그룹 모두 p<0.001, |Z|>30). 3-2 뒤(3-2b)에서는 회귀추정풍속이 기존 최고 단일피처보다 유의하게 나은지도 같은 검정으로 확인(3그룹 모두 p≈0, |Z|>20). **이 두 검정 코드는 노트북에 실제로 들어있어서, 재실행하면 언제든 다시 확인 가능** — 대화로만 남기지 않고 재현 가능한 코드로 남겨둠.
  5. **남는 위험(중요, 잊지 말 것)**: 최종 피처는 "가진 데이터 전부"로 다시 학습한 회귀를 쓴다(`03_features.ipynb` 3-3). 이러면 train 쪽 값은 회귀가 정답을 이미 본 뒤 만든 값이라 약간 낙관적일 수 있다(스케일러를 전체 데이터로 fit하면 안 되는 것과 같은 종류). out-of-sample 검증에서 과적합 징후가 없어서 위험은 낮다고 판단했지만, **`04_model_selection`에서 A안/B안 검증 점수를 볼 때 이 피처가 여전히 비슷한 상관을 유지하는지 반드시 재확인**해야 한다. 떨어진다면 fold별로 회귀를 다시 적합하는 방식으로 바꿔야 한다.

- **curtailment 확정 구간(2023-02-13~17, 2024-01-18~24, 2024-02-22~03-01)을 학습 라벨에서 제외하기로 확정** (이전 세션엔 "미해결"이었음). **판단 근거(새로 발견)**: 대회 평가 산식은 실제 발전량이 설비용량 10% 이상인 시간대만 채점하는데, curtailment 구간은 발전량이 0이라 애초에 채점 대상이 아니다. 즉 test(2025)에 비슷한 정지가 또 있어도 그 시간을 못 맞히는 건 점수에 영향이 없다 — 그래서 "완전 제외"가 test에서 재현 못 할 정보를 쓰는 게 아니라 그냥 "학습에 안 쓰는 것"일 뿐이라 leakage 문제가 없다고 결론 내림. GFS/LDAPS 입력 컬럼 자체는 그대로 두고 라벨(kpx_group_*)만 NaN 처리함.
- 라벨 결측 처리 확정: `kpx_group_1/2`의 2022-10-24~27 나흘(최대 96시간) 구간은 보간 근거가 약해 그대로 NaN 유지(자연히 학습에서 dropna로 빠짐). 6시간 이내 짧은 결측은 선형보간(짧은 시간 지속성 가정).
- test_base LDAPS 결측 3시점은 ffill로 확정·구현 완료.
- **결빙(icing) 위험 피처 확정(group_1/2 전용)**: 실제 피처는 기온+회귀추정풍속(`ws_est`)만으로 계산(test에서도 가능). 발생비율 group_1 train 11.45%/test 9.24%, group_2 train 9.74%/test 7.88%. **검증은 반드시 SCADA 실측 풍속 구간으로 통제**해야 하며(아래 버그·주의사항 7번 참고), 그렇게 검증한 결과 3~7m/s 전 구간에서 결빙위험 시 발전량이 낮게 나와 EDA 6-3 결론과 일치함을 확인.
  - **"3~7 m/s"라는 조건 자체의 근거(민석님이 "왜 저렇게 했냐"고 지적해서 뒤늦게 채움)**: SCADA 풍속 구간별 영하/영상 발전량 감소율을 직접 계산함 — group_1: 2~3구간 28%, 3~4구간 36%, 4~5구간 31%, 5~6구간 22%, 6~7구간 10%, 7~8구간부터 6%→1%로 급락. group_2: 3~4구간 53%, ..., 6~7구간 24%, 7~8구간 16%, 8~9구간 11%, **9~10구간까지도 8.8% 남아있음**. 하한 3은 그 아래(2~3구간)는 감소율은 커도 발전량 절대값이 무시할 수준(22.6→16.2kWh)이고 3m/s가 컷인 근방이라서 잡음. 상한 7은 group_1 기준으로는 깔끔하게 꺾이지만 **group_2는 9~10m/s까지 잔여 효과가 남아있어서, 이 컷오프가 두 그룹에 완벽하게 맞는 건 아니고 "근사치"임을 그대로 인정**함 — 효과가 기대에 못 미치면 그룹별 상한 다르게 쓰거나 연속값 피처로 바꿀 후보.
- **GFS-LDAPS 앙상블 차이 피처 확정**: `{group}_gfs_ldaps_diff`(부호 있음, 평균 -2.26~-2.68 — GFS가 LDAPS보다 체계적으로 낮다는 02_eda 결론과 일치), `_absdiff`(절댓값, 불확실성 크기).
- **돌풍성(gust) 피처 확정**: LDAPS 50m u/v 최대-최소 범위를 벡터로 합친 `{group}_gust_proxy`. 발전량과 단독 상관은 약함(-0.02~-0.05)이 의도된 결과(난류 강도 지표라 발전량 수준 자체와는 약하게만 연관됨).
- **문서 가독성 보완(민석님 요청)**: 3-3 회귀계수 출력을 numpy 배열 print에서 pandas DataFrame 표(행=절편+6개 입력변수 역할명, 열=그룹)로 바꿈. 10절 제목·note에 "판단 기준(두 조건 AND)"과 "표 읽는 법(4~5m/s 구간 예시로 숫자까지 짚어줌)"을 명시적으로 추가함 — `reports/03_features.md`에도 동일 내용 반영. **이런 식의 쉬운 설명·표 형태는 원래 CLAUDE.md 6.2절에 있던 규칙인데 이번에 지키지 않아서 지적받음 — 앞으로 노트북에 결과표/회귀계수 등을 낼 때는 처음부터 DataFrame 표 + 예시 숫자로 짚어주는 형태로 만들 것.**

### 02_eda 최종본에서 이어받은 것 (요약, 원본은 `reports/02_eda.md`)
- 검증 전략: A안(2022~2023 학습→2024 검증) + B안(확장 윈도우 3-fold) 항상 병행
- `kpx_group_3`은 2022년 라벨 자체가 없음
- 격자 매핑: GFS 전 그룹 공통 g5, LDAPS는 group_1→g5, group_2→g6, group_3→g12
- 후류효과 관측됐으나 그룹 단위 모델링이라 추가 조치 없음(그룹평균 잡음으로 이해하고 넘어감)
- 예보 오차 하한: LDAPS-SCADA RMSE 2~3m/s — 모델 튜닝으로 못 줄이는 구조적 한계
- 풍향 피처는 그룹별로 설계(3개 그룹 다 WSW/W가 주풍향, group_3만 W가 크게 앞섬)

## 노트북 재사용 가능한 이름·규칙 — 다음 노트북 작성 시 참고
- `wind_speed(df, u, v)` = `sqrt(u**2+v**2)`, `wind_direction_deg(df, u, v)` = `(270-degrees(atan2(v,u)))%360` — `02_eda`/`03_features` 공통
- `GROUP_NEAREST_LDAPS = {"kpx_group_1": 5, "kpx_group_2": 6, "kpx_group_3": 12}`, `GFS_NEAREST_GRID = 5`
- `TURBINE_GROUP_MAP`: VESTAS 1~6→group_1, 7~12→group_2, UNISON 1~5→group_3
- SCADA 그룹 집계: 10분→1시간 **평균**(발전량과 달리 합계 아님), `resample("h", closed="right", label="right")`
- `steiger_z_dependent(r_a, r_b, r_ab, n)`: 종속적인 두 상관계수 차이의 유의성 검정(Steiger 1980) — `03_features.ipynb` 2-1에서 정의, 3-2b에서 재사용. `scipy.stats` 필요(venv에 설치됨, requirements.txt는 아직 없음 — 아래 환경 메모 참고)
- `03_features.ipynb` 컬럼 네이밍: `{group}_ws10_nearest`, `ldaps_ws10_avg16`, `gfs_g5_ws_{10m,80m,100m,850hPa}`, `scada_ws_{group}`(회귀 학습용, train 전용), **`{group}_ws_est`(회귀 추정풍속, 최종 피처)**, `{group}_ws_est_corrected`(밀도보정), `{group}_ws_est_sq/cube`, `{group}_wd_sin/cos`, `hour_sin/cos`, `month_sin/cos`, `{group}_gfs_ldaps_diff/absdiff`(9절), `{group}_icing_risk`(10절, group_1/2만), `{group}_gust_proxy`(11절)
- **`train_features_v1.parquet`에는 절대 모델 입력으로 쓰면 안 되는 컬럼이 섞여 있음**: `scada_ws_kpx_group_*`, `scada_kpx_group_*`(01_preprocessing 산출), `year`(3-2 검증용 임시) — test_features_v1.parquet에는 없으므로 `04_model_selection`에서 피처 목록 고를 때 반드시 제외

## 이번까지 발견된 버그·주의사항 (같은 실수 반복 방지용 — 꼭 읽을 것)
1. **연속-0 구간(zero-run) 탐지 로직 함정**: `run_id = (~is_zero).cumsum()`로 그룹을 나누면, 각 그룹은 "0이 아닌 값 1개 + 뒤따르는 0들"로 구성된다(맨 앞 그룹 제외). `is_zero.loc[grp.index].all()`로 "그룹 전체가 0인지" 검사하면 **거의 항상 False가 나와 아무것도 못 찾는다**. 올바른 방법: `zero_s = s[is_zero]; zero_run_id = run_id[is_zero]` 로 **0인 값만 먼저 걸러낸 뒤** groupby.
2. **노트북 파일이 커지면 `Read` 도구가 실패한다**: `02_eda.ipynb`가 122개 셀까지 커지자 `Read`가 "25000 토큰 초과"로 못 읽었고, `NotebookEdit`도 `Read` 선행이 필요해 같이 막혔다. **해결책**: 별도 `.py` 스크립트 파일을 `Write`로 만들어 `json.load`/`json.dump`로 노트북을 직접 조작한다(cell dict의 `id`/`cell_type`/`source`/`outputs`/`execution_count` 채우기, `source`는 `text.splitlines(keepends=True)`). `python -c "..."`로 bash에 직접 파이썬 코드(특히 백틱 포함 마크다운 텍스트)를 넣지 말 것 — bash가 백틱을 명령어 치환으로 오작동시켜 SyntaxError가 난다. **`03_features.ipynb`도 이 방식(json 스크립트)으로 만들고 계속 확장했다(43→48→58셀)** — 노트북이 작을 때는 `NotebookEdit`을 시도해봐도 되지만, 안 되면 바로 이 방식으로 전환할 것.
3. **`NotebookEdit`으로 여러 셀을 순서대로 삽입할 때**: `edit_mode="insert"`는 지정한 `cell_id` 바로 뒤에 넣는다. 여러 셀을 순서대로 넣으려면 **같은 anchor에 거꾸로(마지막 셀부터) 반복 삽입**한다. json 스크립트 방식에서는 `cells.insert(idx, ...)`로 원하는 위치에 바로 넣을 수 있어 더 편하다.
4. **`01_preprocessing.ipynb`에서 겪은 실수**: SCADA `_power_kw10m`을 "10분 평균출력"으로 가정해 평균 집계했더니 라벨과 상관이 거의 0 — 실제로는 "10분간 발전량(kWh)"이라 **합계**가 맞았다. 컬럼명을 그대로 믿지 말고 항상 라벨/실측과 대조 검증.
5. **시각화는 "대체"가 아니라 "추가"가 기본**: boxplot을 히스토그램으로 통째로 바꿨다가 "둘 다 하라고" 피드백 받음. 교체하라는 명시적 지시가 없으면 다른 관점의 그림은 별도 셀로 추가.
6. **AI가 물리 공식으로 만든 파생 피처는 실제로 검증하기 전엔 신뢰하지 말 것**: 시어지수 외삽처럼 도메인 근거가 그럴듯해 보여도, 실제 상관을 찍어보니 오히려 원래 피처보다 나빴다. **"물리적으로 맞는 방향"과 "지금 가진 데이터로 안정적으로 계산 가능한지"는 별개 문제** — 새 피처를 만들 때마다 반드시 raw 단일 피처 대비 상관(가능하면 out-of-sample)을 확인하고 나서 채택 여부를 결정한다. 민석님이 "회귀로 예측하면 안 되냐"고 되물어서 더 나은 방법을 찾았다 — 이런 되묻는 질문이 AI의 첫 시도가 최선이 아닐 수 있다는 걸 계속 상기시켜준다.
7. **추정치로 만든 피처를 "다른 추정치 구간"으로 검증하면 착시가 생긴다**: 10절 결빙 위험 피처를 검증할 때, 회귀추정풍속(`ws_est`) 구간으로 통제했더니 오히려 "결빙일 때 발전량이 더 높다"는 반대 결과가 나왔다(`ws_est`가 완벽하지 않아 진짜 풍속을 온전히 통제 못 했기 때문). **SCADA 실측 풍속 구간으로 다시 통제하니 정상적으로(결빙 시 발전량 낮음) 나왔다.** 교훈: 어떤 피처를 검증할 때 통제 변수로 쓰는 다른 피처가 그 자체로 오차가 있는 추정치라면, 가능한 한 실측값으로 통제해야 한다 — 6번 교훈과 같은 맥락(추정 피처는 계속 의심하고 검증할 것).
8. **구간·임계값(threshold)을 정할 때 "EDA에서 확인했다"고만 쓰고 실제 숫자를 안 보여주면 안 된다**: 결빙 위험 피처의 "3~7 m/s"라는 조건을 처음 만들 때, 왜 하필 3이고 왜 하필 7인지 실제 감소율 숫자를 안 보여주고 넘어갔다가 지적받았다. 나중에 계산해보니 하한(3)은 근거가 괜찮았지만 상한(7)은 group_2엔 정확히 안 맞는 근사치였다(9~10m/s까지 잔여 효과 있음). **모든 임계값·구간은 처음 정할 때부터 그 경계 근처 값들의 실제 수치(구간별 표)를 같이 적어서, 왜 그 경계인지 나중에 검증할 수 있게 해야 한다** — "대략 이 근처"라고 넘어가지 말고 숫자로 확인하고, 정확히 안 맞는 부분(이번엔 group_2 상한)이 있으면 그것도 숨기지 말고 그대로 적을 것.

## 실험 기록
| 날짜 | 실험 | 로컬 Score | 리더보드 | 결론 |
|---|---|---|---|---|
| (아직 없음 — 베이스라인 이전 단계) | | | | |

## 미해결 질문
1. **3절 SCADA 회귀 추정풍속의 fold-purity**: 지금은 전체 train으로 한 번만 학습해서 쓴다. `04_model_selection`의 A안/B안 검증에서 이 피처가 여전히 잘 작동하는지 반드시 재확인 — 떨어지면 fold별 재학습으로 바꿔야 함(위 "결정 사항" 5번 참고)
2. group_1/2의 2022-10-24~27 결측 나흘 구간, 지금은 그대로 NaN 유지(보간 안 함)로 확정했지만 실제 모델 성능에 영향 있는지는 아직 안 봄
3. group_3(UNISON)만 결빙 효과가 없는 이유(터빈 제작사 차이 추정)는 공식 스펙 문서 없이는 확정 불가 — 그래서 group_3엔 결빙 피처를 아예 안 만들었음
4. 후류효과의 정량적 반영 — 그룹 단위 모델링 구조상 "안 함"으로 결론냈으나, 모델 성능이 기대에 못 미치면 재검토 후보

## 환경 메모
- **scipy를 venv에 새로 설치함**(`03_features.ipynb` 2-1/3-2b의 Steiger's Z 검정에 실제로 씀). **이 프로젝트에는 아직 `requirements.txt`가 없음** — 재현성(2차 평가 요건)을 위해 조만간 만들어야 함(민석님 답변 대기 중, 위 "다음 할 일" 0번)
- `02_eda.ipynb`/`reports/02_eda.md`는 이미 커밋됨(`2b9ce0a`). **아직 커밋 안 된 것**: `notebooks/03_features.ipynb`(신규), `reports/03_features.md`(신규), `HANDOFF.md`(수정) — 이번 세션 종료 시 커밋 제안함
- **`03_features.ipynb`는 민석님이 직접 실행 완료함(에러 0건, AI 사전 검증 수치와 완전히 일치).** `data/processed/train_features_v1.parquet`/`test_features_v1.parquet`도 민석님 실행으로 최종 확정됨. `reports/03_features.md`의 수치도 검증 완료 상태.
- `02_eda.ipynb`는 파일이 커서(122셀) 도구로 편집할 때 위 "버그·주의사항 2번" 방식을 써야 한다.
