# HANDOFF — 마지막 갱신: 2026-08-04 (장소: 집 → 학교 이동, 세션 6회차 종료)

## 새 세션 시작 시 가장 먼저 할 것
1. 이 파일 전체를 읽는다 (특히 **"버그·주의사항"** — 같은 실수를 반복한 적이 여러 번 있다)
2. `git log --oneline -5`, `git status` 확인. **학교↔집 이동 시 push 여부 반드시 확인**
3. **`reports/*.md`와 이 문서가 어긋나면 원본 리포트를 우선한다.**
4. **작업 파일은 `notebooks/06_rebuild.ipynb`다.** `04`·`05`는 **동결**(실행 금지)
5. 3줄 요약 보고 후 아래 "🚀 다음 할 일" 1번부터 제안

---

# 🚀 현재 위치 (마감 2026-08-14 10:00 — **10일 남음**)

| | |
|---|---|
| **리더보드 최고** | **v6 = 0.6416** (1-NMAE 0.8687 / FICR 0.4145) — **08-04에도 안 깨짐** |
| 1등 | 0.67365 (1-NMAE 0.87964 / FICR 0.46767) — 격차 **-0.0321** |
| 순위 | **30등 밖** (민석님 확인) |
| 작업 파일 | `notebooks/06_rebuild.ipynb` (60셀) |
| 새 공통 모듈 | **`src/measure.py`**(측정·판정), **`src/placement.py`**(밴드 배치) |
| 오늘 제출 | v8(0.6398 ↓), v9(0.6370 ↓) — **둘 다 실패, 원인 규명 완료** |

## 세 줄 요약
**06_rebuild에서 파이프라인을 측정 계층부터 새로 세워 v6를 완벽 재현했고(블렌드 곡선 7점이 소수점 4자리까지 일치),
그 위에서 카드 15장 이상을 검증했는데 전부 "효과 없음"으로 닫혔다.
남은 것은 신경망 다양성 확대 · 밴드 집중 재가중 · 피처 재설계 세 가지뿐이다.**

---

# ⛔ 이번 세션에서 **닫힌** 카드 (재시도 금지)

| 카드 | 결과 | 근거 위치 |
|---|---|---|
| **위치·스케일 축 (양방향)** | **v6가 꼭짓점.** 2차 적합상 최적 k=1.009, 이득 +0.00015 | v8/v9 리더보드 |
| 고장·정비 보정 | 오차의 2~6%뿐 | 06 2-3 |
| **부분 성능저하 라벨 정제**(Codex 1순위) | 성능저하 행 빼도 σ **+1.1%**(오히려 증가) | 06 10층 |
| group_3 따라잡기 | 조건 맞춰도 1.868배 — UNISON 특성 | 06 3-2 |
| 데이터 더 모으기 | group_1 학습 절반으로 줄여도 **-6.5%(좋아짐)** | 06 3-2 |
| 2022년 빼기 | 그룹별 부호 반대(-6.5% / +4.2%) | 06 3-2 |
| 바람 공간정보 3축 | 상한 전부 -1% 미달 (최고 -0.91%) | 06 4층 |
| 행별 밴드 배치 | 미래 fold -0.0016 (fold1만 +0.0116) | 06 5-2 |
| MLP λ(ficr_weight) | 1.0~3.0에서 FICR 방향 없음(폭 0.008) | 06 6-2 |
| MLP 학습량 | 상한 근처도 못 감. 더 돌리면 과적합 | 06 6-3 |
| LightGBM 하이퍼파라미터 | **11개 설정 전부 기본값보다 나쁨** | 06 7층 |
| GBDT 패밀리 앙상블 | 오차 상관 ρ=0.982 → 이득 -0.79%, 순 악화 | 06 8층 |
| 풍속 손실가중(slope/pcrev) | **-0.0098 / -0.0095**, 4 fold 전부 하락 | 06 9층 |
| 표본가중(metric/eval_only) | -0.0009 / -0.0037 | 06 9층 |
| **블렌드·모양 축 전체** | **위치를 맞추면 전 후보가 v6보다 나쁨** | 06 16층 |

---

# ⭐ 이번 세션의 핵심 발견 3가지 (근거 포함 — 재검증 없이 신뢰)

## 1. σ를 줄이는 것과 FICR을 올리는 것은 **다른 일**이다

04의 `FICR ≈ σ^-0.86`, "오차 폭이 병목", "1등 되려면 σ 13% 감소" — **전제가 틀렸다.**

11층 실측: 블렌드로 σ가 3.4~5.6% 줄었는데 **band6는 오히려 떨어졌다.**

| | σ 변화 | band6 **기대**(정규분포) | band6 **실제** |
|---|---|---|---|
| group_1 | -4.3% | +0.0123 | **-0.0150** |
| group_2 | -3.4% | +0.0096 | -0.0012 |
| group_3 | -5.6% | +0.0145 | -0.0021 |

**이유**: 평균을 내면 꼬리(큰 오차)는 줄지만 봉우리(정확히 맞힌 경우)도 뭉개진다.
σ는 제곱이라 꼬리가 지배하고, FICR은 봉우리만 본다.
**⇒ 앞으로 σ가 줄었다는 이유로 채택하지 말 것. 반드시 band6·FICR로 판정한다.**

## 2. 위치·스케일 축은 **양방향 모두** 닫혔다 (이번에 처음 확인)

과거엔 **위쪽만**(τ 0.60/0.65/0.70, 확대 k>1) 시험해서 결론이 반쪽이었다.

| 제출 | 조작 | 1-NMAE | FICR | Total |
|---|---|---|---|---|
| v9 | k=0.96 (내림) | **0.87072** ↑ | **0.40326** ↓↓ | 0.63699 |
| **v6** | k=1.00 | 0.8687 | **0.4145** | **0.6416** |
| v8 | k=1.04 (올림) | 0.86576 ↓ | 0.41379 | 0.63978 |

- **NMAE는 내리는 쪽을 원한다**(k↓에서 +0.0020) = 우리는 조건부 중앙값보다 위에 있다
- **FICR은 k=1.0 근처가 꼭짓점**이고 아래로 훨씬 가파르다(-0.0112 vs -0.0007)
- 2차 적합: 총점 꼭짓점 **k=1.0087, 값 0.64175** (v6 대비 **+0.00015**) → **쓸 값어치 없음**

## 3. 로컬 검증은 "위로 미는 조작"만 과대평가한다

| 시도 | 로컬 | 리더보드 |
|---|---|---|
| v3 (τ=0.60, 편향 +0.051) | +0.0110 | **-0.0061** |
| exp019 (이전 프로젝트, 아핀 상향) | 개선 | **-0.0087** |
| v8 (확대 k=1.04) | +0.0024 | **-0.0018** |
| v9 (압축 k=0.96) | **-0.0094** | -0.0046 ← **방향은 맞혔다** |

**반전이 아니다.** 내리는 방향은 로컬이 맞혔고 **올리는 방향만 틀렸다.**

> **규칙: 평균 예측을 올리는 후보는 로컬 점수를 깎아서 읽는다.**
> **평균을 안 움직이는 후보는 로컬을 그대로 믿어도 된다.**

물리적 이유: 2025년은 예보 풍속이 3년 중 가장 센 해(LDAPS 10m 4.84→5.11, GFS850 8.04→8.75).
정격 근처 시간이 많은데 실제 발전량은 용량에서 막히고, FICR은 발전량 가중이라 그 구간이 점수를 지배한다.

**이 규칙의 활용법(16층에서 실제로 씀)**: 후보를 비교할 때 **채점행 평균 예측을 v6에 맞춰 평행이동**한 뒤 비교한다.
그러면 위치 효과가 제거되고 "모양"만 남으며, 그 숫자는 로컬을 그대로 믿어도 된다.
16층 실측 — 3등분 블렌드는 맞추기 전 **+0.0003**이었는데 맞춘 뒤 **-0.0027**로 뒤집혔다.

---

# 🚀 다음 할 일 (우선순위순)

## 0. 먼저 확인 (5분)
- `git status`로 push 여부 확인
- 커널을 새로 띄우면 06_rebuild를 **1-1부터 순서대로** 다시 돌려야 한다(캐시 재생성 ~20분)

## 1. ⭐ 신경망 다양성 확대 (학습 ~120회, 30~40분)

**왜 1순위인가**: 10층에서 오차 상관을 실측했는데 **구조가 다르면 상관이 확 낮았다.**

| 짝 | 오차 상관 |
|---|---|
| LightGBM ↔ XGBoost | **0.9819** (8층 실패 원인) |
| LightGBM ↔ 산식MLP | **0.9407** |
| LightGBM ↔ 직접모델 | **0.8848** |
| 산식MLP ↔ 직접모델 | **0.8485** |

**"구현을 바꾸는 건 소용없고 구조를 바꿔야 한다"가 숫자로 확인됐다.**
지금 신경망은 산식손실 MLP **한 종류**뿐이다. 손실·구조가 다른 신경망을 2~3개 더 만들면
상관이 더 낮은 멤버를 얻을 수 있다.

**구체적으로 만들 것** (`src/nn.py` 확장):
- (a) **L1 손실 MLP** — 13절에 있던 것(B평균 0.5879)을 지금 피처·라벨로 다시
- (b) **분위수 손실 MLP** (τ=0.5) — 트리와 같은 목적함수, 다른 함수족
- (c) **구조 변경** — 512-256-128 (지금은 256-256), 또는 잔차연결

**⚠️ 판정은 16층 방식으로**: 새 멤버를 섞은 뒤 **채점행 평균을 v6에 맞춰 평행이동**하고 비교한다.
맞추기 전 숫자로 판단하면 위치 효과에 속는다.

## 2. 밴드 집중 재가중 (학습 ~36~60회, 15~20분)

FICR은 밴드 밖 행에서 **어차피 0원**이다. 그런데 손실함수는 그 행들을 계속 줄이려 애쓴다.
**가망 없는 행의 가중을 낮추고 밴드 경계 근처 행에 용량을 몰아주면** band6가 오를 수 있다.

트리로 계단 손실을 직접 최적화하는 건 불가능하지만(이전 프로젝트가 CV 0.61→0.56으로 실패),
**반복 재가중은 가능하다.**

**구현**:
1. 기준 모델로 학습 → 학습 구간 잔차 계산
2. `|잔차|/용량 > 0.12`(밴드의 1.5배)인 행의 가중을 0.3배로
3. 재학습 → 2~3회 반복
4. `pl.gbdt_fold_fit_fn`에 `sample_weight` 주입 경로가 없으므로 **새 fit_fn을 노트북에 작성**

**⚠️ 주의**: 학습 구간 잔차로만 가중을 정한다. **검증 구간 라벨은 절대 안 본다.** 누수 아님.

## 3. 피처 재설계 (반나절)

지금 850개 중 **상위 15개가 gain의 44~65%**를 먹고 수백 개가 gain 0이다.

- gain 0인 피처를 걷어내고 다시 학습 (15-2의 "top50도 같다"는 **트리 얘기**다. 신경망엔 다를 수 있다)
- **아직 안 고친 한 줄짜리 두 개** (05 3-6에 적혀 있고 여전히 미적용):
  - `ws_est_corrected`(밀도보정)를 만들어놓고 **파워커브에는 원본 `ws`를 넣고 있다**
    (IEC 61400-12-1은 밀도보정 풍속을 파워커브에 넣으라고 한다)
  - `gust_proxy`가 분모(U)로 안 나눈 raw 변동폭이라 **난류강도(TI = σ/U)가 아니다.**
    `roll7_std / roll7_mean`이면 TI가 된다. TI는 파워커브 모양을 실제로 바꾼다

## 4. (비용 큼, 마지막) 작은 시공간 풍속 신경망 — Codex 4순위

4층에서 닫은 건 **스칼라 축 하나씩**이다. 여러 격자 × 시간의 **결합 효과**는 안 봤다.
GFS 3×3(44 km × 55 km)은 풍속 10 m/s에서 중심까지 1시간 넘게 걸려 물리적으로 의미가 있다.
다만 **9개 격자값이 이미 전부 평평한 피처로 들어가 있어** 기대치는 낮다.

---

# 📌 현재 파일 구조

```
src/
  metric.py       공식 산식 (수정 금지)
  submission.py   제출 생성·검증  ⚠️ 아래 버그 1번 필독
  nn.py           MetricMLP(산식손실) + WindMLP + 학습/추론
  pipeline.py     공통 정의 전부 (이번 세션에 wmode 인자 추가)
  measure.py      ★신규 — 측정·판정 계층 (Bench / compare / headroom / group_report)
  placement.py    ★신규 — 밴드 배치 (cdf_samples / best_placement) — 5층에서 기각됐으나 코드는 유효
notebooks/
  01~03           완료
  04, 05          ★동결 (실행 금지)
  06_rebuild.ipynb ★작업 중 (60셀) — 0층 측정 → 16층 모양비교
models/
  v5_parts.npz    v6의 test 예측 조각(lgb/mlp × 3그룹, 8760행). **제출 재조립에 필수**
submissions/      v1~v9
```

## `src/measure.py` 사용법 (다음 세션에서 그대로 쓸 것)

```python
import src.measure as ms
ms.assert_matches_official(actual_df, pred_df)      # 분해가 공식 metric()과 같은지
pg = ms.per_group_scores(actual_df, pred_df, row_mask=None)   # 그룹별 nmae/ficr/σ/편향/band6
bench = ms.Bench(); bench.add_from_cache(ctx, PRED, "이름")   # PRED 규약 그대로 흡수
bench.table()                                       # A안/B fold/B평균/Bσ/정제B평균/시드σ (정렬 안 함)
bench.compare("기준", "후보")                        # fold별 짝지은 차이 → 개선/동률/악화
ms.headroom(resid, axis)                            # 피처 만들기 **전** 상한 계산
ms.group_report(bench, "이름")                       # 그룹별 어디서 지는지
```

주요 상수: `ms.MIN_EFFECT=0.002`(실효크기 하한), `ms.K_NOISE=2.0`, `ms.CONTAMINATED`

**판정 규칙(코드에 박혀 있음)**: B안 3 fold **부호 일치** + `|평균Δ| ≥ max(2×노이즈, 0.002)`.
A안이 문턱 넘어 반대면 **"효과 불확실"**(CLAUDE.md 5장).

---

# 🏆 확정된 최종 구성 (v6 = 리더보드 0.6416, 여전히 최고)

```
1단계  풍속 2종(GBDT l2 결정적 · WindMLP) → 각각 LightGBM 발전량 예측 → 0.7 : 0.3
2단계  위 결과 0.5  +  산식손실 MetricMLP 0.5                        → 최종

  LightGBM 발전량 : objective=quantile, τ=0.50, 라벨 B_norm, 표본가중 actual/capacity(하한 0.1),
                    피처 상위 200, seed 5개
  산식손실 MLP    : 원본 라벨, 채점행만(이용률≥0.10), T_SOFT=0.006, full-batch,
                    256-256 GELU+BN+Dropout0.15, seed **(42, 7, 123, 2024, 31)**
```

---

# 📊 실험 기록 (리더보드 실측)

| 날짜 | 제출 | 구성 | 로컬 B평균 | **리더보드** | 1-NMAE | FICR |
|---|---|---|---|---|---|---|
| 08-01 | v1 | GBDT풍속 + τ0.60 + seed5 | 0.6298 | 0.6271 | 0.8592 | 0.3950 |
| 08-01 | v2 | + WindMLP 블렌드 | 0.6321 | 0.6288 | 0.8595 | 0.3981 |
| 08-02 | v3 | + `B_norm` τ=0.60 | 0.6366 | 0.6254 ↓ | 0.8520 | 0.3989 |
| 08-02 | v4 | `B_norm` τ=0.50 | 0.6356 | 0.6315 | 0.8620 | 0.4009 |
| 08-02 | v5 | + 산식손실 MLP 0.3 | 0.6377 | 0.6413 | 0.8671 | 0.4155 |
| 08-03 | **v6** | MLP 블렌드 0.5 | 0.6377 | **0.6416** ⭐ | 0.8687 | 0.4145 |
| 08-03 | v7 | MLP 블렌드 0.7 | 0.6355 | 0.6405 | 0.8688 | 0.4123 |
| **08-04** | **v8** | v6 + **확대 k=1.04** | – | **0.6398** ↓ | 0.86576 | 0.41379 |
| **08-04** | **v9** | v6 + **압축 k=0.96** | – | **0.6370** ↓ | 0.87072 | 0.40326 |
| | 1등 | | | **0.67365** | 0.87964 | 0.46767 |

---

# ⚠️ 버그·주의사항 (생략 금지)

## 1. ⛔ `src/submission.py` — 기본 인자는 **정의 시점에 박힌다** (이번에 실제로 터짐)

```python
def build_submission(pred_df, sample_path: Path = SAMPLE_SUBMISSION_PATH):
```

`sample_path`는 **기본 인자**라 함수 정의 순간에 값이 박힌다.
나중에 `sub.SAMPLE_SUBMISSION_PATH = 절대경로`로 바꿔도 **안 바뀐다.**
반면 `SUBMISSIONS_DIR`은 함수 **안에서** 읽으므로 덮어쓰기가 통한다 — 그래서 **하나만 통했다.**

**올바른 방법**: 호출할 때마다 `sample_path=`를 **직접 넘긴다.**
```python
SAMPLE = pl.REPO_ROOT / "data" / "sample_submission.csv"
sub.SUBMISSIONS_DIR = pl.REPO_ROOT / "submissions"
s = sub.build_submission(df, sample_path=SAMPLE)
sub.validate_submission(s, sample_path=SAMPLE)
sub.save_submission(s, fname, sample_path=SAMPLE)
```
(과거 HANDOFF의 "둘 다 덮어써라"는 **틀린 처방**이었다. 정정함.)

## 2. ⛔ `src/pipeline.py`를 고치면 **커널이 자동으로 안 읽는다** (이번에 실제로 터짐)

`TypeError: got an unexpected keyword argument 'wmode'` — 파일은 고쳤는데 커널이 옛 모듈을 들고 있었다.
셀 맨 위에 넣을 것:
```python
import importlib, inspect
importlib.reload(pl); importlib.reload(ms)
assert "wmode" in inspect.signature(pl.gbdt_fold_fit_fn).parameters, "재로딩 실패"
```
**`assert`를 반드시 붙인다.** 재로딩이 조용히 실패하면 옛 코드 결과를 새 카드 결과로 오독한다.

## 3. ⛔ 노트북 셀 간 **변수가 살아남아 조용히 잘못된 값을 쓴다** (이번에 실제로 터짐)

13층에서 `KeyError: np.float64(1.05)`. 원인: 후보가 비어 `k_fit` 재계산이 건너뛰어졌는데
**12층에서 쓰던 같은 이름의 `k_fit`(1.05)이 살아 있었다.**
→ 셀 시작에서 `for _v in ("k_fit","safe","K"): globals().pop(_v, None)`로 지운다.
→ **에러로 터져서 다행이었다. 안 터지고 틀린 답을 냈으면 못 잡았다.**

## 4. ⛔ AI가 `.ipynb`를 쓰면 **VS Code가 열어둔 버전이 덮어쓴다** (이번에 실제로 겪음)

셀 3개를 추가했는데 통째로 사라졌다.
→ AI가 노트북을 건드리기 전에 **"탭을 닫았다 다시 열어주세요"** 를 반드시 요청한다.
→ AI는 `Write`로 통째로 쓰지 말고 **JSON을 읽어 셀만 append**한다(기존 출력 보존).

## 5. ⛔ 메모리 — 피처 프레임 1개가 171 MB

`frame_for_fold`는 `(g, cv_suffix, pc_norm, speedup, wweight)`로 캐시한다.
`wweight`를 바꾸면 프레임 9개(1.5 GB)가 **새로 생긴다.** 변형이 끝나면 즉시 비울 것:
```python
for k in [k for k in list(ctx.cache) if k[0]=="frame" and k[-1]==wweight_tag]: del ctx.cache[k]
```

## 6. ⛔ `wind_estimate`의 캐시 키에 **`train_mask`가 없다**

`key = ("wind", g, cv_suffix, objective, speedup, wweight)`.
**다른 `train_mask`로 불러도 옛 결과를 조용히 돌려준다.** 학습 기간을 바꾸는 실험을 할 땐
`pl.fit_wind_gbdt`를 직접 부르거나 `cv_suffix`를 다르게 줄 것.

## 7. ⛔ `import src.nn as nn` 금지 — `torch.nn`과 충돌. 반드시 `mnn`

## 8. ⛔ 04를 전체 재실행하면 8~15시간. **얻는 게 없다.** 결론은 전부 기록돼 있다

## 9. ⛔ LightGBM 기본값은 완전 결정적 — 시드 앙상블하려면 `RAND_PARAMS`
`subsample_freq=1`을 빠뜨리면 `subsample`이 통째로 무시된다.

## 10. 사용자 피드백
- **`train.ipynb`/`inference.ipynb`는 2차 평가용.** 1차 리더보드를 통과해야 의미가 생기므로
  **점수 작업 중에는 우선순위로 올리지 말 것.** (여러 번 지적받음)
- 설명할 때 **AI만 아는 용어를 쓰지 말 것.** 처음 나오는 말은 한 줄 정의 + 비유를 붙인다
- 셀 실행은 민석님이 직접 한다. AI는 코드를 쓰고 결과를 받아 다음을 안내한다
- 제출 CSV를 덮어쓰는 것에 민감 — 덮어쓸 가능성이 있으면 반드시 먼저 알린다
- **계획이 여러 갈래로 섞이면 하나의 순서로 정리해 달라** (Codex 의견과 섞였을 때 지적받음)

---

# 🔍 미해결 질문 (트레이드오프까지)

1. **1등은 FICR 0.4677을 어떻게 만드나** — 우리 0.4145. NMAE 격차는 0.011뿐인데 FICR은 0.053이다.
   같은 오차 크기로 밴드를 훨씬 잘 맞힌다는 뜻이고, 그 방법을 우리는 아직 모른다.
   - 가설 A: 우리가 못 만든 피처가 있다 → 다음 할 일 3
   - 가설 B: 모델 구조가 다르다 → 다음 할 일 1
   - 가설 C: 데이터를 우리와 다르게 쓴다(외부 데이터는 규정 확인 필요)
2. **group_3을 아예 포기할 것인가** — 오라클로도 다른 둘의 1.87배다.
   라벨-SCADA 상관이 0.9966(다른 둘은 0.9998)이라 라벨 자체에 잡음이 있다.
   - 그대로 둔다: 지금처럼. 총점의 1/3을 계속 손해
   - group_3만 보수적으로 → **위치 축이 닫혔으므로 기대 낮음**
3. **06_rebuild의 캐시를 디스크에 저장할 것인가** — 커널 재시작 시 20분 재계산.
   `models/rebuild_pred.npz`로 `PRED`를 저장하면 다음 세션이 빨라진다. (아직 안 함)

---

# 🌱 환경 메모
- `requirements.txt` **아직 없음**. 필요: `lightgbm xgboost catboost scikit-learn pandas pyarrow scipy torch==2.13.0+cpu`
- torch 설치: `pip install torch --index-url https://download.pytorch.org/whl/cpu`
- 새 기기: clone → venv → 위 설치 → `data/` 복사 → `01_preprocessing.ipynb` 실행으로 캐시 재생성
- `models/v5_parts.npz`(0.3 MB)는 **git에 올려도 무방** — 작고, 제출 재조립에 필수
