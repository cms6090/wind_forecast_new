# HANDOFF — 마지막 갱신: 2026-08-03 01:40 (장소: 집, 세션 4회차 종료)

## 새 세션 시작 시 가장 먼저 할 것
1. 이 파일 전체를 읽는다 (특히 **"버그·주의사항"** 섹션 — 같은 실수를 세 번 반복한 적이 있다)
2. `git log --oneline -5`, `git status` 확인. **학교↔집 이동 시 push 여부 반드시 확인**
3. **`reports/*.md`와 이 문서가 어긋나면 원본 리포트를 우선한다.**
   단 04 노트북의 20·21절은 아직 리포트가 없고 **노트북 마크다운이 정본**이다
4. **작업 파일은 이제 `notebooks/05_pipeline.ipynb`다.** `04_model_selection.ipynb`는 **동결**
   (231셀 실험 원장. 실행하지 말 것 — 이유는 아래 "버그·주의사항 8번")
5. 3줄 요약 보고 후 아래 "🚀 다음 할 일" 1번부터 제안

---

# 🚀 다음 할 일 (2026-08-03~, 마감 2026-08-14 10:00 — **11일 남음**)

## 0. 현재 위치 한눈에

| | |
|---|---|
| **리더보드 최고** | **v6 = 0.6416** (1-NMAE 0.8687 / FICR 0.4145), MLP 블렌드 **w=0.5** |
| 1등 | **0.67365** (1-NMAE 0.87964 / FICR 0.46767) — 격차 **-0.0321** |
| 이전 프로젝트(`D:\공모전\wind-forecast_Competition`) 최고 | 0.63886 → **이미 넘었다** |
| 작업 파일 | `notebooks/05_pipeline.ipynb` (신규, 21셀) |
| 공통 모듈 | **`src/pipeline.py`(신규)**, `src/nn.py`(신규) |
| **바로 제출 가능** | **v6 (MLP 비중 0.5)** — `models/v5_parts.npz` 저장 완료, **학습 0회** |

---

## 1. ✅ 완료 (2026-08-03) — 블렌드 비중 확정, **이 카드는 닫는다**

| 제출 | MLP 비중 | Total | 1-NMAE | FICR |
|---|---|---|---|---|
| v5 | 0.3 | 0.6413 | 0.8671 | **0.4155** |
| **v6** | **0.5** | **0.6416** ⭐ 현재 최고 | 0.8687 | 0.4145 |
| v7 | 0.7 | 0.6405 | **0.8688** | 0.4123 |

**봉우리는 0.4~0.5. 곡선이 매우 완만해(0.6405~0.6416) 더 탐색할 값어치가 없다. `w=0.5`로 확정.**

### ⭐ 여기서 얻은 핵심 관찰 — 산식손실 MLP는 FICR이 아니라 **NMAE**를 기여한다
비중을 올릴수록 **1-NMAE는 0.8671→0.8687→0.8688로 오르다 포화**하고
**FICR은 0.4155→0.4145→0.4123로 단조 하락**한다.
**대회 산식을 손실로 쓴 모델인데 정작 FICR은 LightGBM보다 나쁘다**(로컬도 MLP 0.3877 vs LGB 0.4036).

**이유**: 우리 LightGBM은 **`B_norm` + τ=0.50이라는 FICR 전용 장치 두 개**를 이미 달고 있어
MLP가 가져갈 FICR 몫이 없었다. 대신 **매끄러운 함수라는 본래 강점(NMAE)** 으로 기여했다.

### → 여기서 나온 새 튜닝 손잡이 (05 4절에 추가할 것)
점수는 `0.5×(1-NMAE) + 0.5×FICR`인데 **1-NMAE는 0.869로 이미 1에 가깝고 FICR은 0.414로 멀다.**
**두 항의 남은 여지가 완전히 다르다.** 손실의 FICR 항에 가중치 λ를 준다:
```python
L = 0.5·nmae − 0.5·λ·ficr_soft        # λ ∈ {1(현행), 1.5, 2, 3}
```
λ를 키우면 **NMAE를 조금 내주고 밴드 안으로 들어가는 쪽**을 택한다.
지금 MLP가 정확히 반대 방향으로 가고 있으므로 되돌릴 손잡이가 필요하다. **12회 학습(2분).**
(`src/nn.py`의 `metric_loss`에 `ficr_weight` 인자를 추가하면 된다.)

## ~~1-old. 즉시 (5분) — v6 제출~~ (완료)

`models/v5_parts.npz`가 저장돼 있으므로 **학습 없이** 블렌드 비중만 바꿔 제출할 수 있다.

- **경로 A (권장)**: `05_pipeline.ipynb` 1절 셀1(셋업) → 3절 셀 실행 → `SUBS[0.5]` 저장
  - ⚠️ 3절은 2절의 `lgb_part`/`mlp_part`가 필요한데, **`v5_parts.npz`가 있으면 자동으로 로드**한다(2절 건너뛰기 가능)
- **경로 B**: 04의 21-7 셀 하단 주석 두 줄을 풀어 저장 (04를 다시 열어야 함)

**왜 0.5인가**: v5의 리더보드 오프셋이 **처음으로 양수(+0.0036)** 였다. 로컬이 MLP 기여를
**5배 과소평가**했다는 뜻이고(로컬 +0.0021 vs 실제 +0.0098), 최적 비중도 0.3보다 높을 가능성이 크다.
학습 구간이 가장 긴 A안은 **0.8**을 선호했다(아래 표).

| MLP 비중 | 0.0 | 0.3(v5) | 0.4 | 0.5 | 0.6 | 0.7 | **0.8** |
|---|---|---|---|---|---|---|---|
| **A안**(학습 2년) | 0.6418 | 0.6424 | 0.6428 | 0.6429 | 0.6432 | 0.6431 | **0.6438** |
| B평균(학습 1.5~3년) | 0.6356 | **0.6377** | 0.6373 | 0.6377 | 0.6372 | 0.6355 | 0.6344 |

**0.5 → (좋아지면) 0.7 → (좋아지면) 0.8 순으로 한 단계씩.** A안도 fold 하나라 그 봉우리가 노이즈일 수 있어 한 번에 안 뛴다.

파일명 제안: `submissions/20260803_v6_metricmlp05.csv`

---

## 🔧 `05_pipeline.ipynb`를 v6 기준으로 **재작성 완료** (2026-08-03 새벽, 33셀/코드 16개)

내일은 **위에서 아래로 실행하고 판단만** 하면 된다.

| 절 | 무엇 | 비용 | 언제 |
|---|---|---|---|
| **1** | 셋업 + **이식 검증** (`pipeline.py`가 04를 정확히 옮겼나) | 36회 / 5분 | **가장 먼저** |
| **2** | ⭐⭐ **풍속 잔차 진단** (2-1~2-5) | **0회** / 30분 | **본 작업** |
| **3** | 산식손실 MLP 튜닝 (3-1 λ → 3-2 학습량 → 3-3 피처 → 3-4 구조 → 3-5 정규화 → 3-6 최종) | ~200회 / 40분 | 값싼 개선 |
| **4** | 최종 파이프라인 + 제출 (4-1 parts / 4-2 제출) | 51회 or **0회** | 구성이 바뀌었을 때만 |
| 5 | 로드맵 | – | |

- **1절이 `❌ 불일치`면 거기서 멈추고 알릴 것.** `pipeline.py`와 05를 버리고 04로 돌아가야 한다
- **2절과 3절은 독립.** 시간이 없으면 **2절부터** (본 작업이고 학습이 0회다)
- 4-1은 `models/v5_parts.npz`가 있으면 **자동으로 건너뛴다.** MLP 설정을 바꿨을 때만 파일을 지우고 다시 실행

### 재작성하면서 고친 것
- 블렌드 비중 기본값 **0.3 → 0.5**(v6 확정), 제출 절이 v6와 대조하도록
- **폐기된 "A안 우선" 규칙 제거** → 전부 `B안 평균` 기준으로 통일
- 풍속 진단을 **2절로 승격**(원래 6절). MLP 튜닝보다 우선순위가 높다
- MLP 튜닝 순서를 **λ부터**로 재배치 (v5~v7 리더보드에서 갓 나온 가설이라 근거가 가장 신선)

### 새로 만든 손잡이
`src/nn.py`의 `metric_loss`에 **`ficr_weight`(λ)** 인자 추가. `pl.MLP_CFG["ficr_weight"]`로 조절
(기본 1.0 = 대회 산식 그대로). λ>1 = "NMAE를 조금 내주고 밴드 안으로".

### 스모크 테스트에서 미리 보인 신호
`speedup(실측 ÷ 10m예보)` 중앙값 **1.355**, 10~90% 분위 **0.82 ~ 2.30**.
**편차가 매우 크다** — 시어(대기 안정도)나 지형 효과가 실재할 가능성이 높다.

## 2. ⭐⭐ 내일 학교에서 — 풍속 잔차 진단 (학습 0회, 30분) ← **최우선**

### 왜 이게 최우선인가 — 숫자로

14절 오라클 실험이 이미 측정해둔 σ 분해:

```
σ_전체 0.166  =  풍속오차 0.146  ⊕  예측불가 0.079      (⊕ = 제곱합의 제곱근)
                    └─ 88% ─┘
```

"예측불가 0.079"는 **실측 나셀 풍속을 완벽히 알 때 남는 잔차**(`oracle_ws`).
즉 바람만 정확해지면 σ가 0.166 → 0.079까지 내려갈 여지가 있다.

FICR ≈ σ^-0.86 (오라클 실측 σ 0.079 / FICR 0.76으로 보정한 지수)로 환산하면:

| 풍속 오차 | σ_전체 | 1-NMAE | FICR | **Total** |
|---|---|---|---|---|
| 현재 | 0.166 | 0.8671 | 0.4155 | **0.6413** |
| **-10%** | 0.153 | 0.8775 | 0.446 | **0.662** |
| **-20%** | 0.141 | 0.8871 | 0.478 | **0.683** |
| -30% | 0.129 | 0.8967 | 0.515 | 0.706 |
| (오라클) | 0.079 | – | 0.76 | 0.86 |
| **1등** | | 0.87964 | 0.46767 | **0.67365** |

**풍속 RMSE를 현재 1.41~1.53 → 1.2~1.3 m/s로만 내리면 1등이다.**
반대로 풍속을 안 건드리는 개선(τ·블렌드·라벨 정제)은 전부 0.079 위에서 노는 것이라
상방이 막혀 있다 — 20절 +0.010, 21절 +0.010이 그 증거다.

### 정확히 무엇을 하나 (05_pipeline.ipynb에 **6절 신규 추가**, 기존 절은 건드리지 않음)

**"풍속 RMSE 1.5가 예보의 한계인가, 우리 모델의 한계인가"** 를 가르는 진단이다.
이걸 모르면 격자 CNN에 3일 쓰고 아무것도 못 얻을 수 있다.

현재 풍속 모델(`pl.wind_estimate`, GBDT l2)의 **검증 구간 잔차**(추정 − SCADA 실측)를
아래 축으로 쪼개 **조건부 편향이 남아 있는지** 본다. 남아 있으면 = 모델 개선 여지가 있다.
순수 백색잡음이면 예보 한계이고, 그때는 격자 CNN조차 소용없다.

1. **풍향 30도 구간별 잔차 평균·표준편차** (12구간)
2. **`scada_ws / 예보풍속` 비율을 풍향 구간별로** ← **지형 speed-up 곡선.** 아래 3번 참조
3. **시간대(0~23)별 · 계절별** 잔차 → 야간에 크게 틀리면 **대기 안정도 미반영**
4. **풍속 구간별**(0.5 m/s bin) 잔차 → 강풍에서 크면 외삽 문제
5. **잔차의 시간 자기상관**(lag 1~6h) → 크면 위상 오차
6. 그룹 간 잔차 상관 (같은 단지이므로 높을 것 — 공통 오차 성분의 크기)

**판정 규칙(사전 등록)**: 어떤 축에서든 **조건부 편향이 잔차 σ의 20% 이상**이면 그 축에 대응하는
피처를 만든다. 전부 10% 미만이면 예보 한계로 보고 격자 CNN(공간 구조)으로 넘어간다.

---

## 3. 진단 결과에 따라 — 풍속 개선 카드 (근거 순)

### 3-1. ⭐⭐ 지형 speed-up (풍향 의존) — 가장 저평가된 카드
**가덕산은 능선이다.** 능선 위에서는 기류가 압축돼 가속된다(Jackson–Hunt 이론).
그리고 **speed-up 계수는 풍향에 따라 크게 다르다** — 능선에 직각이면 크게 가속, 능선을 따라 불면 거의 없음.
우리는 수 km 해상도 예보 격자를 쓰는데 **그 해상도로는 능선 지형이 표현되지 않는다.**
즉 예보 풍속과 실제 허브고도 풍속 사이에 **풍향에 따라 달라지는 체계적 배율**이 존재한다.

- **진단이 공짜**: `scada_ws / 예보풍속`을 풍향 30도 구간별로 그리면 끝(위 2절 2번)
- 곡선이 평평 → 지형 효과가 없거나 이미 학습됨 / 1.1~1.5로 출렁 → **명시적 피처로 만들 값어치 확실**
- ⚠️ **04의 교훈 준수**: 구간 더미가 아니라 **연속적인 speed-up 배율**로 넣는다
  ("도메인 지식은 어떤 변수를 만들지에 쓰고, 어떻게 이산화할지는 모델에 맡겨라")

### 3-2. ⭐ 파워커브 기울기 가중 손실 — 비용 몇 줄
지금 풍속 모델은 `l2`로 **모든 풍속 구간을 동등하게** 학습한다. 그런데 발전량은 v³로 반응한다.
- 6 m/s에서 0.5 m/s 오차 → 발전량 오차 큼(파워커브 급경사)
- 16 m/s에서 0.5 m/s 오차 → **발전량 오차 거의 0**(정격 평탄부)

**즉 상관없는 구간의 오차를 줄이느라 중요한 구간을 희생하고 있다.**
해결: 풍속 모델 학습에 `sample_weight ∝ |dP/dv|`(파워커브 기울기).

> ⚠️ 18-1의 "발전량 환산 오차 지표" 실패와 헷갈리지 말 것. 그건 **모델 선택 지표**로 썼다가
> 실패한 것이고 이건 **학습 손실**이다. 오히려 16절 교훈("l1이 풍속은 정확한데 l2가 발전량 점수는
> 높다")이 이미 이 방향을 가리키고 있었다.

### 3-3. 대기 안정도 지표 — 연직 시어를 직접 결정
10m 예보에서 117m를 추정하는 문제의 **물리적 핵심**. 멱법칙 `v(z)=v(10)·(z/10)^α`에서
- **안정(야간 복사냉각·역전층)**: α ≈ 0.3~0.4 → 117m가 10m의 **1.9배**
- **불안정(주간 대류)**: α ≈ 0.1 → **1.3배**

**같은 10m 풍속인데 허브고도가 45% 차이난다.** `blh`는 이미 있으나 안정도 지표는 없다.
**벌크 리처드슨 수 근사**: `Ri_b ≈ (g/θ)·(Δθ·Δz)/(ΔU)²` — LDAPS 2m 기온·상층 기온·풍속 차로 계산.
(상층 기온이 어느 높이에 있는지 먼저 확인할 것.)

### 3-4. 후류(wake)
group_1/2는 **같은 VESTAS 12대를 6대씩 나눈 것**이다. 주풍향(WSW/W)일 때 상·하류 관계가 정해진다.
나셀 풍속 자체가 후류 영향을 받으므로 **풍향별 group_1↔group_2 풍속 비율**이 달라질 것. 진단이 쉽다.

### 3-5. 격자 CNN — 유일하게 남은 구조적 카드
850개 평평한 피처는 **바람이 어느 방향에서 다가오는지(이류)** 를 표현하지 못한다.
비용이 크므로 **위 진단 결과를 보고** 착수한다.

### 3-6. (참고) TI = σ/U 정규화, 파워커브 입력 밀도보정
- 현재 `gust_proxy`는 분모(U)로 안 나눈 raw 변동폭이라 gain이 낮았다(0.037~0.231%).
  13절의 `roll7_std`를 `roll7_mean`으로 나누면 바로 TI가 된다
- IEC 61400-12-1은 **밀도보정한 풍속을 파워커브에 넣으라**고 한다. 지금 `ws_est_corrected`를
  만들어놓고 정작 파워커브에는 원본 `ws`를 넣고 있다. **코드 한 줄** 수정

---

## 4. 산식손실 MLP 튜닝 — **코드는 이미 05 4절에 작성돼 있음, 미실행**

여태 **`T_SOFT`만 스윕**했고 나머지는 전부 **이전 프로젝트가 179피처로 정한 값**을 그대로 쓴다.
MLP가 v5에서 리더보드 +0.0098을 만들었으므로 이제 주력 모델이고, **1회 학습 10초**라 비용도 싸다.

05 4절이 **좌표별(one-factor-at-a-time)** 탐색으로 구성돼 있다(격자 아님 — 다중비교 방어).

| 소절 | 바꾸는 것 | 비용 |
|---|---|---|
| 4-1 | **학습량** (`patience` 60→120, `max_epochs` 400→800) ← **가장 의심스럽다** | 36회 ≈ 7분 |
| 4-2 | **피처 세트** (200 / 400 / 전체 850) | 36회 ≈ 10분 |
| 4-3 | **구조** (256×2 / 512×2 / 512-256-128 / **128×2**) | 48회 ≈ 10분 |
| 4-4 | **정규화** (dropout, weight decay) | 48회 ≈ 10분 |
| 4-5 | 최종 후보 시드 5개 + 블렌드 곡선 | 120회 ≈ 20분 |

**4-1이 가장 의심스러운 이유**: full-batch라 **1에폭 = 기울기 1스텝**인데 시험 학습이 **61에폭**에서
최적을 찍었다. 파라미터 12만 개짜리 모델이 **기울기 갱신 61번**으로 학습을 끝낸 셈이다.

**4-2의 근거**: 지금 MLP는 **LightGBM 중요도로 고른 200개**를 쓴다. 트리는 상관 높은 것 중
하나만 있으면 되지만 **신경망은 약한 신호가 많이 모이면 도움이 된다.** 트리에게 gain 0이던
피처가 신경망에는 쓸모 있을 수 있다. (15-2의 "top50까지 줄여도 같다"는 **트리 얘기**다.)

**⚠️ 판정 지표는 `A안`** (신경망 계열 규칙 — 아래 "결정 사항 3번"). 단 B평균이 -0.005 이상
나빠지면 기각. 시드 간 σ가 0.0024이므로 **A안 +0.005 미만은 노이즈로 본다.**

---

## 5. ‼️ `train.ipynb` / `inference.ipynb` — **데드라인 2026-08-10 (마감 4일 전)**

2차 평가 **필수 요건**이다. 성능이 아무리 좋아도 이게 없으면 무의미하다.
**`src/pipeline.py`가 생겨서 두 노트북은 얇은 껍데기면 된다** —
05 2절의 `build_final_parts()`를 학습·저장 쪽과 로드·예측 쪽으로 쪼개는 작업이다(반나절).

- `train.ipynb`: 전체 train으로 풍속 2종 + LightGBM(seed 5) + 산식손실 MLP(seed 5) 학습 →
  `models/final/`에 저장 + `config.json`
- `inference.ipynb`: 저장 모델 로드 → test 예보 적용 → 2단 블렌드 → `submissions/` CSV
- `requirements.txt`도 이때 작성 (**아직 없음**. `torch==2.13.0+cpu` 포함)

---

# 📌 현재 파일 구조

```
src/
  metric.py       공식 산식 (수정 금지)
  submission.py   제출 생성·검증
  nn.py           ★신규 — MetricMLP(산식손실) + WindMLP(풍속) + 학습/추론 함수
  pipeline.py     ★신규 — 공통 정의 전부 (데이터·fold·피처·풍속·라벨·LightGBM·채점)
notebooks/
  01~03           완료
  04_model_selection.ipynb  ★동결 (231셀, 1~21절 실험 원장. 실행 금지)
  05_pipeline.ipynb         ★작업 중 (21셀). 1절 이식검증 / 2절 최종파이프라인 / 3절 블렌드 / 4절 MLP튜닝
models/
  v5_parts.npz    ★v5의 lgb_part·mlp_part. 블렌드 실험을 학습 0회로 만든다
submissions/      v1~v5
```

## `src/pipeline.py` 사용법 (다음 세션에서 그대로 쓸 것)

```python
import sys; sys.path.append("..")
import src.pipeline as pl
import src.nn as mnn                      # ⚠️ torch.nn과 이름 충돌 — 반드시 mnn으로

ctx = pl.load_context()                   # train/test + 컬럼분류 + fold + 가동률 한 번에
X   = pl.frame_for_fold(ctx, g, "2024_01")          # 피처 프레임 (캐시됨)
keep= pl.keep_for_fold(ctx, g, "2024_01", mask)     # 상위 200개 (캐시됨)
m   = pl.lgbm_train_label(ctx, X[keep], g, mask)    # B_norm τ=0.50 기본값
d   = pl.run_variant(ctx, PRED_CACHE, "이름", pl.lgbm_fold_fit_fn(...))
pl.summarize([d])
pl.metric_mlp_fit_fn(cfg, seeds=(42,7,...))         # 산식손실 MLP
pl.blend_score(ctx, PRED_CACHE, "varA", "varB", w)  # 재학습 없는 블렌드
```

주요 상수: `pl.BEST_TOPN=200`, `pl.DEFAULT_TAU=0.50`, `pl.DEFAULT_LABEL_MODE="B_norm"`,
`pl.MLP_CFG`(산식손실 MLP 기본 설정), `pl.AVAIL_FLOOR=0.2`, `pl.B_FOLDS`

---

# 🏆 확정된 최종 구성 (v5 = 리더보드 0.6413)

```
1단계  풍속 2종(GBDT l2 결정적 · WindMLP) → 각각 LightGBM 발전량 예측 → 0.7 : 0.3
2단계  위 결과 (1-w)  +  산식손실 MetricMLP  w(=0.3)                  → 최종

  LightGBM 발전량 : objective=quantile, τ=0.50, 라벨 B_norm(가동률 정규화),
                    표본가중 actual/capacity(하한 0.1), 피처 상위 200, seed 5개(RAND_PARAMS)
  산식손실 MLP    : 원본 라벨, 채점행만(이용률≥0.10), T_SOFT=0.006, full-batch,
                    256-256 GELU+BN+Dropout0.15, sigmoid 출력, 조기종료=대회산식, seed 5개
  풍속 GBDT       : objective=l2, n_estimators=3000, 무작위성 없음(결정적), 입력 850개
```

---

# 📊 실험 기록 (리더보드 실측)

| 날짜 | 제출 | 구성 | 로컬 B평균 | **리더보드** | 1-NMAE | FICR | 오프셋 |
|---|---|---|---|---|---|---|---|
| 08-01 | v1 | GBDT풍속 + τ0.60 + seed5 | 0.6298 | 0.6271 | 0.8592 | 0.3950 | -0.0027 |
| 08-01 | v2 | + WindMLP 블렌드 | 0.6321 | 0.6288 | 0.8595 | 0.3981 | -0.0033 |
| 08-02 | v3 | + `B_norm` τ=0.60 | 0.6366 | **0.6254** ↓ | 0.8520 | 0.3989 | **-0.0112** ⚠️ |
| 08-02 | v4 | `B_norm` **τ=0.50** | 0.6356 | 0.6315 | 0.8620 | 0.4009 | -0.0041 |
| 08-02 | **v5** | **+ 산식손실 MLP 0.3** | 0.6377 | **0.6413** ↑ | 0.8671 | 0.4155 | +0.0036 |
| 08-03 | **v6** | MLP 블렌드 **0.5** | 0.6377 | **0.6416** ⭐ | 0.8687 | 0.4145 | +0.0039 |
| 08-03 | v7 | MLP 블렌드 0.7 | 0.6355 | 0.6405 | **0.8688** | 0.4123 | +0.0050 |
| | 1등 | | | **0.67365** | 0.87964 | 0.46767 | |

**v5~v7 세 점으로 확인된 것**: 비중을 올리면 **1-NMAE는 오르다 포화, FICR은 단조 하락.**
총점 봉우리는 0.4~0.5. 그리고 **B평균 순위(0.3≈0.5 > 0.7)가 리더보드 순위와 일치**했다(A안은 불일치).

---

# ✅ 확정된 결론 (근거 포함 — 재검증 없이 신뢰해도 됨)

## 1. ⭐⭐ 위치 최적화는 끝났다 — **리더보드로 확인됨**
13절에서 로컬로 내린 결론("FICR은 위치가 아니라 폭(σ)의 문제")이 v3 실패로 **리더보드에서도 확인**됐다.
편향을 +0.023 밀었는데 **FICR이 0.0008밖에 안 움직이고 1-NMAE만 0.0075 잃었다.**
FICR이 상향에 반응하지 않는다는 것은 **v2가 이미 2025년의 FICR 최적 위치 근처**였다는 뜻.
**⇒ 남은 제출 기회를 τ·편향·후처리에 더 쓰지 말 것.**

## 2. ⭐ 로컬 편향 +0.03 규칙 (정량화됨)
> **로컬 편향이 +0.03을 넘는 구성은 로컬 점수를 믿지 말 것. 2025년은 상향 편향의 여유가 없다.**

v3(편향 +0.051)만 오프셋이 -0.0112로 깨졌고 v4(+0.024)·v5는 정상. 2025년은 예보 풍속이
3년치 어느 해보다 강한 해(`ldaps_ws10_avg16` 5.107 vs 4.74~4.98)라 예측이 애초에 높은 쪽에 있다.
**이전 프로젝트의 exp019(아핀 후처리, 위로 미는 보정)도 같은 이유로 Public -0.0087로 실패했다 — 독립 2회 확인.**

## 3. ⚠️ 검증 지표는 **`B안 평균`** 이다 — "신경망은 A안" 규칙은 **폐기됨(2026-08-03)**

한때 "신경망 계열은 A안을 주 지표로" 라는 규칙을 세웠다가 **v6/v7 리더보드로 반증됐다.**

| MLP 블렌드 비중 | 0.3 | 0.5 | 0.7 | 0.8 |
|---|---|---|---|---|
| A안 | 0.6424 | 0.6429 | 0.6431 | **0.6438** ← 0.8이 최고라고 했다 |
| **B평균** | **0.6377** | **0.6377** | 0.6355 | 0.6344 ← 0.3·0.5 공동 최고 |
| **리더보드(실측)** | 0.6413 | **0.6416** | 0.6405 | – |

**B평균이 맞았고 A안이 틀렸다.**

**왜 규칙을 잘못 세웠나 (반복 방지)**: v5의 리더보드 오프셋이 +0.0036으로 처음 양수가 나오자
"로컬이 MLP를 과소평가하니 비중을 더 올려야 한다"고 해석했다. 그런데 그건
**모델 전체의 오프셋(level)** 이지 **비중 곡선의 기울기(slope)** 가 아니다. 둘을 혼동했다.
> **교훈: 오프셋이 바뀌었다는 사실로부터 "어느 방향으로 더 가야 한다"를 유도하면 안 된다.**
> 오프셋은 절편이지 기울기가 아니다.

**⇒ 모든 판정의 주 지표는 `B안 평균`.** A안은 방향 일치 확인용 보조 지표로만 쓴다(CLAUDE.md 5장 원칙 그대로).

## 4. 라벨 정제(`B_norm`) — 채택. 단 τ와 함께 조정해야 한다
- 20-4: `B_norm`(y ÷ max(가동률,0.2)) B평균 0.6298 → **0.6366**. `C_dropdown`·`B_norm_pc`와
  0.001 이내라 **가장 단순하고 데이터를 안 버리는 것**을 골랐다. `D_weight`는 +0.0012로 기각
  (행의 84%가 `avail=1.0`이라 가중이 거의 안 바뀌는 미지근한 절충안이었다)
- **⭐ 이득이 "편향 밀어내기"가 아님이 증명됐다**: `B_norm` τ=0.50은 편향 **+0.0241**
  (기준선 +0.0283보다 **낮다**)인데 B평균 0.6356(+0.0058). **같은 편향에서 더 높은 점수**
- **σ는 못 줄였다**(0.1684 → 0.1697). 20절은 σ 카드가 아니라 FICR 카드였다

## 5. 산식손실 MLP — 채택. 단 기대의 1/8이었고 그 이유도 규명됨
- 이전 프로젝트는 이걸로 Public **+0.0160**을 얻었는데 우리는 **+0.002**(로컬)였다
- **이유**: 그쪽 +0.016의 대부분은 "τ=0.70으로 망가진 FICR을 MLP가 되돌린 것"인데,
  우리는 20절에서 `B_norm`+τ=0.50으로 **그 구멍을 이미 메웠다**(우리 LGB FICR 0.4036 > 그쪽 0.3979)
- **교훈**: 밖에서 가져온 개선폭은 **"그쪽 기준선의 약점"이 우리에게도 있는지** 먼저 확인해야 한다
- 다만 **리더보드에서는 +0.0098**이 나왔다(결론 3번 참조)
- `T_SOFT` 스윕 결과 **0.006이 내부 꼭짓점**(0.004: 0.6300 / **0.006: 0.6307** / 0.010: 0.6276 /
  0.020: 0.6274) — 이전 프로젝트 선택값과 일치, 밴드 항이 실제로 작동한다는 증거
- 손실 교체 효과 자체는 확실: 13절 MLP(L1) B평균 **0.5879** → 산식손실 **0.6307**

## 6. 그룹별 파라미터 — **네 번 연속 기각. 이 카드는 닫는다**
15-3(그룹별 τ +0.0008) · 17-3 · 20-5(+0.0009) · 21-5(그룹별 블렌드 가중 +0.0011).
이질성 자체는 실재하나(21-5에서 group_1은 0.0, group_3은 0.7 선호) 크기가 노이즈 수준이다.

## 7. ⚠️ FICR은 편향이 아니라 "밴드 안 확률질량"에 반응한다
20-5에서 "group_2는 편향이 +0.060으로 과하니 τ를 내려야 한다"고 예측했으나 **틀렸다**(최적은 0.60).
group_2는 편향 +0.060에서도 ≤6% 비중이 최대였다.
**편향을 FICR의 대리지표로 읽으면 안 된다.**

## 8. 2022년 데이터가 이상하다 (20-2 발견, 미활용)
group_1의 '1대 이상 정지' 비율이 **2022-08 0.538 / 2022-12 0.616**인데 2023~24는 0.02~0.17.
최장 정지 구간 Top 10 중 **8개가 2022년**(최대 137시간). **시운전 조정 또는 초기 대규모 정비 기간**으로 보인다.
→ CLAUDE.md 2.3절 "연도별 drift"가 **기상이 아니라 운영 쪽에서** 실재한다는 첫 직접 증거.
**아직 아무 조치도 안 했다** — 학습 구간에서 제외하거나 가중을 낮추는 실험이 남아 있다.

## 9. 2024년 12월 group_3 = 정지율 0.815 (B fold3 오염)
12월의 81.5%가 1대 이상 정지. **B안 fold3 검증 구간(2024-07~12)에 직접 들어간다.**
**⇒ fold3 점수는 앞으로도 신뢰도를 낮춰 읽을 것.**

## 10. 단지 전체 동시 정지 구간 (제조사가 다른데 겹침 = 개별 고장 아님)
`2024-02-20~03-01`(g2 52h + g3 44h), `2022-03-17~19`(g1 61h + g2 63h), `2023-12-16`(g1 60h + g2 45h).
curtailment 신호는 "약하게 있음": 심야/주간 비율 1.2~1.3배(판정 문턱 1.5배 미달),
지속시간 중앙값 2~4시간(짧음), 전대수 동시 정지 0.29~0.49%.

## 11. 이전 프로젝트에서 가져온 자산 (`D:\공모전\wind-forecast_Competition`)
- **산식손실 MLP** → `src/nn.py`로 이식 완료
- **exp019(아핀 후처리) 실패 기록** → 우리 v3 실패와 같은 교훈, 결론 2번의 독립 증거
- `reports/phase6_nn_metric_loss.md`가 그쪽 정본. `experiments/log.csv`에 exp001~019 전부 기록돼 있음
- ⚠️ **LightGBM 커스텀 목적함수로 산식 최적화는 불가능**하다(그쪽이 시도했다 CV 0.61→0.56).
  부스팅은 잎 값을 `-Σg/Σh`로 정하는데 비볼록 밴드 보너스의 기울기를 처리 못 한다.
  내장 `l1`의 `RenewTreeOutput`(잎 값을 잔차 중앙값으로 재계산) 보정이 커스텀에는 없다.
  **경사하강법만 된다.**

---

# ⚠️ 이번까지 발견된 버그·주의사항 (생략 금지)

## 1. ⛔ 04 노트북에는 이미 제출한 CSV를 덮어쓰는 셀이 5개 있다
`save_submission` 주석이 풀린 셀: v1/v2/v3/v4/v5. **04를 처음부터 다시 돌리면 제출 기록이 사라진다.**
특히 v2·v3·v4·v5는 torch(MLP)를 포함해 **재현이 비트 단위로 보장되지 않는다.**
→ 04를 다시 돌릴 일이 있으면 **`submissions/` 폴더를 먼저 백업**할 것.

## 2. ⛔ AI가 노트북 파일을 덮어쓰면 저장 안 된 출력이 날아간다
VS Code에서 노트북을 연 채로 AI가 `.ipynb`를 다시 쓰면, **저장되지 않은 셀 출력이 리로드되며 사라진다.**
실제로 21-6 셀의 출력을 한 번 날렸다. → **셀을 추가·수정하기 전에 반드시 Ctrl+S 요청할 것.**

## 3. ⛔ `-metric_loss`는 score가 아니라 **score − 0.5** 다
`score = 0.5·(1−nmae) + 0.5·ficr = **0.5** − 0.5·nmae + 0.5·ficr` 인데 손실에는 상수 0.5가 없다.
학습에는 무해(상수는 기울기에 영향 없음)하지만 **점수와 비교할 때는 `mnn.soft_group_score()`** 를 쓸 것.

## 4. ⛔ 커널 재시작 = 51회 학습 증발 (실제로 겪음)
`lgb_part`/`mlp_part`를 저장하기 전에 커널이 초기화돼 전부 다시 돌려야 했다.
→ **이제 `models/v5_parts.npz`에 저장돼 있다.** 앞으로도 **비싼 중간 산출물은 즉시 디스크에 저장**할 것.

## 5. ⛔ `src/submission.py`의 CWD 함정
`SAMPLE_SUBMISSION_PATH`/`SUBMISSIONS_DIR`이 상대경로라 `notebooks/`에서 돌리면 잘못 해석된다.
**둘 다** 덮어써야 한다 — `SUBMISSIONS_DIR`을 빠뜨리면 **에러 없이** `notebooks/submissions/`에 조용히 저장된다.
`src/pipeline.py`의 `pl.REPO_ROOT`를 쓰면 안전하다.

## 6. ⛔ `import src.nn as nn` 금지 — `torch.nn`과 충돌
반드시 **`import src.nn as mnn`**.

## 7. ⛔ 메모리 — 피처 프레임 1개가 171 MB다
(26304 × 850, float64). 20-4의 `FRAME20` 캐시는 최대 18개 = **3.0 GB**.
`train` 원본만 178 MB. 긴 실행 전에 **다른 앱을 닫고 커널을 새로 시작**할 것.

## 8. ⛔ 04를 전체 재실행하면 8~15시간 (CatBoost 포함)
학습 1000회 이상(9절 84 + 15절 180 + 16~18절 250 + 20~21절 250 …).
**얻는 게 없다 — 결론은 전부 기록돼 있다.** 재현성을 확인하고 싶으면 **05 1절**(이식 검증, 36회 5분)이
같은 일을 더 강하게 한다(모듈로 다시 짠 코드가 같은 답을 내므로).

## 9. ⛔ 03절 파워커브 누수 (해결 완료, 재발 방지 기록)
`leaky_cols_for_group()`에 `f"{g}_power_curve_est"`(원본)가 빠져 9~12절이 누수 상태로 실행됐다.
**이름을 열거하는 방식은 언젠가 반드시 하나를 빠뜨린다.**
→ `assert_no_leak()`의 **규칙 기반** 점검으로 대체(`pl.assert_no_leak`).
컬럼 이름에 `ws_est`/`power_curve_est` 표식이 있으면 `__` 앞 뿌리를 구해 그 fold의 허용 2개가
아니면 무조건 실패. 측정된 누수 크기는 A안 -0.0050 / B평균 -0.0045로 작았고 **상대 비교 결론은 유효**했다.

## 10. ⛔ 풍속은 평균 내지 말 것 (Jensen 부등식)
발전량은 풍속에 볼록(~v³)하게 반응하므로 여러 풍속 추정을 평균하면 극값이 눌려 **강풍 발전량을
체계적으로 과소평가**한다. 17절에서 1단계 상관 1위(`avgall` 0.9146)가 2단계 점수 꼴찌였다.
**앙상블은 파이프라인 마지막 단(발전량 예측)에서만.**

## 11. ⛔ LightGBM 기본값은 완전 결정적이다
`subsample=1.0`, `colsample_bytree=1.0`, `subsample_freq=0`이라 seed를 바꿔도 같은 트리가 나온다.
시드 앙상블을 하려면 `RAND_PARAMS = dict(subsample=0.8, subsample_freq=1, colsample_bytree=0.8)`.
**`subsample_freq=1`을 빠뜨리면 `subsample`이 통째로 무시된다.**

## 12. ⛔ 후버 손실의 `alpha`는 타깃 스케일이다
9절에서 단조제약 실험이 폭락(0.4177)한 진짜 범인은 제약이 아니라 **`alpha` 기본값 0.9**였다.
타깃이 0~21,600 kWh인데 alpha=0.9면 그래디언트가 사실상 1/1000로 눌린다.
**단조 제약 자체는 무죄 — 판정 종료(채택 안 함, 효과 불확실).**

## 13. 사용자 피드백
- 노트북이 229셀까지 길어져 **"보기 힘들다"** → `src/pipeline.py` 분리 + `05_pipeline.ipynb` 신설
- **셀 실행은 민석님이 직접 한다.** AI는 코드를 쓰고 결과를 받아 다음을 안내한다
- 제출 CSV를 덮어쓰는 것에 민감 — **덮어쓸 가능성이 있으면 반드시 먼저 알릴 것**

---

# 🔍 미해결 질문 (트레이드오프까지)

1. **풍속 RMSE 1.5는 예보 한계인가 모델 한계인가** ← 위 "다음 할 일 2번"이 이 질문에 답한다.
   모델 한계면 3-1~3-4가 통하고, 예보 한계면 격자 CNN(공간 구조)만 남는다
2. **2022년을 학습에서 뺄 것인가**
   - 뺀다: 라벨 품질이 나쁜 구간 제거. 단 group_3은 2023년부터만 있어 표본이 더 줄어든다
   - 가중만 낮춘다: 정보를 버리지 않음. 단 효과가 애매할 수 있다
   - 그대로 둔다: `B_norm`이 이미 가동률로 보정하므로 중복일 수 있다 ← **가능성 있음. 먼저 확인 필요**
3. **`A_asis` + τ=0.50을 안 해봤다** — v4는 라벨과 τ **둘 다** v2와 다르므로 `B_norm`의 순수 기여가
   분리되지 않았다. 로컬에서 같은 편향 수준 비교로는 `B_norm`이 +0.0058 우세.
   산식손실 MLP가 밴드 배치를 시각별로 처리하므로 **`B_norm`의 가치가 재평가될 수 있다**
4. **발전량 LightGBM 하이퍼파라미터 미튜닝** — `learning_rate`·`num_leaves`·`min_child_samples`·정규화가
   전부 기본값. 다만 **풍속 LightGBM에서 5방향 전부 기본값을 못 이긴** 전례가 있어 기대치는 낮다
5. **풍속 MLP(`WindMLP`) 미튜닝** — 512-256-128을 18-4에서 임의로 정했다. 풍속 정확도(σ)와 직결되므로
   "다음 할 일 2~3번" 작업 때 함께 볼 것

---

# 🌱 환경 메모
- `requirements.txt` **아직 없음** (5번 작업 때 작성). 필요 패키지:
  `lightgbm xgboost catboost scikit-learn pandas pyarrow torch==2.13.0+cpu`
- torch 설치: `pip install torch --index-url https://download.pytorch.org/whl/cpu`
- 새 기기: clone → venv → 위 설치 → `data/` 복사 → `01_preprocessing.ipynb` 실행으로 캐시 재생성
- `models/v5_parts.npz`(0.3 MB)는 **git에 올려도 무방**하다 (작고, 재생성에 51회 학습이 든다)
